# R7.10 Real-repository Benchmark — 2026-09-22

## Purpose

This document records the R7.10 pinned real-repository stress benchmark across successive authority repairs.

The benchmark answers a **safety / fail-closed** question. It is intentionally distinct from the R7.14 **positive-yield** acceptance gate.

## Current production candidate

```text
cd3b1d4b64168c4e95284299c5715b3db0e4da4b
fix: fail closed on structural visibility directives
```

Rereview #8 and the subsequent implementation hardening closed additional Angular render/visibility authority gaps: `ngNonBindable`, quoted-attribute tag-boundary confusion, fake control flow inside quoted attributes, nested/extra Angular control blocks, plain-text quote brace corruption, and legacy/custom `*structuralDirective` ancestors.

## Exact-SHA standard gates

All required standard gates passed on exact production `cd3b1d4b64168c4e95284299c5715b3db0e4da4b`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35714413695 — PASS
pinned Loren                                35714413680 — PASS
Loren-main canary                           35714413696 — PASS
pinned Jellyfin + parity/provenance         35714413688 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             194 / 194 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes         43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10688588126
sha256:140afc4cf9be4aa1c0961c4372a34d3c1d73abfbfabe805106adb839dab6c106
```

## Current benchmark execution

The unchanged pinned matrix was triggered from a temporary wrapper based on exact production `cd3b1d4b...`:

```text
base production: cd3b1d4b64168c4e95284299c5715b3db0e4da4b
wrapper commit:  41c73a4c6a16065c49981bbd59b3e6dbc022b3cc
workflow run:    35714494308
result:          PASS — 3 / 3 jobs
```

The wrapper only changes workflow trigger configuration. PKC production/test source is inherited from exact `cd3b1d4b...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Facts | Relations | Knowledge files | Artifact ID | Digest |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | 415 | 1,580 | 25 | `10688482518` | `sha256:13277c4e9f017e32b45e531ce6ee3306fa443b2c8456d073132d16f77bcb7997` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | 441 | 660 | 26 | `10688922105` | `sha256:0b53c682d59415ac5ca2a976413bdc1d27b386dd493b8e9790eadfe9d5287910` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | 488 | 2,054 | 28 | `10688757227` | `sha256:d03f6b00cedd49b60ac6aa1be330ea580e86789c670eced03e9259d64e76d1d6` |

## R7.9 / R7.10 semantic artifact inspection

Artifact ZIPs were opened and `.pkc/facts.json`, `.pkc/feature-candidates.json`, and generated Markdown were inspected directly.

For every repository:

```text
R7.9 rendered-value terminal: 0
selected API projection:      0
ui-member-visibility:         0
joint-visibility candidate:   0
combined visibility rule:     0
```

No repair manufactured unsupported cross-layer authority on the pinned real repositories.

## How to interpret the zeros

There are two separate acceptance questions:

### 1. Safety / fail-closed benchmark

> Can PKC run on unchanged real repositories and refuse to invent a full cross-layer claim when exact proof is missing?

**PASS.** All 3 repositories build/scan/generate knowledge successfully, and unsupported R7.9/R7.10 full-chain authority remains zero.

A zero here is a conservative success: PKC says “not proven” instead of guessing.

### 2. R7.14 positive-yield gate

> Can at least one unchanged real repository naturally produce the exact supported full cross-layer answer?

**NOT PASS yet.** None of these three pinned repositories naturally contains a supported full positive shape under the current bounded analyzer.

Therefore:

```text
benchmark safety/stability = PASS
R7.14 positive usefulness = NOT PASS
```

Zero R7.9/R7.10 positives does **not** mean PKC extracted nothing. The benchmark repos still produce hundreds of facts and relations, and large real systems such as pinned Jellyfin produce 43,365 facts, 195,316 relations, 386 workflows and 116 product features. The zero applies only to this narrow authoritative full-chain proof.

## Mutation-causality cross-check

The modern agentic Angular CRM artifact still contains exactly two raw `UpdatedAt` mutation facts:

```text
user.UpdatedAt
contact.UpdatedAt
```

Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Direct candidate inspection found zero `UpdatedAt` mutation promotion. The post-R7.9 mutation-causality blocker remains closed.

## Historical repaired benchmark checkpoints

```text
initial R7.10                  eb643092...  run 35646837448
post-rereview #1              da5d2377...  run 35650761753
post-rereview #2 hidden       97161baa...  run 35687153720
post-rereview #3 display      dc69e442...  run 35687951314
post-rereview #4 visibility   1fc4d212...  run 35696381103
post-rereview #5 template     e54b8444...  run 35699541953
post-rereview #6 bindings     5d43b180...  run 35702503746
post-rereview #7 comment      d785807d...  run 35704017705
rereview #8 hardening final   cd3b1d4b...  run 35714494308
```

Green benchmark/CI never substitutes for independent authority review; synthetic adversarial review has repeatedly found semantic counterexamples despite stable real-repository safety gates.

## Current conclusion

Exact production `cd3b1d4b...` remains fail closed on the three unchanged pinned repositories while accepted mutation-causality behavior remains intact.

It does **not** satisfy R7.14 because no current stress repository naturally emits a supported positive cross-layer answer.

## Next gate

Fresh independent rereview #9 must review exact production SHA:

```text
cd3b1d4b64168c4e95284299c5715b3db0e4da4b
```

Request: `docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-9-request.md`.

Only after that rereview finds no new compile-valid/runtime-valid false-positive blocker may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; V0.5 remains locked until E completes.
