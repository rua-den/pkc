# R7.10 Real-repository Benchmark — 2026-09-22

## Purpose

This document records the R7.10 pinned real-repository stress benchmark across successive render-authority repairs. It is a **stability / fail-closed gate**, not the R7.14 positive-yield gate.

## Current repaired production candidate

```text
1fc4d212b9c7add2f012f51adf3eef0c16f34dae
fix: reject static visibility hidden renders
```

The independent render-authority rereviews have successively exposed inert-template/comment/tag interpolation, static HTML `hidden`, static inline `display:none`, and static inline `visibility:hidden` false-positive classes.

Rereview #4 found this new compile-valid/runtime-valid shape on `dc69e442...`:

```html
<section style="visibility: hidden">
  @if (displayPrice > 0) {
    <strong>{{ displayPrice }}</strong>
  }
</section>
```

`1fc4d212...` repairs that bounded case by extending the existing static inline declaration parser to `visibility:hidden` / `visibility:hidden !important`, while retaining the earlier `display:none` handling. Dynamic style bindings, stylesheet/class CSS, browser cascade/computed CSS and general DOM-runtime behavior remain outside the claimed proof boundary.

## Exact-SHA standard gates

All required standard gates passed on exact production SHA `1fc4d212b9c7add2f012f51adf3eef0c16f34dae`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35696272391 — PASS
pinned Loren                                35696272323 — PASS
Loren-main canary                           35696272328 — PASS
pinned Jellyfin + parity/provenance         35696273519 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             167 / 167 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes         43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10680339386
sha256:a31f2b97674c0a18fa4b3587a1d422dc1a7228e75ba67015f86791ca73467031
```

## Current repaired benchmark execution

Workflow dispatch is not exposed through the connector, so the unchanged pinned matrix was triggered from a temporary wrapper branch based exactly on `1fc4d212...`:

```text
branch:         benchmark/r710-visibility-1fc4d212
wrapper commit: 7101b4c911539821c7c368203e0b05d150cbea64
workflow run:   35696381103
result:         PASS — 3 / 3 jobs
```

GitHub compare `1fc4d212...7101b4c9` confirms the wrapper differs by exactly one branch-trigger line in `.github/workflows/real-repo-benchmark.yml`. PKC production and test source are byte-identical to `1fc4d212...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Target build outcome | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1580 | 25 | `10679924411` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure | 0 | 441 | 660 | 26 | `10680915908` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure | 0 | 488 | 2054 | 28 | `10680019528` |

Artifact digests:

```text
jin12
sha256:723e597275aab0e116daa920ba62faec025f786c0572a2ca16176aa1498a98a4

agentic-angular
sha256:f0477e13b6c91f88eed8f99485961fdc285922ff269a9fec4b79b29acb83fee5

kesetovic-crm
sha256:0171e0091f4520adabbba43e3fa43add73bc07cf7e44f650615922a6e2fc79e0
```

The two target-build failures are unchanged benchmark-repository dependency/build-policy outcomes. PKC itself exits `0` and generates portable knowledge for all three source trees.

## R7.9 / R7.10 semantic artifact inspection

The three current artifact ZIPs were opened and `.pkc/facts.json`, `.pkc/feature-candidates.json`, and generated knowledge were inspected directly.

For every repository:

```text
R7.9 rendered-value terminal: 0
selected API projection:      0
ui-member-visibility:         0
joint-visibility candidate:   0
combined visibility rule:     0
```

Generated knowledge contains zero supported current R7.9/R7.10 combined visibility claim for these repositories. Therefore the visibility-hidden repair did not force unsupported real-repository shapes into cross-layer authority.

This remains safety evidence only. No benchmark repository naturally emits the bounded positive shape, so R7.14 remains **NOT PASS**.

## Agentic mutation-causality cross-check

The modern Angular CRM artifact still contains exactly two raw `UpdatedAt` mutation facts. Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Direct candidate inspection found:

```text
raw UpdatedAt mutation facts:        2
candidate UpdatedAt mutation facts:  0
false PO-facing UpdatedAt claims:    0
```

The post-R7.9 mutation-causality blocker therefore remains closed.

## Historical checkpoints

```text
initial R7.10
 eb64309263489a2b9bd658762b4526a4a32a8508
 benchmark run 35646837448 — PASS 3 / 3

post-rereview #1
 da5d23771ef8c9d58d0333d1f949e8d742210043
 benchmark run 35650761753 — PASS 3 / 3

post-rereview #2 static-hidden repair
 97161baa2d0aff9131a7acf9db752393ae913d64
 benchmark run 35687153720 — PASS 3 / 3

post-rereview #3 display-none repair
 dc69e44206942ffb0012e1994d6d39249d1db4be
 benchmark run 35687951314 — PASS 3 / 3
```

Each historical green benchmark was stability evidence only; later adversarial rereviews still found synthetic semantic counterexamples. Green benchmark/CI never substitutes for independent authority review.

## Current conclusion

On the three unchanged pinned repositories, exact production candidate `1fc4d212...` remains fail closed rather than inventing R7.9 rendered-value or R7.10 joint backend/frontend visibility authority, and accepted mutation-causality behavior remains intact.

It does **not** satisfy R7.14 because no current benchmark repository naturally emits a supported positive cross-layer answer.

## Next gate

Fresh independent rereview #5 must review exact production SHA:

```text
1fc4d212b9c7add2f012f51adf3eef0c16f34dae
```

Request: `docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-5-request.md`.

Only after that rereview finds no new compile-valid/runtime-valid blocker may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; V0.5 remains locked.
