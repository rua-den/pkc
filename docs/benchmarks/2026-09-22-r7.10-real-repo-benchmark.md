# R7.10 Real-repository Benchmark — 2026-09-22

## Purpose

This document records the R7.10 pinned real-repository stress benchmark across successive authority repairs. It is a **stability / fail-closed gate**, not the R7.14 positive-yield gate.

## Current repaired production candidate

```text
a3334bc202ee5a9e2dc8cc6d7c176dde8e06c9f9
fix: reject SVG definition renders
```

Independent rereview #10 found an SVG direct-render authority defect: interpolation under SVG definition containers such as `<defs>` and `<symbol>` could be mistaken for directly visible rendered text. The repair rejects direct-visible render authority beneath those containers without adding a general SVG/runtime renderer.

## Exact-SHA standard gates

All required standard gates passed on exact production SHA `a3334bc202ee5a9e2dc8cc6d7c176dde8e06c9f9`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35740454910 — PASS
pinned Loren                                35740454928 — PASS
Loren-main canary                           35740454970 — PASS
pinned Jellyfin + parity/provenance         35740454878 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             201 / 201 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes         43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10698544024
sha256:dc679ba75f586ef7e7f0fd4b3ac8e0f79c70ab496132896b2c7bbdece9d50ef4
```

## Current repaired benchmark execution

The unchanged pinned matrix was triggered from a temporary wrapper branch based exactly on `a3334bc...`:

```text
branch:         benchmark/r710-svg-a3334bc2
wrapper commit: e01326c66fef67679b53af459e0bb9208b5dd3f4
workflow run:   35740571925
result:         PASS — 3 / 3 jobs
```

GitHub compare confirms the wrapper differs by exactly one branch-trigger line in `.github/workflows/real-repo-benchmark.yml`. PKC production and test source are byte-identical to `a3334bc...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Target build | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1580 | 25 | `10699379067` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure, unchanged project condition | 0 | 441 | 660 | 26 | `10699408849` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure, unchanged project condition | 0 | 488 | 2054 | 28 | `10698434908` |

Artifact digests:

```text
jin12
sha256:6f46ad1b02c0bbe71a0d3ca1a8b325cf104f8a7224e369d0618bfea7d48dff8d

agentic-angular
sha256:639a4faa92653250a9d439461be261ec47a2efd68f9954c70278fd1aa3ed297d

kesetovic-crm
sha256:8b98a64b12457087cd1ea403c1ba024ce5d65bb6b92a9fa7efac895fda287fc9
```

## R7.9 / R7.10 semantic artifact inspection

The artifact ZIPs were opened and `.pkc/facts.json`, feature candidates, product features and generated Markdown were inspected directly.

For every repository:

```text
R7.9 rendered-value terminal:       0
selected API projection:            0
ui-member-visibility:               0
joint-visibility candidate:         0
combined visibility rule:           0
```

Therefore the SVG definition repair did not force unsupported real-repository shapes into R7.9/R7.10 authority.

## How to read this benchmark

### 1. Safety / fail-closed

> Can PKC run on unchanged real repositories without inventing a full cross-layer proof when the exact supported chain is absent?

**PASS.** All three repositories compile through PKC and remain at zero unsupported R7.9/R7.10 promotion.

A zero here is good: it means PKC did not join facts merely because names or nearby code looked similar.

### 2. Positive usefulness / R7.14

> Does at least one unchanged real repository naturally contain the complete supported backend → API → frontend → rendered-value/visibility chain so PKC emits a useful authoritative cross-layer answer?

**NOT PASS YET.** None of the current three stress repositories naturally yield the complete supported shape.

Therefore:

```text
Safety benchmark                  PASS
R7.14 positive real-project yield NOT PASS
```

Do not weaken authority or modify benchmark source merely to turn the zeros into positives.

## Mutation-causality cross-check

The modern agentic Angular CRM artifact still contains exactly two relevant raw `UpdatedAt` mutation facts:

```text
user.UpdatedAt
contact.UpdatedAt
```

Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Their exact fact IDs occur zero times in feature candidates, product features and generated Markdown. The post-R7.9 mutation-causality blocker remains closed.

## Historical repaired benchmark checkpoints

```text
initial R7.10                  eb643092...  run 35646837448
post-rereview #1              da5d2377...  run 35650761753
post-rereview #2 hidden       97161baa...  run 35687153720
post-rereview #3 display      dc69e442...  run 35687951314
post-rereview #4 visibility   1fc4d212...  run 35696381103
post-rereview #5 template     e54b8444...  run 35699541953
post-rereview #6 bindings     5d43b180...  run 35702503746
post-rereview #7 comment      d785807d...  run 35704017705
post-rereview #8 hardening    cd3b1d4b...  run 35714494308
post-rereview #9 style        1bc67e62...  run 35717582575
post-rereview #10 SVG         a3334bc2...  run 35740571925
```

Green benchmark/CI never substitutes for independent authority review; synthetic adversarial reviews have repeatedly found real semantic counterexamples despite stable real-repository gates.

## Current conclusion

Exact production `a3334bc...` remains fail closed on the three unchanged pinned repositories, while accepted mutation-causality behavior remains intact.

It does **not** satisfy R7.14 because no current stress repository naturally emits a supported positive cross-layer answer.

## Next gate

Fresh independent rereview #11 must review exact production SHA:

```text
a3334bc202ee5a9e2dc8cc6d7c176dde8e06c9f9
```

Request: `docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-11-request.md`.

Only after that rereview finds no new compile-valid/runtime-valid false-positive blocker may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; V0.5 remains locked until E completes.
