# R7.10 Real-repository Benchmark — 2026-09-22

## Purpose

This document records the R7.10 pinned real-repository stress benchmark across successive render-authority repairs. It is a **stability / fail-closed gate**, not the R7.14 positive-yield gate.

## Current repaired production candidate

```text
dc69e44206942ffb0012e1994d6d39249d1db4be
fix: reject static display none renders
```

Rereview #1 repaired false rendered-value authority for inert `<ng-template>`, HTML comments, and HTML tag/attribute interpolation. Rereview #2 repaired static HTML `hidden` ancestry. Rereview #3 then found static inline CSS could still suppress presentation while retaining render authority:

```html
<section style="display: none">
  @if (displayPrice > 0) {
    <strong>{{ displayPrice }}</strong>
  }
</section>
```

`dc69e442...` repairs that bounded case by rejecting simple interpolation beneath a static inline `display:none` / `display:none !important` ancestor. Dynamic style bindings, stylesheet/class CSS and computed browser CSS remain outside the claimed proof boundary.

## Exact-SHA standard gates

All required standard gates passed on exact production SHA `dc69e44206942ffb0012e1994d6d39249d1db4be`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35687831689 — PASS
pinned Loren                                35687831632 — PASS
Loren-main canary                           35687831587 — PASS
pinned Jellyfin + parity/provenance         35687831537 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             164 / 164 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes         43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10677695966
sha256:088353b7a1e30bf2aa68dce408246c2f88de50b843e88a7c57bb8fac595bdb60
```

## Current repaired benchmark execution

Workflow dispatch is not exposed through the connector, so the unchanged pinned matrix was triggered from a temporary wrapper branch based exactly on `dc69e442...`:

```text
branch:         benchmark/r710-css-dc69e442
wrapper commit: b3b0c5b40c0d34cafe3e7a60c28341ea777dd2cf
workflow run:   35687951314
result:         PASS — 3 / 3 jobs
```

The wrapper differs from production only by the benchmark workflow branch-trigger line; PKC production/test source is byte-identical to `dc69e442...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Target build outcome | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1580 | 25 | `10676974702` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure | 0 | 441 | 660 | 26 | `10676969751` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure | 0 | 488 | 2054 | 28 | `10677306261` |

Artifact digests:

```text
jin12
sha256:640cfe616ac523f6e868446b31ce467644b82920b7ba20044340cfd5b652ffcc

agentic-angular
sha256:c9f3106843d14f368abb37da9d0b61e391d943567f2c45511f61ae1150ce4723

kesetovic-crm
sha256:ef3e57e3138492b87d0e8c12c8204e63e977487f46a3ce3c20a6869f05ca1408
```

The two target-build failures are unchanged benchmark-repository dependency/build-policy outcomes. PKC itself exits `0` and generates portable knowledge for all three source trees.

## R7.9 / R7.10 semantic artifact inspection

The three artifact ZIPs were opened and `.pkc/facts.json`, `.pkc/feature-candidates.json`, and generated knowledge were inspected directly.

For every repository:

```text
R7.9 rendered-value terminal: 0
selected API projection:      0
ui-member-visibility:         0
joint-visibility candidate:   0
combined visibility rule:     0
```

Generated knowledge contains zero `Combined observable visibility` rule for these repositories. Therefore the static-CSS repair did not force unsupported real-repository shapes into R7.9/R7.10, but R7.14 positive real-project yield remains **NOT PASS**.

## Agentic mutation-causality cross-check

The modern Angular CRM still contains exactly two raw `UpdatedAt` mutation facts. Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Artifact inspection found:

```text
raw UpdatedAt mutation facts:        2
candidate UpdatedAt mutation facts:  0
transitive-mutation warning:         retained
false PO-facing UpdatedAt claims:    0
```

The post-R7.9 mutation-causality blocker remains closed.

## Historical checkpoints

```text
initial R7.10
 eb64309263489a2b9bd658762b4526a4a32a8508
 benchmark run 35646837448 — PASS 3 / 3

post-rereview #1
 da5d23771ef8c9d58d0333d1f949e8d742210043
 benchmark run 35650761753 — PASS 3 / 3

post-rereview #2 static-hidden repair
 97161baa2d0aff9131a7acf9db752393ae913d64
 benchmark run 35687153720 — PASS 3 / 3
```

Each historical green benchmark was stability evidence only; later adversarial rereviews still found synthetic semantic counterexamples. Green benchmark/CI never substitutes for independent authority review.

## Current conclusion

On the three unchanged pinned repositories, exact production candidate `dc69e442...` remains fail closed rather than inventing R7.9 rendered-value or R7.10 joint backend/frontend visibility authority, and accepted mutation-causality behavior remains intact.

It does **not** satisfy R7.14 because no current benchmark repository naturally emits a supported positive cross-layer answer.

## Next gate

Fresh independent rereview #4 must review exact production SHA:

```text
dc69e44206942ffb0012e1994d6d39249d1db4be
```

Request: `docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-4-request.md`.

Only after that rereview finds no new compile-valid/runtime-valid blocker may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; V0.5 remains locked.
