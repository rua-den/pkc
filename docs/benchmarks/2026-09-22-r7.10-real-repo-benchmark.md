# R7.10 Real-repository Benchmark — 2026-09-22

## Purpose

Re-run the pinned three-repository matrix after the R7.10 joint-visibility implementation. This is a stability/fail-closed benchmark, not the R7.14 positive-yield gate.

## PKC implementation under test

```text
eb64309263489a2b9bd658762b4526a4a32a8508
fix: bound Angular visibility to active template structure
```

The GitHub connector does not expose workflow dispatch, so the benchmark used a temporary branch based exactly on that implementation SHA:

```text
branch:          benchmark/r710-eb643092
wrapper commit:  0bd0e501cc2bfc1f04d7cee275f43e5f888b3d1c
workflow run:    35646837448
result:          PASS — 3 / 3 jobs
```

The wrapper differs from the implementation only in `.github/workflows/real-repo-benchmark.yml` trigger scope. PKC source/test logic is byte-identical.

## Pinned repositories

| Benchmark | Exact SHA | Target build | PKC exit | Facts | Relations | Knowledge files |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1580 | 25 |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure | 0 | 441 | 660 | 26 |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure | 0 | 488 | 2054 | 28 |

The two target-build failures remain benchmark-repository dependency/build-policy outcomes. PKC itself exits `0` and emits portable knowledge for all three.

## R7.9 / R7.10 yield

Artifact inspection of `.pkc/facts.json`, `.pkc/feature-candidates.json` and generated Markdown found, for every benchmark repository:

```text
ui-member-visibility facts:             0
rendered UI value terminals:            0
selected API projection facts:          0
joint-visibility candidate facts:       0
Combined observable visibility rules:   0
```

Therefore:

- no unsupported real-repository shape was force-composed into R7.10;
- the new R7.10 code does not create false positive joint visibility on these three repositories;
- the existing benchmarks still do not naturally exercise the accepted R7.9/R7.10 bounded positive shape;
- R7.14 positive real-project yield remains **NOT YET PASS**.

Do not weaken the accepted proof boundary merely to make these repositories emit a positive.

## Mutation-causality regression cross-check

The modern Angular CRM still contains two raw timestamp mutation facts:

```text
user.UpdatedAt
contact.UpdatedAt
```

Both remain correctly labeled:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Artifact inspection found:

```text
raw UpdatedAt mutation facts:        2
candidate UpdatedAt mutation facts:  0
transitive-mutation warning:         retained
```

Generated PO-facing knowledge contains no reintroduced `UpdatedAt` state-change claim from those AppDbContext timestamp mutations.

## R7.10 authority conclusion from this benchmark

The benchmark supports the release-safety claim:

> R7.10 remains fail closed on these unchanged real repositories rather than correlating backend predicates and frontend visibility by textual similarity.

It does **not** support the product-usefulness claim required by R7.14 because the benchmark emits no positive R7.9/R7.10 chain.

## Next gate

Automated exact-SHA standard gates and this benchmark are green. R7.10 / all of D still require independent rereview of exact implementation SHA:

```text
eb64309263489a2b9bd658762b4526a4a32a8508
```

Review request:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-review-request.md`
