# R7.10 Real-repository Benchmark — 2026-09-22

## Purpose

This document records the R7.10 pinned real-repository stress benchmark across the successive render-authority repairs.

This benchmark is a **stability / fail-closed gate**. It is **not** the R7.14 positive-yield gate. Zero supported cross-layer positives are acceptable here if the unchanged repositories do not naturally fit the bounded proof shape.

## Current repaired production candidate

```text
97161baa2d0aff9131a7acf9db752393ae913d64
fix: reject statically hidden rendered text
```

Independent rereview #1 had repaired false rendered-value authority for:

```text
bare/inert <ng-template>
HTML comments
HTML tag/attribute interpolation
```

Independent rereview #2 then found another runtime-valid false-authority shape:

```html
<section hidden>
  @if (displayPrice > 0) {
    <strong>{{ displayPrice }}</strong>
  }
</section>
```

The static HTML `hidden` boolean attribute suppresses presentation of the subtree. Production candidate `97161baa...` repairs that boundary by rejecting simple interpolation beneath a statically hidden ancestor while deliberately not claiming general dynamic `[hidden]`, CSS or DOM-runtime semantics.

## Exact-SHA standard gates

All required standard gates passed on exact production SHA `97161baa2d0aff9131a7acf9db752393ae913d64`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35686749488 — PASS
pinned Loren                                35686749493 — PASS
Loren-main canary                           35686749523 — PASS
pinned Jellyfin + parity/provenance         35686749575 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             161 / 161 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes         43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10676304879
sha256:7dcfed74eadb0146ab422c497953d8054ed01d5fddb6b6e6013a37b7729fd28b
```

## Current repaired benchmark execution

Workflow dispatch is not exposed through the connector, so the unchanged pinned matrix was triggered from a temporary wrapper branch based exactly on production SHA `97161baa...`:

```text
branch:         benchmark/r710-hidden-97161baa
wrapper commit: 850eea0b64f464bdc1a5ea536adb39d4b912c650
workflow run:   35687153720
result:         PASS — 3 / 3 jobs
```

GitHub compare `97161baa...850eea0b` confirms the wrapper differs by only the benchmark branch-trigger line in `.github/workflows/real-repo-benchmark.yml`. PKC production and test source are byte-identical to `97161baa...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Target build outcome | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1580 | 25 | `10677178181` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure | 0 | 441 | 660 | 26 | `10677435079` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure | 0 | 488 | 2054 | 28 | `10676444714` |

Artifact digests:

```text
jin12
sha256:ff0da4dc8cf0cafcd52a63bca3fc557d3a3159493d3c2b853461b8c529f89713

agentic-angular
sha256:364d5b8a567ae7ba40ba12a564b6019eec2e0629c5a117a6c9aca63f0220bccb

kesetovic-crm
sha256:b77758005534bfb292c965aec52fbda1b146667655db0279561b20a6a558d09d
```

The two target-build failures are unchanged benchmark-repository dependency/build-policy outcomes. PKC itself exits `0` and generates portable knowledge for all three source trees.

## R7.9 / R7.10 semantic artifact inspection

The three artifact ZIPs were opened and `.pkc/facts.json`, `.pkc/feature-candidates.json`, and generated knowledge were inspected directly.

For **every** repository:

```text
R7.9 rendered-value terminal: 0
selected API projection:      0
ui-member-visibility:         0
joint-visibility candidate:   0
combined visibility rule:     0
```

Generated knowledge also contains zero `Combined observable visibility` rule and zero R7.10 frontend rendered-visibility rule for these repositories.

Therefore:

- the static-hidden repair did not force unsupported real-repository shapes into R7.9/R7.10;
- no unsupported rendered-value authority or joint visibility was introduced;
- the existing three repositories still do not naturally exercise the current bounded positive cross-layer shape;
- R7.14 positive real-project yield remains **NOT PASS**.

Do not weaken the proof boundary merely to manufacture benchmark yield.

## Agentic mutation-causality cross-check

The modern Angular CRM artifact still contains exactly two raw timestamp mutation facts:

```text
user.UpdatedAt
contact.UpdatedAt
```

Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
analysisCaveat = runtime-pattern-selected-receiver-requires-caller-object-proof
```

Artifact inspection found:

```text
raw UpdatedAt mutation facts:        2
candidate UpdatedAt mutation facts:  0
transitive-mutation warning:         retained
false PO-facing UpdatedAt claims:    0
```

The post-R7.9 mutation-causality blocker therefore remains closed.

## Historical repaired benchmark after rereview #1

The previous candidate was:

```text
da5d23771ef8c9d58d0333d1f949e8d742210043
fix: require visible text interpolation
```

Its wrapper benchmark was:

```text
branch:         benchmark/r710-rereview1-05ad6cb9
wrapper commit: d38b21a68e007c1a85a8ec8e906a08f0db440661
run:            35650761753 — PASS, 3 / 3 jobs
```

That run also produced zero supported R7.9/R7.10 positives, but independent rereview #2 later demonstrated a synthetic static-hidden false-authority shape that the real repositories did not contain. This is why benchmark green is evidence of stability, not semantic completeness.

## Initial diagnostic benchmark

Initial pre-rereview implementation:

```text
eb64309263489a2b9bd658762b4526a4a32a8508
fix: bound Angular visibility to active template structure
```

Diagnostic wrapper run:

```text
branch:         benchmark/r710-eb643092
wrapper commit: 0bd0e501cc2bfc1f04d7cee275f43e5f888b3d1c
run:            35646837448 — PASS, 3 / 3 jobs
```

This likewise demonstrates why a green stress benchmark cannot replace adversarial independent semantic rereview.

## Current conclusion

The current benchmark supports this release-safety claim:

> On these three unchanged repositories, exact production candidate `97161baa...` remains fail closed rather than inventing R7.9 rendered-value or R7.10 joint backend/frontend visibility authority, and the accepted mutation-causality boundary remains intact.

It does **not** satisfy R7.14 product usefulness because no current benchmark repository naturally produces a supported positive cross-layer answer.

## Next gate

The implementation session that produced `97161baa...` cannot self-certify the repair. The next gate is fresh independent rereview #3 of exact production SHA:

```text
97161baa2d0aff9131a7acf9db752393ae913d64
```

Request:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-3-request.md`

Only after that rereview finds no new compile-valid/runtime-valid counterexample may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; V0.5 remains locked.
