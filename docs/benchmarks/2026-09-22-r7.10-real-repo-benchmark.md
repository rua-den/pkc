# R7.10 Real-repository Benchmark — 2026-09-22

## Purpose

This document records both the initial R7.10 benchmark and the repaired-candidate rerun after independent rereview #1 found false rendered-value authority at the Angular template boundary.

This benchmark is a stability/fail-closed gate. It is **not** the R7.14 positive-yield gate.

## Final repaired PKC candidate under test

```text
da5d23771ef8c9d58d0333d1f949e8d742210043
fix: require visible text interpolation
```

The first independent rereview found three compile-valid Angular shapes that could previously be over-promoted as rendered UI values:

```html
<ng-template>
  @if (displayPrice > 0) {
    {{ displayPrice }}
  }
</ng-template>

<!-- {{ displayPrice }} -->

<div data-price="{{ displayPrice }}"></div>
```

Those were repaired regression-first in:

```text
05ad6cb937010702a3fd01d5ef756e31bc4e8d9b
099fabfcaeecbaf009b75052d20075745ee02437
da5d23771ef8c9d58d0333d1f949e8d742210043
```

The resulting authority boundary accepts only active visible text interpolation inside the current bounded grammar. Commented interpolation, tag/attribute interpolation, and interpolation under an inert `<ng-template>` ancestor fail closed.

## Exact standard gates before benchmark rerun

All required standard gates passed on exact production SHA `da5d23771ef8c9d58d0333d1f949e8d742210043`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35650084914 — PASS
pinned Loren                                35650084917 — PASS
Loren-main canary                           35650084926 — PASS
pinned Jellyfin + parity/provenance         35650084759 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             155 / 155 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay build       PASS
PokeTrade            PASS
```

## Repaired benchmark execution

Workflow dispatch is not exposed through the connector, so the pinned matrix was triggered from a temporary wrapper branch based exactly on `da5d2377...`:

```text
branch:          benchmark/r710-rereview1-05ad6cb9
wrapper commit:  d38b21a68e007c1a85a8ec8e906a08f0db440661
workflow run:    35650761753
result:          PASS — 3 / 3 jobs
```

GitHub compare `da5d2377...d38b21a` proves the wrapper differs by exactly one branch-trigger line in `.github/workflows/real-repo-benchmark.yml`. PKC production and test source are byte-identical to `da5d2377...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Target build outcome | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1580 | 25 | `10662460505` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure | 0 | 441 | 660 | 26 | `10661878054` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure | 0 | 488 | 2054 | 28 | `10662505428` |

Artifact digests:

```text
jin12
sha256:f846b5384cc6472bf76ee469bfda888d5721664a440d2f18bf2e358d2f130753

agentic-angular
sha256:1a0e1eae1af7cba2c786dfb2c38050387f42dd6f2cc00708af4020ecd9673286

kesetovic-crm
sha256:48e3620862ec46aee4a1dbb119326be81fda60d38f8e44881208b1605bc1d399
```

The two target-build failures are benchmark-repository dependency/build-policy outcomes already known from the earlier run. PKC itself exits `0` and generates portable knowledge for all three unchanged source trees.

## R7.9 / R7.10 cross-layer yield

Artifact inspection of `.pkc/facts.json`, `.pkc/feature-candidates.json` and generated knowledge found, for every benchmark repository:

```text
R7.9 rendered-value terminal: 0
selected API projection:      0
ui-member-visibility:         0
joint-visibility candidate:   0
combined visibility rule:     0
```

Therefore:

- no unsupported real-repository shape was force-composed into R7.9/R7.10;
- the repaired rendered-value authority did not introduce false joint visibility;
- the existing three repositories still do not naturally exercise the supported bounded positive shape;
- R7.14 positive real-project yield remains **NOT YET PASS**.

Do not weaken the accepted proof boundary merely to manufacture a benchmark positive.

## Agentic mutation-causality cross-check

The modern Angular CRM still contains two raw timestamp mutation facts:

```text
user.UpdatedAt
contact.UpdatedAt
```

Both remain labeled:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Artifact inspection on the repaired candidate found:

```text
raw UpdatedAt mutation facts:        2
candidate UpdatedAt mutation facts:  0
transitive-mutation warning:         retained
false PO-facing UpdatedAt claims:    0
```

The post-R7.9 mutation-causality blocker therefore remains closed.

## Comparison with initial R7.10 benchmark

The initial pre-rereview implementation was:

```text
eb64309263489a2b9bd658762b4526a4a32a8508
fix: bound Angular visibility to active template structure
```

Initial diagnostic benchmark:

```text
branch:         benchmark/r710-eb643092
wrapper commit: 0bd0e501cc2bfc1f04d7cee275f43e5f888b3d1c
run:            35646837448 — PASS, 3 / 3 jobs
```

That run already showed conservative zero positive yield, but independent rereview later discovered that the synthetic/frontend authority boundary still admitted inert or non-visible interpolation shapes. Therefore `eb643092...` is **not** the final review candidate. Astra must review `da5d2377...`.

## Current conclusion

The repaired benchmark supports this release-safety claim:

> On these three unchanged repositories, PKC remains fail closed rather than inventing rendered-value or joint backend/frontend visibility authority.

It does **not** satisfy R7.14 product usefulness because no current benchmark naturally produces a supported positive cross-layer answer.

## Next gate

Rereview #1 found and repaired blockers, so it cannot self-certify the repair. The next gate is independent rereview #2 of exact production SHA:

```text
da5d23771ef8c9d58d0333d1f949e8d742210043
```

Astra request:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-2-request.md`

Only after Astra finds no new compile-valid/runtime-valid counterexample may R7.10 and all of D be marked PASS / COMPLETE and E be unlocked.
