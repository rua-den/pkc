# R7.10 Native-SVG Fail-closed Real-repository Benchmark — 2026-09-23

## Purpose

This document records the pinned real-repository safety benchmark for the current R7.10 native-SVG fail-closed repair. It is a **stability / fail-closed gate**, not the R7.14 positive-yield gate.

## Exact production candidate

```text
7818c7ed646b30cb7b8505f053572783e075af6f
fix: fail closed on native SVG render authority
```

The repair intentionally stops claiming authoritative directly-visible render proof for native SVG interpolation. Ordinary HTML remains supported, including HTML under SVG `foreignObject`.

## Exact-SHA standard gates

All required standard gates passed on exact production SHA `7818c7ed646b30cb7b8505f053572783e075af6f`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35765278584 — PASS
pinned Loren                                35765278607 — PASS
Loren-main canary                           35765278416 — PASS
pinned Jellyfin + parity/provenance         35765278447 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             231 / 231 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes        43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10712181821
sha256:281403635a62680c71640f0a839fdbeffdbcaf72d59b38407789c65037c59adb
```

## Benchmark execution

The unchanged pinned matrix was triggered from a temporary wrapper branch based exactly on `7818c7ed...`:

```text
branch:         benchmark/r710-native-svg-7818c7ed
wrapper commit: 87255ba6f5f012d82ee17f039d540db6bbdf01bf
workflow run:   35765659218
result:         PASS — 3 / 3 jobs
```

GitHub compare confirms the wrapper differs from production by exactly one branch-trigger line in `.github/workflows/real-repo-benchmark.yml`. PKC production and test source are byte-identical to `7818c7ed...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | 0 | 415 | 1580 | 25 | `10711409458` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | 0 | 441 | 660 | 26 | `10711259746` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | 0 | 488 | 2054 | 28 | `10711519386` |

Artifact digests:

```text
jin12
sha256:15b9a85614984f05aef447bbfeb89cd08ad111d5ed1e531b869d809e481a1766

agentic-angular
sha256:c3f2c77a4f3132431685d75bb5c56c8dbbfe5564f39ed10f795247c724907a74

kesetovic-crm
sha256:4175f1171a61c9d9c95016c40f55a7c861b8c1b7762e4b9bcc44cf2b5b37224d
```

## R7.9 / R7.10 semantic artifact inspection

The artifact ZIPs were downloaded and `.pkc/facts.json`, feature candidates, product features, `PKC_KNOWLEDGE.md` and canonical knowledge Markdown were inspected directly.

For every repository:

```text
R7.9 rendered-value terminal:       0
selected API projection:            0
ui-member-visibility:               0
joint-visibility candidate:         0
combined visibility rule:           0
```

The exact R7.9/R7.10 authority markers are also absent from feature-candidate and product-feature projections. Therefore the native-SVG fail-closed repair did not force unsupported real-repository shapes into cross-layer authority.

## How to read this benchmark

### 1. Safety / fail-closed

> Can PKC run on unchanged real repositories without inventing a full cross-layer proof when the exact supported chain is absent?

**PASS.** All three repositories compile through PKC and remain at zero unsupported R7.9/R7.10 promotion.

A zero here is good: PKC did not join facts merely because names or nearby code looked similar.

### 2. Positive usefulness / R7.14

> Does at least one unchanged real repository naturally contain the complete supported backend → API → frontend → rendered-value/visibility chain so PKC emits a useful authoritative cross-layer answer?

**NOT PASS.** None of the current three stress repositories naturally yields the complete supported shape.

Therefore:

```text
Safety benchmark                  PASS
R7.14 positive real-project yield NOT PASS
```

Do not weaken authority, re-enable unsupported native SVG, or modify benchmark source merely to turn the zeros into positives.

## Mutation-causality cross-check

The modern agentic Angular CRM artifact still contains exactly two relevant raw `UpdatedAt` mutation facts:

```text
cs:backend/CrmApi/Data/AppDbContext.cs:68:mutation:user.UpdatedAt
cs:backend/CrmApi/Data/AppDbContext.cs:70:mutation:contact.UpdatedAt
```

Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Each exact mutation fact ID occurs zero times in:

```text
.pkc/feature-candidates.json
.pkc/product-features.json
PKC_KNOWLEDGE.md
knowledge/**/*.md
```

The post-R7.9 mutation-causality blocker remains closed.

## Historical context

The preceding benchmark record is:

`docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`.

It records the earlier SVG-resource candidate and historical repaired benchmark checkpoints. This new record supersedes its "current candidate" section but does not rewrite that historical evidence.

Green benchmark/CI never substitutes for independent authority review; prior adversarial rounds repeatedly found semantic counterexamples despite stable real-repository gates.

## Current conclusion

Exact production `7818c7ed...` remains fail closed on all three unchanged pinned repositories, portable/provenance behavior remains stable on Jellyfin, and accepted mutation-causality behavior remains intact.

It does **not** satisfy R7.14 because no current stress repository naturally emits a supported positive cross-layer answer.

## Next gate

Fresh independent rereview #13 must review exact production SHA:

```text
7818c7ed646b30cb7b8505f053572783e075af6f
```

Request: `docs/reviews/2026-09-23-v0.4.7-d-r7.10-rereview-13-request.md`.

Only after that rereview finds no new compile-valid/runtime-valid false-positive blocker may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; later productization/ADO work remains locked.
