# R7.10 HTML Projection / Direct-text Fail-closed Real-repository Benchmark — 2026-09-23

## Purpose

This document records the pinned real-repository safety benchmark for the current R7.10 Angular content-projection and HTML direct-text authority repairs. It is a **stability / fail-closed gate**, not the R7.14 positive-yield gate.

## Exact production candidate

```text
3e6fa7749eb8ef47be4eedb72d1c159cd502692f
fix: bound Angular direct text containers
```

Direct repaired predecessor:

```text
34182e221df6cf50eaa0ac362a5f575e80236a64
fix: reject unproven Angular content projection
```

These repairs build on the native-SVG fail-closed boundary from `7818c7ed646b30cb7b8505f053572783e075af6f`.

The current authority boundary intentionally rejects:

- interpolation below an Angular component/custom-element host when content projection is not proven;
- child text below bounded HTML metadata/fallback/conditional containers such as `title`, `canvas`, `dialog`, `details`, `object` and `noscript`;
- native SVG interpolation for authoritative directly-visible render proof.

Ordinary HTML remains eligible, including supported HTML descendants under SVG `foreignObject`, subject to the same HTML/projection authority rules.

## Exact-SHA standard gates

All required standard gates passed on exact production SHA `3e6fa7749eb8ef47be4eedb72d1c159cd502692f`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35769202107 — PASS
pinned Loren                                35769202115 — PASS
Loren-main canary                           35769202043 — PASS
pinned Jellyfin + parity/provenance         35769202074 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             241 / 241 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes        43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10713378704
sha256:cca8adf6d31a4c0207eaea58e5935147d279a7e9e3db361ad6b32291a0807198
```

Local repository execution is not claimed because the execution container could not resolve `github.com` while cloning and has no local .NET toolchain. Exact-SHA GitHub verification above is the completed validation evidence.

## Benchmark execution

The unchanged pinned matrix was triggered from a temporary wrapper branch based exactly on `3e6fa774...`:

```text
branch:         benchmark/r710-html-projection-3e6fa774
wrapper commit: 28c9758ba5ae172bb0ee52e032a28fa24fb1f917
workflow run:   35769939151
result:         PASS — 3 / 3 jobs
```

GitHub compare confirms the wrapper differs from production by exactly one branch-trigger line in `.github/workflows/real-repo-benchmark.yml`. PKC production and test source are byte-identical to `3e6fa774...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Target build | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1,580 | 25 | `10713667423` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure | 0 | 441 | 660 | 26 | `10713841164` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure | 0 | 488 | 2,054 | 28 | `10713209096` |

The benchmark intentionally preserves PKC evidence even when an unchanged target repository does not build cleanly in the benchmark runner. Target-build failure is therefore recorded, not hidden or converted into PKC success.

Artifact digests:

```text
jin12
sha256:10bb68b7e10714d6c217bce776c09737dfe0330279775840b4bd8b45c7387f72

agentic-angular
sha256:cbbbcc2b9dd42d1bf3b2978ed56e72d057a3452d9e7844cd4b6cc7e14a41863d

kesetovic-crm
sha256:d4f24200afff9a532514731ce93f18f802bfece6117a8f0b9fc386cc4d1a1d39
```

## R7.9 / R7.10 semantic artifact inspection

The artifact ZIPs were downloaded and inspected directly. For every repository:

```text
ui-member-render:           0
ui-member-visibility:       0
renderAuthority markers:    0
selected API/R7.9 terminal: 0
joint/combined visibility:  0
```

Therefore the projection/direct-text repairs did not force unsupported real-repository shapes into R7.9/R7.10 authority.

## Canonical-output stability

Each new benchmark artifact was compared with the preceding safety benchmark artifact for the same pinned repository.

All canonical PKC evidence files are byte-identical between the two benchmark executions. The only differing entry is the generated nested `PKC_KNOWLEDGE.zip`, whose archive bytes may differ because of ZIP metadata/timestamps even when its canonical knowledge inputs are unchanged.

This is stronger than a green workflow alone: the authority repairs changed no canonical output on the current pinned safety corpus.

## Mutation-causality cross-check

The modern agentic Angular CRM artifact still contains exactly two relevant raw `UpdatedAt` mutation facts:

```text
cs:backend/CrmApi/Data/AppDbContext.cs:68:mutation:user.UpdatedAt
cs:backend/CrmApi/Data/AppDbContext.cs:70:mutation:contact.UpdatedAt
```

Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Each exact mutation fact ID occurs zero times outside raw facts in feature candidates, product features and generated canonical/portable Markdown. The post-R7.9 mutation-causality blocker remains closed.

## How to read this benchmark

### Safety / fail-closed

> Can PKC run on unchanged real repositories without inventing cross-layer render/visibility authority when the exact supported proof chain is absent?

**PASS.** All three benchmark jobs complete and all three artifacts remain at zero unsupported R7.9/R7.10 promotion.

### Positive usefulness / R7.14

> Does at least one unchanged real repository naturally contain the complete supported backend → API → frontend → direct-render/visibility chain so PKC emits a useful authoritative cross-layer answer?

**NOT PASS.** None of the current three stress repositories naturally yields the complete supported shape.

Therefore:

```text
Safety benchmark                  PASS
R7.14 positive real-project yield NOT PASS
```

Do not weaken authority, re-enable unsupported SVG/browser behavior, or modify benchmark source merely to turn the zeros into positives.

## Current conclusion

Exact production `3e6fa774...` remains fail closed on the three unchanged pinned repositories. Jellyfin project-semantic/parity behavior remains stable, and the accepted mutation-causality boundary remains intact.

The benchmark does **not** satisfy R7.14 because no current stress repository naturally emits a supported positive V0.4.7 cross-layer answer.

## Next gate

Fresh independent rereview #14 must review exact production SHA:

```text
3e6fa7749eb8ef47be4eedb72d1c159cd502692f
```

Request: `docs/reviews/2026-09-23-v0.4.7-d-r7.10-rereview-14-request.md`.

Only after that rereview finds no new compile-valid/runtime-valid false-positive blocker may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; V0.5 and the prepared AI-workspace/update work remain locked until V0.4.7 completes.
