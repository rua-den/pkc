# R7.10 Nested Angular Dependency Selector Real-repository Benchmark — 2026-09-23

## Purpose

This document records the pinned real-repository safety benchmark for the R7.10 external-component selector and nested `node_modules` projection-authority repair. It is a **stability / fail-closed gate**, not the R7.14 positive-yield gate.

## Exact production candidate

```text
47e098dbe910b7f6cfd933a0595370524bec1fb2
fix: correct resolved package root type
```

Relevant repair chain:

```text
8f667abc819f048b3dc85fc834677b7ca30f5518  fix: recognize external Angular component selectors
67c67fc25b6488dbc9f110a1b05c53a4bfee1a6c  fix: resolve nested Angular dependency selectors
47e098dbe910b7f6cfd933a0595370524bec1fb2  fix: correct resolved package root type
```

Independent rereview #14 had failed predecessor `3e6fa774...` because external Angular dependency components could use native-looking selectors absent from the product-only component inventory. `8f667abc...` introduced bounded imported-package Ivy component selector discovery. Continuation review then found root-only `node_modules` lookup missed nested Angular applications. `67c67fc...` changed external package resolution to follow the importing TypeScript file upward and select the nearest dependency tree; `47e098...` is the compile-correct final form.

## Authority boundary under test

The current implementation intentionally treats unproven component projection as non-authoritative direct render. Relevant selector knowledge may come from:

- local product-source Angular `@Component` selectors;
- bounded imported dependency `ɵɵComponentDeclaration` selectors;
- nearest or root-hoisted imported package roots discovered through Node-style ancestor `node_modules` lookup.

The dependency declaration scan is not product evidence. `node_modules` remains excluded from product facts and generated knowledge, and directive declarations do not become projection blockers merely because they have selectors.

## Exact-SHA standard gates

All standard gates passed on exact production `47e098dbe910b7f6cfd933a0595370524bec1fb2`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35818350915 — PASS
pinned Loren                                35818350859 — PASS
Loren-main canary                           35818350997 — PASS
pinned Jellyfin + parity/provenance         35818350930 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             248 / 248 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin verification:

```text
source build          PASS, 0 warnings / 0 errors
facts                 43,365
relations             195,316
workflow candidates   386
product features      116
knowledge Markdown    504 files
analysis modes        43,365 / 43,365 project-semantic
portable parity       PASS
artifact              10732606989
sha256:1d2b38aadc18828a75625ea94651f2a1acfc3a3813a3cc9df0c57d9be4b1bde2
```

Local repository execution is not claimed because the available execution container could not resolve `github.com` and has no local .NET toolchain. The exact-SHA GitHub verification above is the completed validation evidence.

## Benchmark execution

The unchanged pinned matrix was triggered from a temporary wrapper branch based exactly on production `47e098...`:

```text
branch:         benchmark/r710-nested-node-modules-47e098
wrapper commit: 437d14a9b9ed36ce24e7fd8edfb2cef31eed7f6c
workflow run:   35818835753
result:         PASS — 3 / 3 jobs
```

GitHub compare confirms the wrapper differs from production by exactly one branch-trigger line in `.github/workflows/real-repo-benchmark.yml`. PKC production and test source are byte-identical to `47e098...`.

## Pinned repositories and artifact results

| Benchmark | Exact target SHA | Target build | PKC exit | Facts | Relations | Knowledge files | Artifact ID |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `jin12-xyz/CRM` | `00493af54d4d9e146d1c6eb75f5dc8f3898f09ec` | success | 0 | 415 | 1,580 | 25 | `10732532350` |
| `hackersandwizards/agentic-engineering-training-angular` | `22f2aab64617f4de7984370a5bd40e8c9535dbf5` | failure | 0 | 441 | 660 | 26 | `10733025143` |
| `kesetovic/crm-system` | `8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7` | failure | 0 | 488 | 2,054 | 28 | `10732851433` |

The benchmark preserves PKC evidence when an unchanged target repository does not build cleanly in the runner. Target-build failure is recorded rather than hidden or converted into PKC success.

Artifact digests:

```text
jin12
sha256:6f4e41e7b18357d5a06036bd14a39b15988ba437b013f78b0ecfee9af9b15f9f

agentic-angular
sha256:6c360ae70b72d9d8678b0d209f233d565f176a2aa878a5a20d62876638adb040

kesetovic-crm
sha256:16a95ee2bbb4155d33a1c3f6dfdf60767f036d389a83bff98e2954a86f348c54
```

## R7.9 / R7.10 semantic artifact inspection

All three artifact ZIPs were downloaded and inspected directly. For every repository:

```text
ui-member-render:           0
ui-member-visibility:       0
renderAuthority markers:    0
selected API/R7.9 terminal: 0
joint/combined visibility:  0
```

Exact fact/relation/knowledge counts remain:

```text
jin12       415 / 1,580 / 25
agentic     441 /   660 / 26
kesetovic   488 / 2,054 / 28
```

Therefore external-selector discovery and nearest dependency resolution did not force unsupported real-repository shapes into R7.9/R7.10 authority.

## Mutation-causality cross-check

The modern agentic Angular CRM artifact still contains the same two relevant raw `UpdatedAt` mutation facts:

```text
cs:backend/CrmApi/Data/AppDbContext.cs:68:mutation:user.UpdatedAt
cs:backend/CrmApi/Data/AppDbContext.cs:70:mutation:contact.UpdatedAt
```

Both retain:

```text
mutationReceiverOrigin = runtime-pattern-variable
mutationCausalityBoundary = caller-object-unproven
```

Each exact mutation fact ID occurs zero times outside raw facts in feature candidates, product features and generated Markdown. The mutation-causality blocker remains closed.

## Product-source isolation cross-check

`FrontendSourceScope` continues to exclude `node_modules` from product evidence. The new dependency traversal is used only to discover imported Angular component selector metadata for the projection-authority filter. The benchmark produced no dependency-source fact leakage or new knowledge output attributable to `node_modules` declarations.

## How to read this benchmark

### Safety / fail-closed

> Can PKC resolve the repaired authority model without inventing R7.9/R7.10 authority on unchanged real repositories?

**PASS.** All three jobs complete and all three artifacts remain at zero unsupported R7.9/R7.10 promotion.

### Positive usefulness / R7.14

> Does at least one unchanged real repository naturally contain the complete supported backend → API → frontend → direct-render/visibility chain so PKC emits a useful authoritative cross-layer answer?

**NOT PASS.** None of the current three stress repositories naturally yields the complete supported shape.

Therefore:

```text
Safety benchmark                  PASS
R7.14 positive real-project yield NOT PASS
```

Do not weaken authority or modify benchmark source merely to turn the zeros into positives.

## Current conclusion

Exact production `47e098...` passes all standard gates, retains Jellyfin parity/provenance, keeps the accepted mutation-causality boundary closed, and remains fail closed on the three unchanged pinned repositories.

The benchmark does **not** satisfy R7.14 because no current stress repository naturally emits a supported positive V0.4.7 cross-layer answer.

## Next gate

Fresh independent rereview #15 must review exact production SHA:

```text
47e098dbe910b7f6cfd933a0595370524bec1fb2
```

Request: `docs/reviews/2026-09-23-v0.4.7-d-r7.10-rereview-15-request.md`.

Only after that rereview finds no new compile-valid/runtime-valid false-positive blocker may R7.10 and all of D be marked PASS / COMPLETE and only E be unlocked. R7.14 remains required and NOT PASS; V0.5 and the prepared AI-workspace/update work remain locked until V0.4.7 completes.
