# V0.4.7 Cross-layer PO-question Readiness Acceptance Plan

Last updated: 2026-09-23

## Status

```text
V0.4.6           PASS / COMPLETE
V0.4.7-A         PASS / COMPLETE
V0.4.7-B         PASS / COMPLETE
V0.4.7-C         PASS / COMPLETE
V0.4.7-D / R7.9  PASS / COMPLETE
V0.4.7-D / R7.10 REPAIRED / ALL GATES PASS / PENDING REREVIEW #14
V0.4.7-D overall  PENDING INDEPENDENT REREVIEW #14
V0.4.7-E         LOCKED behind D
R7.14            REQUIRED FOR E / NOT PASS
V0.5             LOCKED
```

Exact repaired R7.10 candidate:

```text
3e6fa7749eb8ef47be4eedb72d1c159cd502692f
fix: bound Angular direct text containers
```

Direct repaired predecessor:

```text
34182e221df6cf50eaa0ac362a5f575e80236a64
fix: reject unproven Angular content projection
```

## Product-knowledge invariants

Keep business conditions, value lineage/provenance, mutation/causality, render authority and visibility authority distinct. Deterministically proven observable behavior may become authoritative product-behavior knowledge. Unsupported inference fails closed. Same/similar names are never sufficient proof.

## Product Owner acceptance questions

Generated portable knowledge must answer these questions from evidence or explicitly say the answer is not proven:

1. Where did this value originally come from?
2. If the upstream value changes later, does the existing downstream value change automatically?
3. What code path can change this value after creation?
4. Was this value directly copied or computed?
5. What was the last observed source before a supported persistence/return boundary?
6. What backend conditions and frontend conditions jointly determine whether an item/value is visible?
7. How does a value travel through backend → DTO/projection → API → frontend composition?
8. If authority is incomplete, what lineage or causal evidence is still deterministically known?

Raw fact existence alone is insufficient; portable knowledge must support a PO-level answer.

## Cross-layer acceptance chain

```text
backend/domain value
→ DTO/projection property
→ API response field
→ typed frontend API result
→ component/view-model assignment
→ authoritative active directly-rendered text
```

C owns backend → DTO/projection → API response. R7.9 owns API result → frontend state → directly rendered value. R7.10 owns bounded joint backend/frontend visibility for that same exact proven path. E validates knowledge-only answers, real-project usefulness and portable parity.

## Regression gates

### R7.1–R7.4

**PASS through A.** Accepted A: `09a0c7f2a058adfd7ddb7b4ac2feb2d17580a429`.

### R7.5–R7.7

**PASS through B.** Accepted B: `17fd30b3a4b8178208adabc12c40dee060bedb54`.

### R7.8 — DTO/projection mapping

**PASS / COMPLETE through C.** Accepted C: `fbb64b9917da1f63362558355201ff7998384ba0`.

### R7.9 — API result to frontend rendered value

**PASS / COMPLETE as accepted predecessor.** Accepted baseline: `fc4bfa7042f59620b2c7ba1c4f6700cf3b02172a`.

### R7.10 — joint backend/frontend visibility

**REPAIRED / ALL GATES PASS / PENDING INDEPENDENT REREVIEW #14.**

For the same R7.9 value path, backend selection and frontend visibility may compose only through exact identities. Frontend evidence cannot upgrade observed-only backend authority. Multiple, nested, raw-text, inert, projection-mediated, metadata/fallback/conditional, structural or otherwise unsupported visibility/render paths fail closed and retain independently proven lower-authority evidence.

#### Rereview #13 superseded

Rereview #13 targeted native-SVG fail-closed candidate `7818c7ed646b30cb7b8505f053572783e075af6f`. Before an independent acceptance record was committed, pre-challenge found two new false-positive authority classes, so #13 is historical/stale rather than PASS.

#### Repair — unproven Angular content projection

A component host can lexically contain an interpolation without rendering it:

```html
@if (isAllowed) {
  <app-shell>{{ displayPrice }}</app-shell>
}
```

If the receiving component does not project that child through matching `ng-content`, treating the lexical interpolation as authoritative direct render is false.

Regression-first repair:

```text
34182e221df6cf50eaa0ac362a5f575e80236a64
fix: reject unproven Angular content projection
```

The authority filter fails closed under custom/component projection boundaries. Product-source component selectors are recognized for element, attribute, class and combined forms. This is deliberately conservative; lack of positive proof does not become render authority.

Focused regression:

`tests/Pkc.CSharp.Tests/AngularComponentProjectionRenderAuthorityRegressionTests.cs`

#### Repair — bounded HTML direct-text containers

Valid Angular templates can place interpolation in HTML elements whose child text is metadata, fallback content or conditionally presented rather than direct page-visible text:

```html
<title>{{ displayPrice }}</title>
<canvas>{{ displayPrice }}</canvas>
<dialog>{{ displayPrice }}</dialog>
<details>{{ displayPrice }}</details>
<object>{{ displayPrice }}</object>
<noscript>{{ displayPrice }}</noscript>
```

Regression-first repair:

```text
3e6fa7749eb8ef47be4eedb72d1c159cd502692f
fix: bound Angular direct text containers
```

The new HTML direct-text authority filter rejects the bounded unsupported metadata/raw/fallback/conditional container set. It does not claim a general browser/CSS/layout engine. A closed unsupported container must not suppress later ordinary HTML direct-render authority.

Focused regression:

`tests/Pkc.CSharp.Tests/AngularHtmlDirectTextAuthorityRegressionTests.cs`

Native SVG direct render remains intentionally unsupported from `7818c7ed...`. HTML inside supported SVG `foreignObject` remains eligible subject to the ordinary HTML and component-projection authority boundaries.

#### Exact-SHA repaired-candidate gates

```text
CI + full PKC tests + WorkPlay + PokeTrade   35769202107 — PASS
pinned Loren                                35769202115 — PASS
Loren-main canary                           35769202043 — PASS
pinned Jellyfin + parity/provenance         35769202074 — PASS

Release build        0 warnings / 0 errors
C# tests             241 / 241 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin remains fully project-semantic: 43,365 facts, 195,316 relations, 386 workflow candidates, 116 product features, 504 knowledge Markdown files; parity/no-leak PASS. Artifact `10713378704`, digest `sha256:cca8adf6d31a4c0207eaea58e5935147d279a7e9e3db361ad6b32291a0807198`.

Local repository test execution is not claimed because the execution container could not resolve `github.com` while cloning and has no local .NET toolchain. Exact-SHA GitHub gates are the completed verification evidence.

#### Repaired real-repository benchmark

```text
base implementation: 3e6fa7749eb8ef47be4eedb72d1c159cd502692f
wrapper commit:      28c9758ba5ae172bb0ee52e032a28fa24fb1f917
run:                 35769939151 — PASS, 3 / 3 jobs
```

Direct artifact inspection across pinned `jin12`, `agentic-angular` and `kesetovic-crm` found zero current R7.9/R7.10 direct-render/visibility authority. Canonical outputs are byte-identical to the previous safety benchmark except generated nested ZIP bytes/timestamps. Agentic mutation-causality remains closed: both `caller-object-unproven` `UpdatedAt` mutation fact IDs occur zero times outside raw facts in candidates/features/generated Markdown.

Detailed evidence: `docs/benchmarks/2026-09-23-r7.10-html-projection-fail-closed-benchmark.md`.

Fresh independent request:

`docs/reviews/2026-09-23-v0.4.7-d-r7.10-rereview-14-request.md`

### R7.11 — authority downgrade retains independent evidence

Permanent. No false business rule may replace lower-authority deterministic lineage/mutation/visibility evidence.

### R7.12 — portable knowledge rendering/parity

Permanent. `knowledge/`, `PKC_KNOWLEDGE.md` and ZIP must preserve critical meaning without raw `.pkc` or source-tree leakage.

### R7.13 — cross-benchmark regression

Permanent sequence:

```text
focused checkpoint regressions
→ full C# / frontend tests
→ Release build
→ PokeTrade known-answer
→ pinned Loren
→ Loren-main canary
→ pinned Jellyfin
→ portable parity/no-leak
→ independent rereview
```

### R7.14 — real-project positive yield

**REQUIRED for E completion; NOT PASS.**

At least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Do not modify a benchmark or weaken fail-closed authority to manufacture yield. The current three-repository stress benchmark still produces zero supported cross-layer positives; that is safety evidence, not R7.14 completion.

## Acceptance matrix

| PO question | Minimum proof |
| --- | --- |
| Where did this value originally come from? | R7.1 + R7.2 + R7.3 |
| If upstream changes later, does downstream change automatically? | R7.4 |
| What can change the value after creation? | R7.6 |
| Was it copied or computed? | R7.1 + R7.5 |
| What was the last source before a supported terminal boundary? | R7.7 |
| How did it reach DTO/API/frontend? | R7.8 + R7.9 |
| What backend + frontend conditions determine visibility? | R7.10 |
| What survives incomplete authority? | R7.11 |
| Does portable handoff preserve the answer? | R7.12 |
| Does it generalize without benchmark exceptions? | R7.13 |
| Does it produce a useful positive on unchanged real software? | R7.14 |

## Current exact action

Do **not** implement E yet.

```text
independent rereview #14 of exact 3e6fa7749eb8ef47be4eedb72d1c159cd502692f
→ PASS: mark R7.10 + all D PASS / COMPLETE; unlock only E
→ FAIL: regression-first minimum generic repair; rerun exact-SHA gates and independent review
```

Native SVG direct-render authority remains intentionally unsupported at this checkpoint. Unproven component projection and bounded non-direct/conditional HTML child text likewise fail closed. R7.14 remains required for E even after D passes. V0.5 and the prepared AI-workspace/update initiative remain locked until V0.4.7 / V0.4.x PO-question-readiness acceptance completes.

## Version semantics

```text
roadmap:             V0.4.7-D / R7.10 pending independent rereview #14
tool/package:        RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```
