# V0.4.7 Cross-layer PO-question + Demo Readiness Acceptance Plan

Last updated: 2026-09-24

## Status

```text
V0.4.6            PASS / COMPLETE
V0.4.7-A          PASS / COMPLETE
V0.4.7-B          PASS / COMPLETE
V0.4.7-C          PASS / COMPLETE
V0.4.7-D / R7.9   PASS / COMPLETE
V0.4.7-D / R7.10  OPEN / REVIEW PAUSED / NOT ACCEPTED
V0.4.7-E0          ACTIVE / USER-AUTHORIZED BOUNDED PREWORK
V0.4.7-E0 / RD1    PASS / COMPLETE
V0.4.7-E0 / RD2    PASS / COMPLETE
V0.4.7-E0 / RD3    PASS / COMPLETE
V0.4.7-E0 / RD4    PASS / COMPLETE — deterministic/synthetic gate
V0.4.7-E0 / RD5    PASS / COMPLETE
V0.4.7-E0 / RD6    PASS / COMPLETE
V0.4.7-E0 / RD7    PASS / COMPLETE
V0.4.7-E0 / RD8    ACTIVE / PARTIAL EVIDENCE / NOT PASS
V0.4.7-E1          LOCKED behind E0
V0.4.7-E2          LOCKED behind E1
R7.14              REQUIRED FOR E2 / NOT PASS
V0.5               LOCKED
```

## Repository-state reconciliation

Remote `main` immediately before the reconciliation docs commit was:

```text
9ef914f57c558e3b2963b9ba4dd187a6ecd6603f
Add question in demo
```

Latest production-code commit beneath that docs-only HEAD:

```text
fa30e3eb140357da5453702090be307417abb4e2
fix: report the preserved workspace when it could not replace the current one
```

Current descendant HEAD `9ef914f` completed all four observed GitHub Actions workflows successfully.

Therefore previous `RD1-RD7 LOCAL PASS / PUSH + CI PENDING` wording is retired. Those checkpoints are now `PASS / COMPLETE`.

Review record:

`docs/reviews/2026-09-24-main-state-reconciliation.md`

## Priority decision

The user explicitly authorized Repository Discovery + bounded-run implementation before formal R7.10/D acceptance because the normal whole-root product path was operationally impractical on the intended demo repository class.

This does **not** self-certify D. R7.10/D stays open and must receive fresh independent acceptance before V0.4.7 can close.

Authoritative override:

`docs/plans/2026-09-24-demo-scan-priority-override.md`

## Product-knowledge invariants

Keep these evidence classes distinct unless exact evidence proves composition:

- business conditions;
- value lineage/provenance;
- mutation/causality;
- API/wire identity;
- render authority;
- visibility authority;
- portable knowledge rendering.

Unsupported inference fails closed. Same/similar names are never proof of identity. Stronger composition failure must preserve independently proven lower-authority evidence.

E0 adds an operability invariant: expensive semantic analysis must not begin as an unbounded whole-root operation before deterministic discovery/plan state exists.

## E0 runtime constraint

Repository discovery and normal PKC compilation must be local and deterministic.

E0 MUST NOT require:

- Claude/OpenAI/Gemini APIs;
- hosted LLM calls;
- API keys;
- user AI tokens;
- source upload to an LLM.

An AI may consume `.pkc/workspace` after compilation. AI generation is not part of repository compilation.

## E0 completed checkpoints

### RD1 — inventory + safe exclusion — PASS / COMPLETE

Proved deterministic shallow inventory, discovery-before-semantics, strong-evidence-only auto-exclusion, name-only negative regressions, test isolation and deterministic output.

Evidence:

`docs/reviews/2026-09-24-rd1-inventory-safe-exclusion-evidence.md`

### RD2 — application boundaries + ownership — PASS / COMPLETE

Proved manifest/project-reference-derived application boundaries, shared ownership, test non-authority, no similar-name linking and UNKNOWN preservation.

Evidence:

`docs/reviews/2026-09-24-rd2-application-boundaries-evidence.md`

### RD3 — vendor/custom frontend classification — PASS / COMPLETE

Proved deterministic first-party/vendor/generated/modified-vendor classification without folder-name authority.

Evidence:

`docs/reviews/2026-09-24-rd3-vendor-frontend-classification-evidence.md`

### RD4 — runtime/plugin provenance — PASS / COMPLETE

Proved authoritative runtime-plugin edges require deterministic loader + identity + build/copy delivery provenance; unsupported shapes remain unresolved/UNKNOWN and tests do not create production edges.

Evidence:

`docs/reviews/2026-09-24-rd4-runtime-plugin-provenance-evidence.md`

This is a deterministic/synthetic gate. Whether the approved private RD8 repository actually contains such shapes is a separate real-repository applicability question.

### RD5 — deterministic ScanPlan — PASS / COMPLETE

Proved stable privacy-safe plan state, explicit exclusions/UNKNOWN, source role/scan mode/coverage accounting and deterministic waves/fingerprint.

Evidence:

`docs/reviews/2026-09-24-rd5-deterministic-scan-plan-evidence.md`

### RD6 — scoped/bounded semantic execution — PASS / COMPLETE

Proved `pkc run` consumes plan-derived semantic scope while preserving accepted scanner authority inside selected scope. Tests/runtime-dependency/generated areas do not enter deep scanning by default; UNKNOWN remains visible.

Evidence:

`docs/reviews/2026-09-24-rd6-scoped-semantic-execution-evidence.md`

### RD7 — coverage + observability — PASS / COMPLETE

Proved `pkc discover <repo>` plan-only inspection, persisted scan coverage and privacy-safe workspace coverage metadata.

Evidence:

`docs/reviews/2026-09-24-rd7-coverage-observability-evidence.md`

## RD8 — current-main private large-repository validation — ACTIVE

### Purpose

RD8 must prove the normal integrated product path is both operationally practical and product-useful on the approved private large mixed repository.

A green build, successful compiler exit, generated JSON, non-zero facts, or generated workspace alone is **not** sufficient.

### Evidence already collected

First private exercise, sanitized:

```text
repository scale: about 26.9k files, 18 hosts
planned semantic files: about 16.8k
facts / relations: about 263.6k / 2.04M
workflow candidates: 4,186

run 1 (RD7):       41.5 min, ~18.6 GB peak, OOM at artifact write
run 2 (patched):   29 min, ~7.5 GB peak, exit 0, workspace generated
pkc discover:      about 98 s
```

Run 2 generated about 703 product features and 4,709 workspace files.

The resource repairs used in run 2 later merged in:

```text
612fa998da6983662a90eed2836714c9928243b5
feat: make large runs failure-safe, resumable and self-describing
```

Later production changes also merged:

```text
48ce2f10c1e506b28e37aa3ff15ba5e241a3da0c
feat: follow sole implementations and surface mapped-field, gate and guard rules

fa30e3eb140357da5453702090be307417abb4e2
fix: report the preserved workspace when it could not replace the current one
```

Because the successful private run predates the fully integrated current production state, it is supporting evidence only.

### RD8 Gate A — fresh current-main operability

Run in the approved source-enabled/company environment:

```text
build PKC Release at current main
pkc discover <private-repo-copy>
pkc run <private-repo-copy>          # fresh acceptance measurement: no --resume
```

Record sanitized:

- discovery elapsed time;
- full-run elapsed time;
- peak working set;
- repository/profile/plan counts;
- planned vs executed semantic files;
- withheld-by-plan / withheld-by-scanner counts;
- UNKNOWN coverage;
- facts, relations, workflows/product features;
- artifact write status;
- final workspace path and file count.

Confirm:

- `.pkc/RUN_SUMMARY.md` and `.pkc/run-summary.json` are internally plausible;
- coverage is honest;
- the final workspace is usable;
- a blocked swap, if it happens, correctly reports `.pkc/workspace.new` rather than the stale workspace.

There is no universal hard RAM threshold. PASS requires material improvement from the original failure mode and practical completion on the approved demo environment.

If the fresh run still has a high peak near the previous ~7.5 GB observation, profile retained memory before changing code. Do not optimize from assumption.

### RD8 Gate B — targeted Level-1 product value

After the fresh workspace exists, run 2-3 known-answer probes using:

`docs/benchmarks/product-value-benchmark-protocol.md`

Prefer questions from `docs/question-trainning.md` plus the quantity-adjustment probe when useful.

For each probe:

```text
phase 1: answer from generated .pkc/workspace only
phase 2: inspect approved source to establish known answer
phase 3: compare and score
```

Required score dimensions:

- business correctness;
- completeness of material rules/conditions/effects;
- unsupported additions / hallucinations;
- uncertainty calibration;
- business wording rather than raw-code restatement;
- evidence/trace completeness.

A useful partial answer may be `PARTIAL`; honest `NOT PROVEN` is preferable to a fabricated answer. However, RD8 demo-readiness requires at least a useful result on the selected known-answer probes, not merely honest failure on all of them.

Do not publish proprietary source bodies in benchmark reports.

### RD8 Gate C — runtime/plugin applicability

The first private repository did not exercise runtime-dependency-index or runtime-plugin cases.

Before RD8 PASS, record one of:

1. `N/A` because the approved repository is demonstrably free of those shapes and the reviewer explicitly accepts absence as the correct real-repository result; or
2. additional validation on another approved real repository/corpus containing those shapes.

RD4 deterministic/synthetic coverage remains valid, but it must not be misreported as private-repository exercise evidence.

### RD8 PASS rule

RD8 is PASS only when Gate A, Gate B and Gate C are all closed with explicit evidence.

## E0 completion

E0 is PASS only when:

```text
RD1-RD7 PASS / COMPLETE
+ RD8 PASS
+ normal product path practically produces the workspace
+ coverage remains honest
+ targeted answers demonstrate useful correctly-calibrated product value
```

## Semantic safety while D is open

E0 is infrastructure/operability work. Existing semantic authority and fail-closed behavior must remain unchanged for source selected for scanning unless a separately accepted semantic repair explicitly changes that boundary.

Do not use E0 as an opportunity to claim R7.10 acceptance.

## E1 — remaining product-value repairs

Only after E0 PASS.

Parked candidate:

```text
branch  fix/product-value-construction-state
commit  35c8e5c5f856e15568aa963bb2d76268008c5570
```

Known candidate gaps from the first private workspace include mediator traversal, legacy-UI linkage and remaining rule-language cleanup. The next actual E1 repair must be chosen from fresh benchmark evidence, not from the stale candidate list alone.

## E2 — real-project product acceptance

Only after E1 PASS.

Formal R7.10/D independent acceptance must also be completed before V0.4.7 closes.

E2 still requires broader product acceptance including R7.14 positive unchanged-real-project yield and Level-2 workspace-only PO/QC validation.

## Verification cadence

```text
focused regression
→ related tests
→ broader relevant suite
→ diff review
→ one coherent implementation commit/push
→ CI final verification
→ status/handoff update
```

For docs-only state reconciliation, no AI benchmark or CI rerun is required because production behavior is unchanged; use `[skip ci]` and verify the committed diff/state directly.

## Current exact action

```text
RD8 current-main private validation — ACTIVE

fresh discover + full run without --resume
→ sanitized resource/coverage/workspace evidence
→ 2-3 targeted Level-1 known-answer probes
→ runtime/plugin applicability disposition
→ RD8 PASS only when all three gates close
```

## Version semantics

```text
roadmap:             E0 ACTIVE / RD1-RD7 COMPLETE / RD8 ACTIVE / D still open
package:             RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```
