# V0.4.7 Cross-layer PO-question + Demo Readiness Acceptance Plan

Last updated: 2026-09-24

## Status

```text
V0.4.6           PASS / COMPLETE
V0.4.7-A         PASS / COMPLETE
V0.4.7-B         PASS / COMPLETE
V0.4.7-C         PASS / COMPLETE
V0.4.7-D / R7.9  PASS / COMPLETE
V0.4.7-D / R7.10 OPEN / REVIEW PAUSED / NOT ACCEPTED
V0.4.7-E0         ACTIVE / USER-AUTHORIZED BOUNDED PREWORK
V0.4.7-E0 / RD1   LOCAL PASS at fd3428f / PUSH + CI PENDING
V0.4.7-E0 / RD2   LOCAL PASS at 05eadb1 / PUSH + CI PENDING
V0.4.7-E0 / RD3   LOCAL PASS at ac3efd9 / PUSH + CI PENDING
V0.4.7-E0 / RD4   LOCAL PASS at a37d936 / PUSH + CI PENDING
V0.4.7-E0 / RD5   LOCAL PASS at c440eea / PUSH + CI PENDING
V0.4.7-E0 / RD6   ACTIVE
V0.4.7-E1         LOCKED behind E0
V0.4.7-E2         LOCKED behind E1
R7.14             REQUIRED FOR E2 / NOT PASS
V0.5              LOCKED
```

## Priority decision

The user explicitly authorized Repository Discovery + bounded run implementation before formal R7.10/D acceptance because the current normal whole-root scan is operationally impractical on the intended demo repository class.

This does NOT self-certify D. R7.10/D stays open and must receive fresh independent acceptance before V0.4.7 can close.

Authoritative override:

`docs/plans/2026-09-24-demo-scan-priority-override.md`

## Product-knowledge invariants

Keep business conditions, value lineage/provenance, mutation/causality, render authority and visibility authority distinct. Unsupported inference fails closed. Similar names are never sufficient proof.

E0 adds an operability invariant: expensive semantic analysis must not begin as an unbounded whole-root operation before PKC has deterministic discovery/plan state.

## E0 — Repository Discovery + bounded run readiness — ACTIVE

A private mixed legacy enterprise repository showed the current whole-root product path can become operationally impractical before useful output; one observed run reached roughly 9 GB RAM. This is design evidence, not a universal numeric threshold.

E0 must solve the structural cause: deterministic repository discovery, scope reduction and bounded execution.

### Zero-token runtime requirement

Repository discovery and normal PKC repository compilation must be local and deterministic.

E0 MUST NOT require:

- Claude/OpenAI/Gemini APIs;
- hosted LLM calls;
- API keys;
- user AI tokens;
- source upload to an LLM.

LLM reconnaissance is allowed only as research/oracle evidence outside PKC runtime.

### Required E0 sequence

```text
RD1 inventory + safe exclusion
→ RD2 application boundaries + ownership
→ RD3 vendor/custom frontend classification
→ RD4 runtime/plugin provenance
→ RD5 deterministic ScanPlan
→ RD6 scoped/bounded semantic execution
→ RD7 coverage + observability + plan-only inspection
→ RD8 private large-repo validation
→ prove pkc run practically produces .pkc/workspace
```

Run sequentially. Do not start RD2 before RD1 PASS.

## RD1 — LOCAL PASS acceptance boundary

Evidence: `docs/reviews/2026-09-24-rd1-inventory-safe-exclusion-evidence.md` (implementation `fd3428f`).

Create regression-first synthetic coverage for a mixed repository containing at least:

- multiple .NET apps/libraries/tests;
- one Angular workspace;
- legacy JavaScript;
- generated/restorable directories;
- infrastructure/configuration;
- ambiguous first-party directories with vendor/legacy-looking names.

RD1 must prove:

- deterministic inventory;
- discovery can run without whole-repository semantic analysis;
- only strongly proven generated/restorable areas auto-exclude;
- `legacy`, `vendor`, `plugins`, `themes`, `Scripts`, `Content`, `old`, `packages` and similar names never create exclusion authority by themselves;
- ambiguous ownership remains included or UNKNOWN;
- tests remain test evidence rather than production authority;
- the new run architecture establishes repository profile/plan state before expensive C# or frontend semantic scanning;
- deterministic ordering/output.

RD1 is not accepted merely because it emits JSON.

## E0 required properties across RD1–RD8

- deterministic repository inventory;
- discovery before expensive C#/frontend semantic scanning;
- application/component boundaries and shared ownership explicit;
- first-party runtime/shared, third-party runtime, modified vendor, infrastructure, tests, generated/restorable and UNKNOWN distinguishable;
- third-party runtime visible without default deep scan;
- modified vendor only narrow deterministic first-party carve-outs;
- runtime/plugin edges require deterministic provenance;
- ambiguous shapes stay UNKNOWN/fail-closed;
- ScanPlan stable, inspectable and privacy-safe;
- semantic scanners execute only planned scopes/application waves;
- UNKNOWN not silently discarded;
- coverage cannot claim READY when required areas are omitted/unsupported;
- plan-only/discovery inspection available before expensive semantic execution;
- private validation sanitizes evidence;
- approved demo repository ultimately produces `.pkc/workspace` practically.

Technical packet:

- `docs/plans/2026-09-24-demo-scan-priority-override.md`
- `docs/plans/2026-09-24-repository-discovery-scan-planning-plan.md`
- `docs/reviews/2026-09-24-repository-discovery-scan-planning-self-review.md`
- `docs/plans/2026-09-24-demo-critical-sequential-execution-plan.md`

## Semantic safety while D is open

E0 is infrastructure/scope work. Existing semantic authority and fail-closed behavior must remain unchanged for source that is selected for scanning.

Do not use E0 as an opportunity to redefine R7.10 authority or claim D acceptance.

If E0 changes shared orchestration that affects accepted regressions, those regressions remain required.

## E1 — remaining product-value repairs

Only after E0 PASS. The validated construction/default/computation candidate remains parked:

```text
branch  fix/product-value-construction-state
commit  35c8e5c5f856e15568aa963bb2d76268008c5570
```

Do not merge during E0.

## E2 — real-project product acceptance

Only after E1 PASS. Formal R7.10/D independent acceptance must also be completed before V0.4.7 closes.

Required E2 evidence still includes R7.14 positive unchanged-real-project yield, Level-2 known-answer benchmark, useful workspace-only PO/QC answers, explicit uncertainty, portable parity/no-source-leak and grounded feature/workflow fidelity.

## Safety / benchmark cadence

```text
focused regression
→ related tests
→ broader relevant suite
→ diff review
→ one coherent implementation commit/push
→ CI final verification
→ update status/handoff
→ next RD checkpoint only after explicit PASS
```

## Current exact action

```text
RD6 scoped/bounded semantic execution — ACTIVE.
```

## Version semantics

```text
roadmap:             E0 bounded prework ACTIVE / RD1–RD5 local PASS / RD6 current / D still open
package:             RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```
