# V0.4.7 Cross-layer PO-question Readiness Acceptance Plan

Last updated: 2026-09-22

## Status

```text
V0.4.6           PASS / COMPLETE
V0.4.7-A         PASS / COMPLETE
V0.4.7-B         PASS / COMPLETE
V0.4.7-C         PASS / COMPLETE
V0.4.7-D / R7.9  PASS / COMPLETE
V0.4.7-D / R7.10 REPAIRED / ALL GATES PASS / PENDING REREVIEW #9
V0.4.7-D overall  PENDING INDEPENDENT REREVIEW #9
V0.4.7-E         LOCKED behind D
R7.14            REQUIRED FOR E / NOT PASS
V0.5             LOCKED
```

Exact repaired R7.10 candidate:

```text
cd3b1d4b64168c4e95284299c5715b3db0e4da4b
fix: fail closed on structural visibility directives
```

## Product-knowledge invariants

Keep business conditions, value lineage/provenance, mutation/causality, render authority and visibility authority distinct. Deterministically proven observable behavior may become authoritative product-behavior knowledge. Unsupported inference fails closed. Same/similar names are never sufficient proof.

## Product Owner acceptance questions

Generated portable knowledge must answer these questions from evidence or explicitly say the answer is not proven:

1. Where did this value originally come from?
2. If the upstream value changes later, does the existing downstream value change automatically?
3. What code path can change this value after creation?
4. Was this value directly copied or computed?
5. What was the last observed source before a supported persistence/return boundary?
6. What backend conditions and frontend conditions jointly determine whether an item/value is visible?
7. How does a value travel through backend → DTO/projection → API → frontend composition?
8. If authority is incomplete, what lineage or causal evidence is still deterministically known?

Raw fact existence alone is insufficient; portable knowledge must support a PO-level answer.

## Cross-layer acceptance chain

```text
backend/domain value
→ DTO/projection property
→ API response field
→ typed frontend API result
→ component/view-model assignment
→ authoritative active rendered text
```

C owns backend → DTO/projection → API response. R7.9 owns API result → frontend state → rendered value. R7.10 owns bounded joint backend/frontend visibility for that same exact proven path. E validates knowledge-only answers, real-project usefulness, and portable parity.

## Regression gates

### R7.1–R7.9

**PASS / COMPLETE through R7.9.** Accepted baselines remain closed unless a new real regression is demonstrated.

### R7.10 — joint backend/frontend visibility

**REPAIRED / ALL GATES PASS / PENDING INDEPENDENT REREVIEW #9.**

For the same R7.9 value path, backend selection and frontend visibility may compose only through exact identities. Frontend evidence cannot upgrade observed-only backend authority. Multiple, nested or unsupported visibility paths fail closed and retain independently proven lower-authority evidence.

#### Independent rereview #8 finding

Rereview #8 found two compile-valid/runtime-valid authority failures:

```html
@if (isAllowed) {
  <strong ngNonBindable>{{ displayPrice }}</strong>
}
```

Here Angular renders literal interpolation syntax rather than the member value, so no R7.9 rendered-member authority is valid.

And:

```html
@if (isAllowed) {
  <div title="price > {{ displayPrice }}"></div>
}
```

Here interpolation is inside an attribute, not visible text. A `>` inside the quoted value could defeat the old simplistic tag-boundary check.

Review record: `docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-8.md`.

#### Repair train and additional adversarial hardening

```text
a36fae917aa687d8b0240514b6144271e6d81b89  fix: harden Angular render authority
e9e22cefc969a058114eee0cc443270d6f5d0900  fix: reject quoted attribute visibility controls
3299e54a1bafdedd5146a86efd58df8e5c8dae99  fix: bound Angular visibility control flow
cd3b1d4b64168c4e95284299c5715b3db0e4da4b  fix: fail closed on structural visibility directives
```

The final boundary requires:

- authoritative R7.9 text render, not `ngNonBindable` literal content or attribute interpolation;
- exactly one supported enclosing Angular `@if`;
- no second/nested Angular control block such as `@for`, `@switch`, `@defer` or companion blocks;
- no unsupported `*structuralDirective` ancestor;
- quote-aware tag/attribute parsing and brace matching that ignores comments, HTML tags and interpolation bodies;
- exact R7.9 render fact ID and exact backend projection/predicate IDs.

Unsupported stronger visibility composition does not erase the independently proven R7.9 rendered-value terminal.

#### Exact-SHA final-candidate gates

```text
CI + full PKC tests + WorkPlay + PokeTrade   35714413695 — PASS
pinned Loren                                35714413680 — PASS
Loren-main canary                           35714413696 — PASS
pinned Jellyfin + parity/provenance         35714413688 — PASS

Release build        0 warnings / 0 errors
C# tests             194 / 194 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin remains fully project-semantic: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 knowledge Markdown files; parity/no-leak PASS. Artifact `10688588126`, digest `sha256:140afc4cf9be4aa1c0961c4372a34d3c1d73abfbfabe805106adb839dab6c106`.

#### Final real-repository safety benchmark

```text
base implementation: cd3b1d4b64168c4e95284299c5715b3db0e4da4b
wrapper commit:      41c73a4c6a16065c49981bbd59b3e6dbc022b3cc
run:                 35714494308 — PASS, 3 / 3 jobs
```

The wrapper changes only workflow trigger configuration; production/test source comes from exact `cd3b1d4b...`.

Direct artifact inspection across pinned `jin12`, `agentic-angular`, and `kesetovic-crm` found zero supported current R7.9/R7.10 full positives. This means the candidate stays fail closed on these unchanged repositories; it does **not** complete R7.14.

Detailed evidence: `docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`.

Fresh independent request:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-9-request.md`

### R7.11 — authority downgrade retains independent evidence

Permanent. No false business rule may replace lower-authority deterministic lineage/mutation/visibility evidence.

### R7.12 — portable knowledge rendering/parity

Permanent. `knowledge/`, `PKC_KNOWLEDGE.md` and ZIP must preserve critical meaning without raw `.pkc` or source-tree leakage.

### R7.13 — cross-benchmark regression

Permanent sequence:

```text
focused checkpoint regressions
→ full C# / frontend tests
→ Release build
→ PokeTrade known-answer
→ pinned Loren
→ Loren-main canary
→ pinned Jellyfin
→ portable parity/no-leak
→ independent rereview
```

### R7.14 — real-project positive yield

**REQUIRED for E completion; NOT PASS.**

At least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Do not modify a benchmark or weaken fail-closed authority to manufacture yield.

Interpret the current three-repository benchmark in two separate dimensions:

```text
Safety / fail-closed: PASS — 3/3 repositories run cleanly and produce zero unsupported full-chain authority.
Positive usefulness:  NOT PASS — none yet naturally emits the exact supported full-chain positive required by R7.14.
```

## Acceptance matrix

| PO question | Minimum proof |
| --- | --- |
| Where did this value originally come from? | R7.1 + R7.2 + R7.3 |
| If upstream changes later, does downstream change automatically? | R7.4 |
| What can change the value after creation? | R7.6 |
| Was it copied or computed? | R7.1 + R7.5 |
| What was the last source before a supported terminal boundary? | R7.7 |
| How did it reach DTO/API/frontend? | R7.8 + R7.9 |
| What backend + frontend conditions determine visibility? | R7.10 |
| What survives incomplete authority? | R7.11 |
| Does portable handoff preserve the answer? | R7.12 |
| Does it generalize without benchmark exceptions? | R7.13 |
| Does it produce a useful positive on unchanged real software? | R7.14 |

## Current exact action

Do **not** implement E yet.

```text
independent rereview #9 of exact cd3b1d4b64168c4e95284299c5715b3db0e4da4b
→ PASS: mark R7.10 + all D PASS / COMPLETE; unlock only E
→ FAIL: regression-first minimum generic repair; rerun exact-SHA gates and independent review
```

R7.14 remains required for E even after D passes. V0.5 remains locked until V0.4.7 / V0.4.x PO-question-readiness acceptance completes.

## Version semantics

```text
roadmap:             V0.4.7-D / R7.10 pending independent rereview #9
tool/package:        RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```
