# V0.4.7 Cross-layer PO-question + Demo Readiness Acceptance Plan

Last updated: 2026-09-24

## Status

```text
V0.4.6             PASS / COMPLETE
V0.4.7-A           PASS / COMPLETE
V0.4.7-B           PASS / COMPLETE
V0.4.7-C           PASS / COMPLETE
V0.4.7-D / R7.9    PASS / COMPLETE
V0.4.7-D / R7.10   OPEN / REVIEW PAUSED / NOT ACCEPTED
V0.4.7-E0           ACTIVE / USER-AUTHORIZED BOUNDED PREWORK
V0.4.7-E0 / RD1     PASS / COMPLETE
V0.4.7-E0 / RD2     PASS / COMPLETE
V0.4.7-E0 / RD3     PASS / COMPLETE
V0.4.7-E0 / RD4     PASS / COMPLETE — deterministic/synthetic gate
V0.4.7-E0 / RD5     PASS / COMPLETE
V0.4.7-E0 / RD6     PASS / COMPLETE
V0.4.7-E0 / RD7     PASS / COMPLETE
V0.4.7-E0 / RD8     ACTIVE / KNOWN RUNTIME-PLUGIN BLOCKER / NOT PASS
V0.4.7-E1           LOCKED behind E0
V0.4.7-E2           LOCKED behind E1
R7.14               REQUIRED FOR E2 / NOT PASS
V0.5                 LOCKED
```

## Priority decision

The user explicitly authorized Repository Discovery + bounded-run implementation before formal R7.10/D acceptance because whole-root operation was impractical on the intended repository class.

This does **not** self-certify D. R7.10/D stays open and requires fresh independent acceptance before V0.4.7 can close.

Authoritative override:

`docs/plans/2026-09-24-demo-scan-priority-override.md`

## Product-knowledge invariants

Keep these evidence classes distinct unless exact evidence proves composition:

- business conditions;
- value lineage/provenance;
- mutation/causality;
- API/wire identity;
- render authority;
- visibility authority;
- portable knowledge rendering;
- repository/runtime topology provenance.

Unsupported inference fails closed. Same/similar names are never proof of identity. Stronger-composition failure must preserve independently proven lower-authority evidence.

E0 adds an operability invariant: expensive semantic analysis must not begin as an unbounded whole-root operation before deterministic discovery/plan state exists.

Repository discovery and normal PKC compilation must be local/deterministic and must not require hosted LLM calls, API keys or proprietary-source upload to an LLM.

## E0 completed checkpoints

### RD1 — inventory + safe exclusion — PASS / COMPLETE

Proved deterministic shallow inventory, discovery-before-semantics, strong-evidence-only exclusion, test isolation and deterministic output.

Evidence: `docs/reviews/2026-09-24-rd1-inventory-safe-exclusion-evidence.md`

### RD2 — application boundaries + ownership — PASS / COMPLETE

Proved manifest/project-reference-derived boundaries, shared ownership, test non-authority, no similar-name linking and UNKNOWN preservation.

Evidence: `docs/reviews/2026-09-24-rd2-application-boundaries-evidence.md`

### RD3 — vendor/custom frontend classification — PASS / COMPLETE

Proved deterministic first-party/vendor/generated/modified-vendor classification without folder-name authority.

Evidence: `docs/reviews/2026-09-24-rd3-vendor-frontend-classification-evidence.md`

### RD4 — runtime/plugin provenance — PASS / COMPLETE

Proved the currently supported authoritative rule: runtime-plugin edges require deterministic loader + identity + build/copy delivery provenance; unsupported/ambiguous shapes remain unresolved/UNKNOWN and tests do not create production edges.

Evidence: `docs/reviews/2026-09-24-rd4-runtime-plugin-provenance-evidence.md`

This is a deterministic/synthetic gate. It does not prove the intended private target's actual plugin shape is currently recovered.

### RD5 — deterministic ScanPlan — PASS / COMPLETE

Proved stable privacy-safe plan state, explicit exclusions/UNKNOWN, source role/scan mode/coverage accounting and deterministic waves/fingerprint.

Evidence: `docs/reviews/2026-09-24-rd5-deterministic-scan-plan-evidence.md`

### RD6 — scoped/bounded semantic execution — PASS / COMPLETE

Proved `pkc run` consumes plan-derived semantic scope while preserving accepted scanner authority inside selected scope.

Evidence: `docs/reviews/2026-09-24-rd6-scoped-semantic-execution-evidence.md`

### RD7 — coverage + observability — PASS / COMPLETE

Proved `pkc discover <repo>` plan-only inspection, persisted scan coverage and privacy-safe workspace coverage metadata.

Evidence: `docs/reviews/2026-09-24-rd7-coverage-observability-evidence.md`

## RD8 — current-main private large-repository validation — ACTIVE / BLOCKED

### Purpose

RD8 must prove the normal integrated product path is operationally practical **and** product-useful on the intended approved large mixed repository, while correctly representing material repository topology.

A green build, successful compiler exit, generated JSON, non-zero facts or generated workspace alone is insufficient.

### Supporting evidence already collected

Sanitized first private exercise:

```text
repository scale: about 26.9k files, 18 hosts
planned semantic files: about 16.8k
facts / relations: about 263.6k / 2.04M
workflow candidates: 4,186

run 1 (RD7):       41.5 min, ~18.6 GB peak, OOM at artifact write
run 2 (patched):   29 min, ~7.5 GB peak, exit 0, workspace generated
pkc discover:      about 98 s
```

Run 2 generated about 703 product features and 4,709 workspace files.

Resource repairs later merged in `612fa998`. Additional semantic/output changes merged in `48ce2f10` and `fa30e3eb`. The old successful run therefore remains supporting evidence only.

## RD8 Gate A — fresh current-main operability

Run in the approved source-enabled/company environment:

```text
build PKC Release at current main
pkc discover <approved-private-copy>
pkc run <approved-private-copy>          # fresh acceptance measurement; no --resume
```

Record sanitized:

- discovery elapsed time;
- full-run elapsed time;
- peak process-tree working set/resident memory;
- repository/profile/plan counts;
- planned vs executed semantic files;
- withheld-by-plan / withheld-by-scanner counts;
- UNKNOWN coverage;
- facts, relations, workflow candidates and product features;
- artifact-write status;
- final workspace path and file count.

Confirm `RUN_SUMMARY.md`, `run-summary.json`, discovery profile, plan, coverage and checkpoint are internally plausible and the final workspace is usable.

No universal hard RAM threshold exists. PASS requires practical completion and material improvement from the original failure mode.

## RD8 Gate B — targeted Level-1 product value

After the fresh workspace exists, run 2-3 known-answer probes using `docs/benchmarks/product-value-benchmark-protocol.md`.

For each probe:

```text
phase 1: answer from generated .pkc/workspace only
phase 2: inspect approved source to establish known answer
phase 3: compare and score
```

Prefer the quantity-adjustment visibility probe plus materially different questions from `docs/question-trainning.md`.

Score:

- business correctness;
- completeness of material conditions/effects;
- unsupported additions/hallucinations;
- uncertainty calibration;
- business wording instead of raw-code restatement;
- evidence/trace completeness.

An honest `NOT PROVEN` is preferable to fabrication, but demo-readiness requires useful results on selected known-answer probes, not honest failure on all probes.

## RD8 Gate C — runtime/plugin real-target correctness — KNOWN BLOCKER

A recovered sanitized reconnaissance proves the intended target class contains runtime plugin modules.

Sanitized topology:

```text
host
  -> reflection-based load from an output subfolder
  -> runtime plugin modules not project-referenced by the host

plugin delivery
  -> custom post-build copy into the runtime output
```

The reconnaissance further identifies solution/build-dependency declarations and post-build copy targets as evidence needed to represent the plugin relationship.

Consequences:

1. runtime/plugin applicability for the intended target is **YES**;
2. the previous `N/A if absent` path is no longer valid for this target;
3. earlier generated PKC output that contained no applicable runtime-plugin edge represents **missing yield**, not architectural absence;
4. RD8-C cannot pass until current PKC deterministically represents the applicable topology or a source-enabled inspection proves the current supported rule already covers the exact target shape.

Review:

`docs/reviews/2026-09-24-rd8-runtime-plugin-target-applicability.md`

### Required defect-shape inspection

The sanitized reconnaissance intentionally omits exact proprietary source syntax. Before changing production code, inspect only the narrow source-enabled shape needed to answer:

- what reflection-loader syntax family is used;
- what build/post-build copy syntax family proves delivery;
- whether solution-level build dependencies are needed for plugin identity/delivery;
- what evidence can be represented in a synthetic regression without proprietary names, paths or values.

Do not assume `Directory.GetFiles`, `Assembly.LoadFrom`, `PostBuildEvent`, `xcopy` or any other specific implementation without evidence.

### Repair rule if current main misses the exact shape

```text
sanitized exact target shape
-> focused red synthetic regression
-> minimum generic deterministic repair
-> negative regressions preserving fail-closed behavior
-> focused + related + full relevant local verification
-> one coherent implementation commit/push
-> rerun affected RD8-C discovery evidence
```

Runtime authority must not be promoted from name similarity, directory proximity, copy-only evidence, loader-only evidence or ambiguous identity.

## RD8 PASS rule

RD8 is PASS only when all three gates close explicitly:

```text
A: fresh current-main run completes practically with plausible artifacts/coverage
B: targeted Level-1 answers are useful and correctly calibrated
C: the intended target's applicable runtime-plugin topology is deterministically represented and verified
```

## E0 completion

E0 is PASS only when RD1-RD7 remain accepted, RD8 PASSes, the normal product path practically produces the workspace, coverage stays honest, and selected product answers demonstrate useful correctly-calibrated product value.

## Semantic safety while D is open

E0 is infrastructure/operability work. Existing semantic authority and fail-closed behavior must remain unchanged unless a separately justified regression-first repair changes the exact boundary. E0 must not be used to claim R7.10 acceptance.

## E1 — remaining product-value repairs

Only after E0 PASS.

Parked candidate:

```text
branch  fix/product-value-construction-state
commit  35c8e5c5f856e15568aa963bb2d76268008c5570
```

Known candidate gaps such as mediator traversal, legacy-UI linkage and rule-language cleanup remain parked until fresh benchmark evidence chooses the next repair.

## E2 — real-project product acceptance

Only after E1 PASS.

Formal R7.10/D independent acceptance must also complete before V0.4.7 closes. E2 still requires R7.14 positive unchanged-real-project yield and broader Level-2 workspace-only PO/QC validation.

## Verification cadence

```text
focused regression
-> related tests
-> broader relevant suite
-> diff review
-> one coherent implementation commit/push
-> CI final verification
-> status/handoff update
```

Docs-only state corrections use `[skip ci]` and direct committed-state verification because production behavior is unchanged.

## Current exact action

```text
RD8-C source-enabled exact-shape inspection
-> regression-first repair only if current main misses that deterministic shape
-> rerun affected runtime-plugin discovery evidence

then / alongside approved-environment evidence collection:
RD8-A fresh discover + full run without --resume
-> RD8-B 2-3 Level-1 probes
-> RD8 PASS only when A+B+C close
```

## Version semantics

```text
roadmap:             E0 ACTIVE / RD1-RD7 COMPLETE / RD8 BLOCKED / D still open
package:             RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```