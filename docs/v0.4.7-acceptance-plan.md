# V0.4.7 Cross-layer PO-question Readiness Acceptance Plan

Last updated: 2026-09-22

## Status

```text
V0.4.6           PASS / COMPLETE
V0.4.7-A         PASS / COMPLETE
V0.4.7-B         PASS / COMPLETE
V0.4.7-C         PASS / COMPLETE
V0.4.7-D / R7.9  PASS / COMPLETE
V0.4.7-D / R7.10 REPAIRED / ALL GATES PASS / PENDING REREVIEW #4
V0.4.7-D overall  PENDING INDEPENDENT REREVIEW #4
V0.4.7-E         LOCKED behind D
R7.14            REQUIRED FOR E / NOT PASS
V0.5             LOCKED
```

Exact repaired R7.10 candidate:

```text
dc69e44206942ffb0012e1994d6d39249d1db4be
fix: reject static display none renders
```

## Product-knowledge invariants

Keep business conditions, value lineage/provenance, and mutation/causality distinct. Deterministically proven observable behavior may become authoritative product-behavior knowledge. Deterministic implementation evidence without full authority remains lower-authority evidence. Unsupported inference fails closed and must not be invented. Same/similar names are never sufficient proof.

## Product Owner acceptance questions

Generated portable knowledge must answer these questions from evidence or explicitly say the answer is not proven:

1. Where did this value originally come from?
2. If the upstream value changes later, does the existing downstream value change automatically?
3. What code path can change this value after creation?
4. Was this value directly copied or computed?
5. What was the last observed source before a supported persistence/return boundary?
6. What backend conditions and frontend conditions jointly determine whether an item/value is visible?
7. How does a value travel through backend → DTO/projection → API → frontend composition?
8. If authority is incomplete, what lineage or causal evidence is still deterministically known?

Raw fact existence alone is insufficient; portable knowledge must support a PO-level answer.

## Canonical lineage semantics

| Semantic | Required meaning |
| --- | --- |
| `copy` | Direct assignment without modeled computation. |
| `snapshot` | Stored copied/derived value captures a point in time; later upstream changes do not automatically mutate it. |
| `derivation` | Downstream value is computed from one or more proven inputs. |
| `reference/dynamic` | Downstream read resolves from upstream state at read time. |
| `override` | Later write supersedes a previously sourced value without erasing original origin. |
| `mutation` | A code path writes downstream value/state after prior state existed. |

## Cross-layer acceptance chain

V0.4.7 must support a proven chain equivalent to:

```text
backend/domain value
→ DTO/projection property
→ API response field
→ typed frontend API result
→ component/view-model assignment
→ authoritative active visible rendered text
```

C owns backend → DTO/projection → API response. R7.9 owns API result → frontend state → rendered-value. R7.10 owns bounded joint backend/frontend visibility for that same exact proven path. E validates knowledge-only answers, real-project usefulness, and portable parity.

## Regression gates

### R7.1–R7.4

**PASS through A.** Accepted A: `09a0c7f2a058adfd7ddb7b4ac2feb2d17580a429`.

### R7.5–R7.7

**PASS through B.** Accepted B: `17fd30b3a4b8178208adabc12c40dee060bedb54`.

### R7.8 — DTO/projection mapping

**PASS / COMPLETE through C.** Accepted C: `fbb64b9917da1f63362558355201ff7998384ba0`.

### R7.9 — API result to frontend rendered value

**PASS / COMPLETE as accepted predecessor.** Accepted baseline: `fc4bfa7042f59620b2c7ba1c4f6700cf3b02172a`.

Supported bounded chain requires exact C response identity, explicit wire name, typed frontend response member, exact service/result assignment, exact component member, and an authoritative active visible Angular text interpolation. Route identity alone never proves a property edge.

### R7.10 — joint backend/frontend visibility

**REPAIRED / ALL GATES PASS / PENDING INDEPENDENT REREVIEW #4.**

For the same R7.9 value path, backend selection and frontend visibility may compose only through exact identities.

Bounded backend positive:

```csharp
var item = source.Single(item => predicate);
return new Response { DisplayValue = item.Value };
```

Requirements include target-project Roslyn semantics, exact `System.Linq.Enumerable.Single/First`, exact selected reference local, supported scalar auto-properties, direct final response initializer, no user-defined conversion, exact selection invocation span and exact projection identity.

Bounded frontend positive:

```html
@if (displayPrice > 0) {
  <strong>{{ displayPrice }}</strong>
}
```

Composition identity:

```text
renderedTerminal.frontendRenderFactId = visibility.renderFactId
renderedTerminal.backendProjectionFactId
→ projection.selectionPredicateFactId
→ predicate.selectedApiProjectionFactId
```

Frontend evidence cannot upgrade observed-only backend authority. Multiple/nested/unsupported visibility paths fail closed and retain independently proven lower-authority evidence.

#### Rereview history

Rereview #1 repaired inert `<ng-template>`, HTML comment, and HTML tag/attribute render authority.

Rereview #2 found static HTML `hidden` ancestry and repaired it at:

```text
97161baa2d0aff9131a7acf9db752393ae913d64
fix: reject statically hidden rendered text
```

Rereview #3 found a new compile-valid/runtime-valid presentation-suppression shape:

```html
<section style="display: none">
  @if (displayPrice > 0) {
    <strong>{{ displayPrice }}</strong>
  }
</section>
```

The interpolation is active template text but not user-visible. The prior filter could still authorize it and therefore overstate both R7.9 rendered-value and R7.10 joint visibility.

Regression-first repair:

```text
dc69e44206942ffb0012e1994d6d39249d1db4be
fix: reject static display none renders
```

The repair recognizes static inline `display:none` and `display:none !important` on active ancestors, while retaining static `display:block`. It deliberately does not solve dynamic style bindings, class stylesheets, computed CSS or general DOM/runtime semantics.

Focused/end-to-end regressions:

`tests/Pkc.CSharp.Tests/StaticCssRenderAuthorityRegressionTests.cs`

#### Exact-SHA repaired-candidate gates

```text
CI + full PKC tests + WorkPlay + PokeTrade   35687831689 — PASS
pinned Loren                                35687831632 — PASS
Loren-main canary                           35687831587 — PASS
pinned Jellyfin + parity/provenance         35687831537 — PASS

Release build        0 warnings / 0 errors
C# tests             164 / 164 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
```

Pinned Jellyfin remains fully project-semantic: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 knowledge Markdown files; parity/no-leak PASS. Artifact `10677695966`, digest `sha256:088353b7a1e30bf2aa68dce408246c2f88de50b843e88a7c57bb8fac595bdb60`.

#### Repaired real-repository benchmark

```text
base implementation: dc69e44206942ffb0012e1994d6d39249d1db4be
wrapper commit:      b3b0c5b40c0d34cafe3e7a60c28341ea777dd2cf
run:                 35687951314 — PASS, 3 / 3 jobs
```

The wrapper changes only the workflow branch-trigger line. Direct artifact inspection across pinned `jin12`, `agentic-angular`, and `kesetovic-crm` found zero supported current R7.9/R7.10 positives and confirmed the mutation-causality blocker remains closed.

Detailed evidence: `docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`.

The implementation session cannot certify its own repair. Fresh independent request:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-4-request.md`

### R7.11 — authority downgrade retains independent evidence

Permanent. No false business rule may replace lower-authority deterministic lineage/mutation/visibility evidence.

### R7.12 — portable knowledge rendering/parity

Permanent. `knowledge/`, `PKC_KNOWLEDGE.md` and ZIP must preserve critical meaning without raw `.pkc` or source-tree leakage.

### R7.13 — cross-benchmark regression

Permanent sequence:

```text
focused checkpoint regressions
→ full C# / frontend tests
→ Release build
→ PokeTrade known-answer
→ pinned Loren
→ Loren-main canary
→ pinned Jellyfin
→ portable parity/no-leak
→ independent rereview
```

### R7.14 — real-project positive yield

**REQUIRED for E completion; NOT PASS.**

At least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Do not modify a benchmark or weaken fail-closed authority to manufacture yield. The current stress benchmark still produces zero supported cross-layer positives; that is safety evidence, not R7.14 completion.

## Acceptance matrix

| PO question | Minimum proof |
| --- | --- |
| Where did this value originally come from? | R7.1 + R7.2 + R7.3 |
| If upstream changes later, does downstream change automatically? | R7.4 |
| What can change the value after creation? | R7.6 |
| Was it copied or computed? | R7.1 + R7.5 |
| What was the last source before a supported terminal boundary? | R7.7 |
| How did it reach DTO/API/frontend? | R7.8 + R7.9 |
| What backend + frontend conditions determine visibility? | R7.10 |
| What survives incomplete authority? | R7.11 |
| Does portable handoff preserve the answer? | R7.12 |
| Does it generalize without benchmark exceptions? | R7.13 |
| Does it produce a useful positive on unchanged real software? | R7.14 |

## Current exact action

Do **not** implement E yet.

```text
independent rereview #4 of exact dc69e44206942ffb0012e1994d6d39249d1db4be
→ PASS: mark R7.10 + all D PASS / COMPLETE; unlock only E
→ FAIL: regression-first minimum generic repair; rerun exact-SHA gates and independent review
```

R7.14 remains required for E even after D passes. V0.5 remains locked until V0.4.7 / V0.4.x PO-question-readiness acceptance completes.

## Version semantics

```text
roadmap:             V0.4.7-D / R7.10 pending independent rereview #4
tool/package:        RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```
