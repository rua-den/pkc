# V0.4.7 Cross-layer PO-question Readiness Acceptance Plan

Last updated: 2026-09-23

## Status

```text
V0.4.6           PASS / COMPLETE
V0.4.7-A         PASS / COMPLETE
V0.4.7-B         PASS / COMPLETE
V0.4.7-C         PASS / COMPLETE
V0.4.7-D / R7.9  PASS / COMPLETE
V0.4.7-D / R7.10 REPAIRED / ALL GATES PASS / PENDING REREVIEW #16
V0.4.7-D overall  PENDING INDEPENDENT REREVIEW #16
V0.4.7-E         LOCKED behind D
R7.14            REQUIRED FOR E / NOT PASS
product-value benchmark  NOT PASS / PARTIAL USEFULNESS
V0.5             LOCKED
```

Exact repaired R7.10 candidate:

```text
37a71172c8c425219aef35f5843ac2109810ae9d
fix: preserve Angular infrastructure render evidence
```

## Product-knowledge invariants

Keep business conditions, value lineage/provenance, mutation/causality, render authority and visibility authority distinct. Deterministically proven observable behavior may become authoritative product-behavior knowledge. Unsupported inference fails closed. Similar names are never sufficient proof.

## Product Owner acceptance questions

Portable knowledge must answer from evidence, or explicitly say the answer is not proven:

1. Where did this value originally come from?
2. If upstream changes later, does the existing downstream value change automatically?
3. What code path can change the value after creation?
4. Was it directly copied or computed?
5. What was the last observed source before persistence/return?
6. What backend and frontend conditions jointly determine visibility?
7. How does a value travel backend → projection/DTO → API → frontend → render?
8. What independently proven evidence survives when stronger authority is incomplete?
9. For a product workflow, who may perform it, what preconditions apply, what state changes, and what side effects occur?
10. Does a feature-level summary preserve the grounded rules present in its workflows?

Raw fact existence alone is insufficient.

## R7.10 — current repair

The current continuation extends the external-component projection boundary to cover linked/workspace packages, repository-bounded symlink targets, unresolved external symbols actually used in standalone component `imports`, bounded direct/const-array imports and Angular framework infrastructure imports that must not erase valid render evidence.

Latest repair chain:

```text
46dcf8941c67a11d96e4416774d02e555a8f6aaf  fix: bound linked Angular package selectors
2ea361ba9d5e6e61041f38ca79e6587e12a1b745  fix: fail closed unresolved Angular component imports
37a71172c8c425219aef35f5843ac2109810ae9d  fix: preserve Angular infrastructure render evidence
```

Native SVG direct-render authority remains intentionally unsupported/fail-closed.

## Exact-SHA verification

```text
CI + full PKC tests + WorkPlay + PokeTrade   35823346084 — PASS
pinned Loren                                35823346060 — PASS
Loren-main canary                           35823346077 — PASS
pinned Jellyfin + parity/provenance         35823346046 — PASS
```

```text
Release build        0 warnings / 0 errors
C# tests             253 / 253 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Jellyfin remains 43,365/43,365 project-semantic with 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 knowledge Markdown files and portable parity/no-leak PASS.

## R7.13 — cross-benchmark safety

Permanent sequence:

```text
focused regressions
→ full tests / Release
→ PokeTrade
→ pinned Loren
→ Loren-main canary
→ pinned Jellyfin
→ portable parity/no-leak
→ unchanged real-repository safety benchmark
→ independent rereview
```

Latest real-repo wrapper is a one-line branch-trigger change on exact production:

```text
production 37a71172c8c425219aef35f5843ac2109810ae9d
wrapper    f76c946e15cfe37d380745e0a2efb22cd3fc9123
run        35823872896 — PASS, 3 / 3
```

Canonical output is byte-stable against the preceding safety benchmark for all three pinned repos and no unsupported R7.9/R7.10 authority appears.

## R7.14 — positive real-project yield

**REQUIRED for E / NOT PASS.**

The current three pinned real repositories still produce zero supported authoritative R7.9/R7.10 cross-layer positives. This may pass safety, but cannot satisfy product acceptance.

Do not weaken authority or alter benchmark source to manufacture a positive.

## E product-value benchmark requirement

E must not treat “benchmark job green” as product acceptance.

Use a curated known-answer set verified independently from pinned source and score endpoint/capability discovery, permissions, business preconditions, direct and construction-time state changes, defaults/computations, side effects/integrations, controller/interface/concrete implementation behavior, UI interaction, UI→API linkage, feature-summary fidelity, cross-layer render/visibility authority and explicit unknowns.

Each known answer must be PASS / PARTIAL / FAIL / N/A.

Latest scorecard: `docs/benchmarks/2026-09-23-real-repo-product-value-scorecard.md`.

Current result:

```text
Safety / fail-closed stability        PASS
Backend workflow usefulness           PARTIAL / PROMISING
Permissions / endpoint discovery      GOOD
Direct controller preconditions       GOOD
Cross-method implementation behavior  NOT PASS
Object defaults / computations        NOT PASS
Side-effect synthesis                 NOT PASS
Feature-summary fidelity              NOT PASS
UI → API usefulness                   PARTIAL / REPO-DEPENDENT
R7.14 positive cross-layer yield      NOT PASS
Overall product-value benchmark       NOT PASS
```

Observed known-answer examples include successful recovery of Agentic login/signup validation and Kesetovic order transition rules, while exposing missing Jin12 service-level user filtering, missing AddOrder initial/computed state, omitted SignalR side effects and feature summaries that lose workflow rules.

## Current exact action

Do **not** implement E yet.

```text
independent rereview #16 of exact 37a71172c8c425219aef35f5843ac2109810ae9d
→ PASS: mark R7.10 + D PASS / COMPLETE; unlock only E
→ FAIL: regression-first minimum generic repair; rerun exact-SHA gates and rereview
```

Request: `docs/reviews/2026-09-23-v0.4.7-d-r7.10-rereview-16-request.md`.

When E opens, the product-value scorecard becomes acceptance work rather than an optional benchmark report.

## Version semantics

```text
roadmap:             V0.4.7-D / R7.10 pending independent rereview #16
tool/package:        RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```
