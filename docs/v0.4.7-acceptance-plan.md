# V0.4.7 Cross-layer PO-question Readiness Acceptance Plan

Last updated: 2026-09-22

## Status

```text
V0.4.6           PASS / COMPLETE
V0.4.7-A         PASS / COMPLETE
V0.4.7-B         PASS / COMPLETE
V0.4.7-C         PASS / COMPLETE
V0.4.7-D / R7.9  PASS / COMPLETE
V0.4.7-D / R7.10 REPAIRED / ALL GATES PASS / PENDING REREVIEW #2
V0.4.7-D overall  PENDING INDEPENDENT REREVIEW #2
V0.4.7-E         LOCKED behind D
V0.5             LOCKED
```

Exact repaired R7.10 candidate:

```text
da5d23771ef8c9d58d0333d1f949e8d742210043
fix: require visible text interpolation
```

## Product-knowledge invariants

Keep these evidence classes distinct:

```text
business conditions
value lineage / provenance
mutation / causality
```

Authority rule:

```text
deterministically proven observable behavior
→ may become authoritative product-behavior knowledge

deterministic implementation evidence without full authority
→ retain as lower-authority lineage / provenance / mutation / causal evidence

unsupported inference
→ fail closed; do not invent
```

Same/similar names are never sufficient proof. Conservative authority downgrade must not erase deterministic evidence.

## Product Owner acceptance questions

Generated portable knowledge must answer these questions from evidence or explicitly say the answer is not proven:

1. Where did this value originally come from?
2. If the upstream value changes later, does the existing downstream value change automatically?
3. What code path can change this value after creation?
4. Was this value directly copied or computed?
5. What was the last observed source before a supported persistence/return boundary?
6. What backend conditions and frontend conditions jointly determine whether an item/value is visible?
7. How does a value travel through backend → DTO/projection → API → frontend composition?
8. If authority is incomplete, what lineage or causal evidence is still deterministically known?

Raw fact existence alone is insufficient; portable knowledge must support a PO-level answer.

## Canonical lineage semantics

| Semantic | Required meaning |
| --- | --- |
| `copy` | Direct assignment without modeled computation. |
| `snapshot` | Stored copied/derived value captures a point in time; later upstream changes do not automatically mutate it. |
| `derivation` | Downstream value is computed from one or more proven inputs. |
| `reference/dynamic` | Downstream read resolves from upstream state at read time. |
| `override` | Later write supersedes a previously sourced value without erasing original origin. |
| `mutation` | A code path writes downstream value/state after prior state existed. |

`copy` and `snapshot` are not synonyms; `override` must not rewrite history.

## Cross-layer acceptance chain

V0.4.7 must support a proven chain equivalent to:

```text
backend/domain value
→ DTO/projection property
→ API response field
→ typed frontend API result
→ component/view-model assignment
→ active visible rendered text
```

C owns backend → DTO/projection → API response.
R7.9 owns the first supported API result → frontend state → rendered-value chain.
R7.10 owns bounded joint backend/frontend visibility for that same exact proven path.
E validates knowledge-only answers, real-project usefulness, and portable parity.

## Regression gates

### R7.1–R7.4 — origin, multi-hop identity, same-name isolation, snapshot/reference semantics

**PASS through A.**

Accepted A:

```text
09a0c7f2a058adfd7ddb7b4ac2feb2d17580a429
```

### R7.5–R7.7 — derivation, later mutation/override, bounded terminal source

**PASS through B.**

Accepted B:

```text
17fd30b3a4b8178208adabc12c40dee060bedb54
```

### R7.8 — DTO/projection mapping

**PASS / COMPLETE through C.**

Accepted C:

```text
fbb64b9917da1f63362558355201ff7998384ba0
```

C proves exact target-project-semantic domain/entity property → DTO/projection property → API response identity and fails closed on name-only, assembly/type/member collisions, custom accessors/conversions, unsupported effects/control flow and missing project semantics.

### R7.9 — API result to frontend rendered value

**PASS / COMPLETE.**

Accepted R7.9:

```text
fc4bfa7042f59620b2c7ba1c4f6700cf3b02172a
```

Supported bounded chain:

```text
accepted C API-response terminal
→ explicit semantic wire identity
→ typed frontend HTTP response contract/member
→ exact resolved service/API method
→ exact subscribe result receiver/member assignment
→ exact component/view-model target
→ authoritative active visible Angular text interpolation
→ rendered UI value
```

Route identity may scope endpoint ↔ API call only. It does not prove the property edge.

### R7.10 — joint backend/frontend visibility

**REPAIRED / ALL GATES PASS / PENDING INDEPENDENT REREVIEW #2.**

For the same already-proven R7.9 value path, portable knowledge may compose backend selection/eligibility and frontend visibility only when exact identities prove the same item/value flow.

Current bounded backend positive:

```csharp
var item = source.Single(item => predicate);
// or Enumerable.First(predicate)
return new Response
{
    DisplayValue = item.Value
};
```

Requirements include:

- target-project Roslyn semantics;
- `System.Linq.Enumerable.Single/First`, not Queryable;
- exact selected reference local;
- scalar auto-properties;
- direct final response initializer;
- no user-defined conversion;
- exact selection invocation span and exact projection fact identity.

Current bounded frontend positive:

```html
@if (displayPrice > 0) {
  <strong>{{ displayPrice }}</strong>
}
```

Composition identity:

```text
renderedTerminal.frontendRenderFactId
= visibility.renderFactId

renderedTerminal.backendProjectionFactId
→ projection.selectionPredicateFactId
→ predicate.selectedApiProjectionFactId
```

No textual similarity may substitute for these IDs. Frontend visibility cannot upgrade an `observed-only` backend predicate. Multiple/nested/unsupported visibility paths fail closed and retain lower-authority evidence.

#### Rereview #1 blocker and repair

Initial R7.10 review candidate:

```text
eb64309263489a2b9bd658762b4526a4a32a8508
```

Independent rereview #1 found that non-visible/inert interpolation could be promoted as rendered UI value. Compile-valid counterexamples included:

- interpolation under bare `<ng-template>`;
- interpolation inside HTML comments;
- interpolation in an HTML tag/attribute.

Regression-first repairs:

```text
05ad6cb937010702a3fd01d5ef756e31bc4e8d9b
099fabfcaeecbaf009b75052d20075745ee02437
da5d23771ef8c9d58d0333d1f949e8d742210043
```

The accepted candidate for rereview #2 therefore requires active visible text interpolation inside the bounded grammar. Unsupported dynamically instantiated template semantics may lose coverage; they must not gain false authority.

#### Exact-SHA repaired-candidate gates

```text
CI + full PKC tests + WorkPlay + PokeTrade   35650084914 — PASS
pinned Loren                                35650084917 — PASS
Loren-main canary                           35650084926 — PASS
pinned Jellyfin + parity/provenance         35650084759 — PASS

Release build        0 warnings / 0 errors
C# tests             155 / 155 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
```

#### Repaired real-repository benchmark

```text
base implementation: da5d23771ef8c9d58d0333d1f949e8d742210043
wrapper commit:      d38b21a68e007c1a85a8ec8e906a08f0db440661
run:                 35650761753 — PASS, 3 / 3 jobs
```

The wrapper changes only a workflow branch-trigger line. Artifact inspection across pinned `jin12`, `agentic-angular`, and `kesetovic-crm` shows PKC exit `0`, no false current R7.9/R7.10 cross-layer positive, and the prior mutation-causality blocker remains closed.

Detailed evidence:

`docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`

Rereview #1 cannot self-certify its repairs. Astra must independently rereview exact `da5d2377...`.

Request:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-rereview-2-request.md`

### R7.11 — authority downgrade retains independent evidence

Permanent. No false business rule may replace lower-authority deterministic lineage/mutation/visibility evidence.

### R7.12 — portable knowledge rendering/parity

Permanent. `knowledge/`, `PKC_KNOWLEDGE.md` and ZIP must preserve critical meaning without raw `.pkc` or source-tree leakage.

### R7.13 — cross-benchmark regression

Permanent sequence:

```text
focused checkpoint regressions
→ full C# / frontend tests
→ Release build
→ PokeTrade known-answer
→ pinned Loren
→ Loren-main canary
→ pinned Jellyfin
→ portable parity/no-leak
→ independent rereview
```

### R7.14 — real-project positive yield

**REQUIRED for E completion; NOT YET PASS.**

At least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Do not modify a benchmark or weaken fail-closed authority to manufacture yield. If current benchmarks do not fit, add a representative unchanged real project.

## Acceptance matrix

| PO question | Minimum proof |
| --- | --- |
| Where did this value originally come from? | R7.1 + R7.2 + R7.3 |
| If upstream changes later, does downstream change automatically? | R7.4 |
| What can change the value after creation? | R7.6 |
| Was it copied or computed? | R7.1 + R7.5 |
| What was the last source before a supported terminal boundary? | R7.7 |
| How did it reach DTO/API/frontend? | R7.8 + R7.9 |
| What backend + frontend conditions determine visibility? | R7.10 |
| What survives incomplete authority? | R7.11 |
| Does portable handoff preserve the answer? | R7.12 |
| Does it generalize without benchmark exceptions? | R7.13 |
| Does it produce a useful positive on unchanged real software? | R7.14 |

## Current exact action

Do **not** implement E yet.

```text
Astra independent rereview #2 of exact da5d2377...
→ if PASS: mark R7.10 + all D PASS / COMPLETE; unlock only E
→ if FAIL: regression-first minimum generic repair; rerun exact-SHA gates and independent review
```

R7.14 remains required for E even after D passes. V0.5 remains locked until V0.4.7 / V0.4.x PO-question-readiness acceptance completes.

## Version semantics

```text
roadmap:             V0.4.7-D / R7.10 pending independent rereview #2
tool/package:        RuaDen.Pkc.Tool 0.4.3-preview.2
C# raw schema:       0.4.4-csharp-raw
merged facts schema: 0.4.4
cross-stack schema:  0.4.6
frontend schema:     0.4.3-frontend
```

Change package/schema only for a deliberate reviewed contract/distribution change, not because a roadmap checkpoint advances.
