# V0.4.7-D / R7.10 Independent Rereview #5 Request — 2026-09-22

## Review target

Independently rereview exact production SHA:

```text
1fc4d212b9c7add2f012f51adf3eef0c16f34dae
fix: reject static visibility hidden renders
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Do not reset `main`; review production behavior at the exact SHA named above.

## Current gate state

```text
V0.4.7-A                                PASS / COMPLETE
V0.4.7-B                                PASS / COMPLETE
V0.4.7-C                                PASS / COMPLETE
V0.4.7-D / R7.9                         PASS / COMPLETE
V0.4.7-D mutation-causality blocker     PASS / CLOSED
V0.4.7-D / R7.10                        REPAIRED / ALL GATES PASS / PENDING REREVIEW #5
V0.4.7-D overall                        PENDING INDEPENDENT REREVIEW #5
V0.4.7-E                                LOCKED behind D
R7.14 real-project positive yield       NOT PASS / REQUIRED FOR E
V0.5                                    LOCKED
```

Do not implement E during this review.

## What rereview #4 found

Independent rereview #4 of `dc69e442...` found this compile-valid/runtime-valid false-authority shape:

```html
<section style="visibility: hidden">
  @if (displayPrice > 0) {
    <strong>{{ displayPrice }}</strong>
  }
</section>
```

`dc69e442...` recognized static inline `display:none` but not static inline `visibility:hidden`. The hidden interpolation could retain authoritative `ui-member-render`, acquire the supported `@if`, and feed false R7.9/R7.10 observable behavior.

Review record:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-4.md`

## Repair under review

`1fc4d212...` extends the existing bounded static inline declaration parser only:

```text
display:none                 → suppress render authority
display:none !important      → suppress render authority
visibility:hidden            → suppress render authority
visibility:hidden !important → suppress render authority
```

matching property/value case-insensitively.

Positive safeguards:

```text
display:block      → existing bounded render authority may remain
visibility:visible → existing bounded render authority may remain
```

The implementation deliberately does not claim:

- Angular `[style]`, `[style.display]`, `[style.visibility]`;
- interpolated or otherwise dynamic inline styles;
- class stylesheets or external stylesheets;
- browser cascade/computed CSS;
- opacity/runtime script mutation;
- signals, outlets, structural directives or general DOM semantics.

Do not fail this review merely because these unsupported classes are not modeled. A blocker requires a concrete current false-positive or authority violation within actual behavior emitted by PKC.

## Existing regression coverage

Primary regression file:

`tests/Pkc.CSharp.Tests/StaticCssRenderAuthorityRegressionTests.cs`

It covers:

- static `display:none` frontend negative;
- static `display:none` end-to-end R7.9/R7.10 negative;
- `display:block` positive safeguard;
- static `visibility:hidden` frontend negative;
- static `visibility:hidden` end-to-end R7.9/R7.10 negative;
- `visibility:visible` positive safeguard.

Earlier permanent render-authority regressions also cover inert `<ng-template>`, comments/tag/attribute interpolation, static HTML `hidden`, ancestry closing/sibling behavior and fail-closed ambiguity.

## Exact-SHA executable evidence

All standard gates passed on exact repaired production:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35696272391 — PASS
pinned Loren                                35696272323 — PASS
Loren-main canary                           35696272328 — PASS
pinned Jellyfin + parity/provenance         35696273519 — PASS
```

Core:

```text
Release build        0 warnings / 0 errors
C# tests             167 / 167 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin:

```text
source build          PASS, 0 warnings / 0 errors
43,365 facts | 195,316 relations | 386 workflow candidates
116 product features | 504 knowledge Markdown files
43,365 / 43,365 project-semantic
portable parity/no-leak PASS
artifact 10680339386
sha256:a31f2b97674c0a18fa4b3587a1d422dc1a7228e75ba67015f86791ca73467031
```

## Real-repository stress benchmark

```text
base production: 1fc4d212b9c7add2f012f51adf3eef0c16f34dae
wrapper:         7101b4c911539821c7c368203e0b05d150cbea64
run:             35696381103 — PASS, 3 / 3 jobs
```

The wrapper differs only in the workflow branch trigger. All three unchanged benchmark repositories emit zero supported current R7.9/R7.10 positives. This is fail-closed evidence only; R7.14 remains NOT PASS.

## Rereview mission

Do not simply reconfirm the `visibility:hidden` fixture. Search adversarially for a **new compile-valid/runtime-valid counterexample** that current production could promote into false PO-facing R7.9/R7.10 authority.

High-value challenge areas include:

1. **Static HTML presentation containers** — concrete source-backed semantics such as inert/closed containers where a current interpolation would actually be non-visible despite surviving the bounded scanner. Verify real browser/Angular semantics before calling a blocker.
2. **Static presentation suppression not yet modeled** — only when a concrete current false-positive can be demonstrated; do not require a general CSS engine.
3. **HTML parser normalization / raw or foreign content** — malformed-looking text is not enough; require compile-valid/runtime-valid Angular/HTML and show how current regex ancestry produces wrong authority.
4. **Multiple or same-line interpolations / identity collisions** — try to cause one source-line render fact to authorize the wrong occurrence or multiple distinct renders to collapse incorrectly.
5. **Quoted/tag syntax edge cases** — demonstrate a concrete scanner mismatch, not theoretical parser incompleteness.
6. **R7.10 backend identity** — selected local, projection fact, overload/symbol identity, same-name collision and `observed-only` non-upgrade must remain exact.
7. **Evidence retention** — stronger authority failure must not erase accepted C/R7.9 lineage or lower-authority deterministic evidence.
8. **Mutation-causality closure** — ensure unrelated runtime-pattern-selected mutations are still not promoted into caller-object state changes.

A valid blocker must show all of:

```text
compile-valid/runtime-valid source shape
→ current PKC emits or can emit an authoritative fact/candidate
→ emitted authority contradicts actual observable behavior or exact identity
→ violation is inside the accepted R7.9/R7.10 claim boundary
```

A merely unsupported feature that fails closed is not a blocker.

## Decision rule

If no new blocker exists:

```text
mark R7.10 PASS / COMPLETE
→ mark all V0.4.7-D PASS / COMPLETE
→ unlock only V0.4.7-E
→ keep R7.14 NOT PASS / REQUIRED FOR E
→ keep V0.5 locked
```

If a blocker exists:

```text
record exact counterexample and violated invariant
→ keep D open and E locked
→ add focused regression first
→ implement minimum generic repair
→ run focused + full relevant verification
→ one coherent production push
→ exact-SHA gates + benchmarks
→ another independent rereview
```

Do not weaken proof authority, add project-specific exceptions, or modify a benchmark merely to manufacture a positive R7.14 shape.
