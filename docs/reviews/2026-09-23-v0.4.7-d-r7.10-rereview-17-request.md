# V0.4.7-D R7.10 Independent Rereview #17 Request — 2026-09-23

## Purpose

Perform a fresh independent rereview. This is not an implementation continuation.

Review exact production SHA:

```text
96205a9a643864facaf9642a3b390ddcdbed59d9
fix: tolerate duplicate Angular import aliases
```

A docs-only `[skip ci]` commit may sit above production on `main`. Do not review the docs HEAD as production and do not reset `main`.

Rereview #16 targeted the older `37a711...` candidate. It is superseded because a new compile-valid R7.10 counterexample was found before #16 completed.

## Read order

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. prior independent R7.10 reviews through #14
7. `docs/benchmarks/2026-09-23-real-repo-product-value-scorecard.md`
8. this request

Then inspect current `main`, recent production commits, regressions and production code.

## Accepted context

Keep accepted A/B/C/R7.9 and mutation-causality closed unless a new real regression is demonstrated.

Rereview #14 was a real FAIL on external component projection authority. Subsequent repairs covered package selector discovery, nested dependency roots, linked/workspace packages, unresolved external component imports, Angular framework infrastructure and bounded const-array handling.

The new continuation repair does not reopen those accepted predecessor checkpoints. It extends the same R7.10 projection-authority boundary across local Angular module indirection.

## New counterexample that invalidated #16

Compile-valid shape:

```text
external package symbol
→ local shared-imports.ts helper
→ optional local barrel / named re-export
→ standalone component imports
→ rendered value
```

The previous implementation could fail closed for a direct unresolved external symbol but lose that risk when the symbol was hidden behind local import/re-export or helper-array indirection.

That violates the proof boundary because the component projection boundary is still unknown, but render authority could survive.

## Production repair chain to challenge

```text
10876a28d15767930e0bc6de95def4993bb7265f  fix: close indirect Angular component import authority
d81be9560c741296271376aca7481db3d9925e41  fix: fail closed unsupported Angular import indirection
96205a9a643864facaf9642a3b390ddcdbed59d9  fix: tolerate duplicate Angular import aliases
```

Primary implementation:

```text
src/Pkc.Frontend/AngularComponentImportClosureAuthorityFilter.cs
src/Pkc.Frontend/AngularUnsupportedComponentImportIndirectionAuthorityFilter.cs
src/Pkc.Frontend/AngularFrontendAdapter.cs
```

Focused regressions:

```text
tests/Pkc.CSharp.Tests/AngularComponentImportClosureAuthorityRegressionTests.cs
tests/Pkc.CSharp.Tests/AngularUnsupportedComponentImportIndirectionAuthorityRegressionTests.cs
```

## Repair semantics

Challenge whether the implementation correctly:

- propagates unresolved projection risk through local named imports;
- follows local named re-exports and export-all barrels;
- follows default import/export risk where explicitly supported;
- propagates through bounded const-array component imports;
- preserves known Angular framework infrastructure positives;
- fails closed for unsupported scalar alias/default-re-export indirection;
- avoids runtime exceptions when duplicate alias names occur in different scopes;
- removes only authoritative `ui-member-render` facts for the affected component and dependent relations;
- preserves lower-authority frontend evidence rather than deleting unrelated facts.

Do not reward a shape merely because it returns no authority. A supported shape that safely fails closed is acceptable; a false positive is a blocker.

## Exact candidate evidence

```text
CI + full PKC tests + WorkPlay + PokeTrade   35832501567 — PASS
pinned Loren                                35832501543 — PASS
Loren-main canary                           35832501552 — PASS
pinned Jellyfin + parity/provenance         35832501534 — PASS
real-repository benchmark                   35833147258 — PASS, 3 / 3
```

Core: Release 0 warnings/errors, C# 259/259, frontend 13/13, tool install, WorkPlay and PokeTrade PASS.

Pinned Jellyfin: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 Markdown files, 43,365/43,365 project-semantic, portable parity/no-leak PASS.

Benchmark wrapper:

```text
production  96205a9a643864facaf9642a3b390ddcdbed59d9
wrapper     65c02df67938197d929d30793dc012dbc3878ca3
```

The wrapper changes only the benchmark branch trigger.

Pinned benchmark output sizes remain at the established baseline:

```text
agentic-angular  441 facts / 660 relations / 26 knowledge files
jin12-crm        415 facts / 1,580 relations / 25 knowledge files
kesetovic-crm    488 facts / 2,054 relations / 28 knowledge files
```

The production delta is fail-closed authority filtering only. It is not product-value work and must not be credited as fixing the known-answer scorecard gaps.

## High-value adversarial seams

Prioritize compile-valid/runtime-valid counterexamples around:

- alias chains crossing multiple local files;
- named ↔ default re-export combinations;
- namespace imports and namespace member use in component imports;
- multiple barrels and cycles;
- duplicate identifiers in different scopes;
- TypeScript path aliases that resolve to local product source;
- package imports that are resolvable but do not prove Angular component metadata;
- component vs directive distinction;
- local/external selector collision;
- conditional component imports or dynamic metadata shapes;
- exact component decorator association;
- lower-authority evidence preservation after filtering.

Also retain prior R7.10 seams: selector matching, static/bound attributes, HTML/SVG namespace transitions, exact enclosing control scope, exact fact-ID composition, observed-only backend authority and mutation-causality closure.

A blocker requires:

1. compile-valid/runtime-valid input in the supported stack;
2. deterministic false or over-authoritative R7.9/R7.10 output;
3. a precise violated proof boundary.

## Product-value benchmark is separate

`docs/benchmarks/2026-09-23-real-repo-product-value-scorecard.md` remains **NOT PASS / PARTIAL USEFULNESS** because PKC still misses interface-to-implementation traversal, construction/default state, side-effect synthesis, feature-summary fidelity and real-project positive R7.14 yield.

Those gaps matter for E. They do not by themselves fail D/R7.10 unless they produce false cross-layer authority.

Do not weaken render/visibility authority to improve benchmark yield.

## Decision rule

If no new R7.10 blocker exists:

```text
R7.10       PASS / COMPLETE
V0.4.7-D    PASS / COMPLETE
V0.4.7-E    UNLOCKED / NEXT
R7.14       NOT PASS / REQUIRED FOR E
V0.5        LOCKED
future W/U  LOCKED until V0.4.7 completes
```

Update status/handoff/milestones/acceptance accordingly.

If a blocker exists, keep E locked and record the exact counterexample, false output, violated boundary, minimum generic repair and regression.

The independent reviewer should not modify production unless explicitly converting the work into an implementation continuation.

## After D passes

E must use the known-answer product-value scorecard as an acceptance gate, not just rerun green workflows. R7.14 still requires at least one unchanged real repository to emit a supported positive cross-layer answer.
