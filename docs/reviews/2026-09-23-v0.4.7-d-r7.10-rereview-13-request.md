# V0.4.7-D R7.10 Independent Rereview #13 Request — 2026-09-23

## Purpose

Perform a fresh independent rereview, not an implementation continuation.

Review exact production SHA:

```text
7818c7ed646b30cb7b8505f053572783e075af6f
fix: fail closed on native SVG render authority
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Do not treat docs HEAD as production and do not reset `main`.

## Read order

Read, in order:

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. independent R7.10 rereview records #1 through #11 and the rereview #12 request/history
7. `docs/benchmarks/2026-09-23-r7.10-native-svg-fail-closed-benchmark.md`
8. this request

Then inspect current `main`, recent commits, regressions and production code.

## Accepted context

Keep accepted A/B/C/R7.9 and the mutation-causality repair closed unless a new real regression is demonstrated.

Previous rereviews/repairs closed concrete false-positive classes across inert templates/comments, presentation suppression, Angular hidden bindings, control-flow lexical scope, `ngNonBindable`, quoted attributes, unsupported nested/structural visibility, style raw text, SVG definitions/resources, SVG text structure, presentation opacity and text-content placement.

Do not merely replay those regressions.

## Why a fresh rereview is required

The prior rereview #12 request targeted:

```text
f9b20c27820a7ea9ac911222c613c2f9cdfb696f
fix: reject SVG resource renders
```

Before an independent acceptance record for that exact candidate was committed, implementation advanced through additional R7.10 SVG authority hardening:

```text
11b6b21315ac21f7b5f947c933b07bd9bb7a3298  fix: enforce SVG text render authority
7dd00c1a960b8e85232d67b779e7906b4206cce6  fix: require SVG text ancestor
3fe0d4452b49a6b4c2b16d422af3975afbda40a8  fix: reject transparent SVG renders
d4416c4a13a04db46091bbffff1c71566c0d5d0c  fix: bound SVG text content authority
```

The implementation continuation reconciled that stale handoff rather than assuming rereview #12 had passed, and independently challenged the newest candidate.

## Current blocker and repair

A compile-valid/runtime-valid native SVG counterexample remained on `d4416c4a...`:

```html
@if (isAllowed) {
  <svg>
    <text fill="none" stroke="none">{{ displayPrice }}</text>
  </svg>
}
```

The interpolation is structurally inside a text element but the element paints neither fill nor stroke. The predecessor could still promote that occurrence as authoritative directly-visible output because its bounded filters did not prove the full native SVG paint/layout model.

Exact repaired production:

```text
7818c7ed646b30cb7b8505f053572783e075af6f
```

The repair intentionally changes the authority boundary instead of adding another incomplete list of SVG presentation properties:

- native SVG interpolation is unsupported for V0.4.7 authoritative directly-visible render proof;
- the implementation does not claim an SVG paint/layout/browser-engine model;
- ordinary HTML interpolation remains eligible for existing render authority;
- HTML descendants inside SVG `foreignObject` remain eligible through the HTML namespace path;
- lower-authority evidence must survive render-authority rejection.

Regression coverage proves the concrete no-paint case, fail-closed native SVG text/tspan/content-model behavior, presentation-suppression cases, and a positive `foreignObject` HTML path.

## Exact repaired-candidate evidence

Production `7818c7ed...` passed:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35765278584
pinned Loren                                35765278607
Loren-main canary                           35765278416
pinned Jellyfin + parity/provenance         35765278447
three-repository benchmark                  35765659218
```

Core: Release build 0 warnings/errors, C# 231/231, frontend 13/13, tool install, WorkPlay and PokeTrade PASS.

Pinned Jellyfin: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 Markdown files, 43,365/43,365 project-semantic, portable parity PASS. Artifact `10712181821`, digest `sha256:281403635a62680c71640f0a839fdbeffdbcaf72d59b38407789c65037c59adb`.

The unchanged three-repository stress benchmark passes 3/3 and still emits zero supported current R7.9/R7.10 positives. This is stability/safety evidence, not R7.14 completion.

Agentic mutation-causality remains closed: the exact two `runtime-pattern-variable / caller-object-unproven` `UpdatedAt` mutation fact IDs occur zero times in feature candidates, product features and portable/canonical Markdown.

Local execution is not claimed because the execution container could not resolve `github.com` while cloning. The evidence above is exact-SHA GitHub verification.

## Review objective

Independently search for another compile-valid/runtime-valid counterexample that causes false or over-authoritative R7.9/R7.10 output.

High-value seams now include:

- HTML/SVG namespace transitions, especially entering/leaving `foreignObject`;
- malformed or ambiguous template structure only where Angular/runtime acceptance and current scanner behavior can still over-promote authority;
- exact control-block scope association across HTML/SVG/template lexical boundaries;
- same-line identity collisions only when they create real false authority or deterministic crash;
- exact fact-ID composition and authority downgrade behavior;
- unsupported nested/control/structural visibility paths that might still be partially promoted;
- backend/frontend identity collisions only where exact current evidence can wrongly join them;
- backend observed-only authority must never be upgraded by frontend evidence;
- mutation-causality closure must remain intact.

Native SVG directly-visible render authority is intentionally **unsupported** at this checkpoint. Lack of native SVG positive authority is not a blocker and must not be treated as a request to build a browser/SVG engine inside V0.4.7.

A blocker requires:

1. compile-valid/runtime-valid input for the relevant supported stack/configuration;
2. current production emits or can deterministically emit false/over-authoritative render, visibility, R7.9 terminal, R7.10 joint fact or PO-facing rule;
3. the violated proof boundary is named precisely.

An unsupported shape that fails closed is not a blocker.

## Decision rule

If no blocker exists:

```text
R7.10       PASS / COMPLETE
V0.4.7-D    PASS / COMPLETE
V0.4.7-E    UNLOCKED / NEXT
R7.14       NOT PASS / REQUIRED FOR E
V0.5        LOCKED until E completes
future W/U  LOCKED until V0.4.7 completes and roadmap is formally re-baselined
```

Update status/handoff/milestones/acceptance accordingly and unlock only E.

If a blocker exists, keep E locked; record the exact counterexample, false output, violated boundary, minimum generic regression-first repair and required regression. Do not modify production in the independent review unless explicitly converted to implementation work.

## After D passes

Finish V0.4.7-E, including R7.14 real-project positive yield: at least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Only after E and the V0.4.x PO-question-readiness exit gate pass may the prepared post-V0.4.7 roadmap re-baseline be applied.

Prepared future packet:

```text
docs/plans/2026-09-22-ai-workspace-continuous-update-plan.md
docs/reviews/2026-09-22-ai-workspace-continuous-update-plan-self-review.md
```

It recommends AI workspace + `run/verify` first, semantic `update/diff` second, and Azure DevOps evidence after those productization gates. This request does not unlock that work.
