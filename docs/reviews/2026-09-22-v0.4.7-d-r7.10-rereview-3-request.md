# V0.4.7-D / R7.10 Independent Rereview #3 Request — 2026-09-22

## Review mode

This is an **independent rereview**, not an implementation continuation.

Do not modify production code merely to make existing fixtures pass. Independently search for another compile-valid/runtime-valid counterexample to the current proof boundary. Green CI and benchmark runs are regression-stability evidence, not semantic acceptance.

Do not start V0.4.7-E during this review.

## Exact production SHA to review

Review exactly:

```text
97161baa2d0aff9131a7acf9db752393ae913d64
fix: reject statically hidden rendered text
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Review production behavior at `97161baa...`; do not reset `main`.

## Read first

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. `docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-1.md`
7. `docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-2.md`
8. `docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`

Then inspect current `main`, recent commits, regressions and production code. Do not infer acceptance from documentation claims.

## Accepted predecessors — keep closed unless a real regression exists

```text
V0.4.7-C
fbb64b9917da1f63362558355201ff7998384ba0

V0.4.7-D / R7.9 baseline
fc4bfa7042f59620b2c7ba1c4f6700cf3b02172a

Mutation-causality repair
67624944da27ff1f1f5a1154018a255aae11d1fe
```

R7.10 rereview may expose a shared render-authority flaw consumed by R7.9/R7.10, but accepted exact identity and mutation boundaries must not be reopened without a concrete regression.

## R7.10 product question

For the **same exact R7.9-proven value path**:

> What backend selection condition and frontend visibility condition jointly determine whether the rendered value is visible?

## Current bounded backend positive

```csharp
var product = _products.Single(product => product.IsPublished);
// or Enumerable.First(predicate)

return new PriceResponse
{
    DisplayPrice = product.Price
};
```

Requirements remain intentionally narrow:

- target-project Roslyn semantics;
- exact `System.Linq.Enumerable.Single` or `First`, not Queryable;
- exact selected reference local;
- exact scalar auto-properties;
- direct final response object initializer;
- no user-defined conversion;
- exact selection invocation span;
- exact projection fact identity.

The backend predicate is authoritative for selected API output only when the exact predicate/selection/projection path is proven.

## Current R7.9 rendered-value boundary

R7.9 must prove:

```text
C-proven API response property
→ explicit wire identity
→ typed frontend HTTP result member
→ exact resolved service/result assignment
→ exact component/view-model member
→ authoritative active visible Angular text interpolation
→ rendered UI value
```

The phrase **active visible Angular text interpolation** is a proof boundary, not a synonym for arbitrary `{{ ... }}` text.

Rereview #1 already found and repaired authority for interpolation that is:

```text
under inert <ng-template>
inside an HTML comment
inside an HTML tag/attribute
```

Rereview #2 found and the current candidate repairs interpolation beneath a static HTML `hidden` ancestor:

```html
<section hidden>
  @if (displayPrice > 0) {
    <strong>{{ displayPrice }}</strong>
  }
</section>
```

The current repair also covers boolean presence semantics such as `hidden="false"`, nested hidden ancestry, closed hidden siblings, void/self-closing elements, comments, and fail-closed mismatched ancestor structure.

Dynamic `[hidden]`, arbitrary CSS visibility, signals, outlets, structural directives and general runtime DOM semantics are **not claimed** by this repair.

Do **not** merely re-confirm the exact static-hidden fixtures. Search for another authority-crossing shape.

## Current bounded frontend R7.10 positive

```html
@if (displayPrice > 0) {
  <strong>{{ displayPrice }}</strong>
}
```

Only one supported enclosing active Angular `@if` is accepted. Unsupported nested/multiple/ambiguous visibility fails closed.

## Exact composition identity to challenge

A joint edge must reuse exact fact IDs:

```text
renderedTerminal.frontendRenderFactId
= visibility.renderFactId

renderedTerminal.backendProjectionFactId
→ projection.selectionPredicateFactId
→ predicate.selectedApiProjectionFactId
```

Textual property/type/predicate similarity must not substitute for these IDs.

Frontend evidence cannot upgrade an `observed-only` backend predicate into an authoritative combined rule. Failure of stronger composition must preserve independently proven lower-authority evidence.

## High-value challenge areas

Search beyond the already repaired fixtures. Useful challenge directions include:

1. other compile-valid HTML/template constructs that contain text interpolation but prevent user-visible presentation;
2. ancestor parsing collisions, especially legal tag/attribute text that could corrupt the bounded stack;
3. raw-text / escapable-raw-text HTML elements or template-owned content with different rendering semantics;
4. multiple interpolation occurrences sharing a source line and exact source-line identity;
5. legal Angular control-flow combinations that can make a syntactically enclosing `@if` non-authoritative or ambiguous;
6. same-name/same-text collisions attempting to bypass exact R7.9 render fact identity;
7. backend overload/local/symbol collisions around selected API projection proof;
8. predicate authority attached to the wrong selection or projection;
9. frontend evidence upgrading `observed-only` backend evidence;
10. independently proven C/R7.9/lineage/mutation evidence being dropped when R7.10 composition fails.

A false negative caused by intentionally unsupported dynamic semantics is not by itself a blocker. A false or over-authoritative PO-facing claim is.

## Production seams to inspect

Especially inspect:

```text
src/Pkc.CSharp/CSharpSelectedApiProjectionLineageEnricher.cs
src/Pkc.CSharp/CSharpSelectedApiPredicateAuthorityEnricher.cs
src/Pkc.Frontend/AngularRenderedMemberAuthorityFilter.cs
src/Pkc.Frontend/AngularRenderedMemberVisibilityEnricher.cs
src/Pkc.Frontend/AngularRenderedMemberVisibilityAuthorityFilter.cs
src/Pkc.Knowledge/ApiFrontendBindingCandidateEnricher.cs
src/Pkc.Knowledge/JointVisibilityCandidateEnricher.cs
src/Pkc.Knowledge/JointVisibilityKnowledgeSynthesizer.cs
```

Focused regressions include:

```text
tests/Pkc.CSharp.Tests/JointVisibilityRegressionTests.cs
tests/Pkc.CSharp.Tests/JointVisibilityTemplateAuthorityRegressionTests.cs
```

## Exact-SHA verification already PASS

On exact production SHA `97161baa2d0aff9131a7acf9db752393ae913d64`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35686749488 — PASS
pinned Loren                                35686749493 — PASS
Loren-main canary                           35686749523 — PASS
pinned Jellyfin + parity/provenance         35686749575 — PASS
```

Core verification:

```text
Release build        0 warnings / 0 errors
C# tests             161 / 161 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin portable parity/no-leak passed with 43,365 project-semantic facts and artifact `10676304879`.

These gates prove regression stability and clean-environment execution, not semantic correctness.

## Repaired pinned real-repository benchmark

Wrapper based exactly on production SHA:

```text
base implementation: 97161baa2d0aff9131a7acf9db752393ae913d64
branch:              benchmark/r710-hidden-97161baa
wrapper commit:      850eea0b64f464bdc1a5ea536adb39d4b912c650
run:                 35687153720 — PASS, 3 / 3 jobs
```

Compare confirms the wrapper changes only one branch-trigger line; production/test source is byte-identical.

Artifact inspection across all three unchanged repositories found:

```text
R7.9 rendered-value terminal: 0
selected API projection:      0
ui-member-visibility:         0
joint-visibility candidate:   0
combined visibility rule:     0
```

Agentic mutation-causality cross-check:

```text
raw UpdatedAt mutation facts:        2
candidate UpdatedAt mutation facts:  0
runtime-pattern-variable:            retained
caller-object-unproven:              retained
transitive-mutation warning:         retained
false PO-facing UpdatedAt claims:    0
```

Detailed benchmark record:

`docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`

Interpret this as fail-closed stress evidence only. R7.14 positive real-project yield remains **NOT PASS**.

## Required review outcome

### If no new compile-valid/runtime-valid blocker exists

Record an independent PASS and then:

1. mark R7.10 PASS / COMPLETE;
2. mark all of V0.4.7-D PASS / COMPLETE;
3. update status, handoff, milestones and acceptance plan;
4. unlock **only V0.4.7-E**;
5. keep R7.14 required for E and still **NOT PASS**;
6. keep V0.5 locked.

Do not claim E complete merely because D passes.

### If a blocker exists

Do not advance E. Record:

- exact compile-valid/runtime-valid counterexample;
- current false or over-authoritative output;
- exact violated proof boundary;
- minimum generic repair direction;
- regression required before implementation.

Repair must be regression-first and must not opportunistically broaden Angular/C# semantics beyond the demonstrated blocker.
