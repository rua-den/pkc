# V0.4.7-D / R7.10 Independent Rereview #7 Request — 2026-09-22

## Review target

Independently review exact production SHA:

```text
5d43b180e09cc7026919a4dff2563f85e39e1b82
fix: fail closed on Angular hidden bindings
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Do not treat the docs HEAD as the implementation SHA and do not reset `main`.

## Context

R7.9 is already accepted. R7.10 is the remaining V0.4.7-D gate and may compose joint backend/frontend visibility only for the same exact R7.9-proven rendered-value path.

Successive independent rereviews have found and repaired these false-authority classes:

1. inert `<ng-template>`, HTML comments, HTML tag/attribute interpolation;
2. static HTML `hidden` ancestry;
3. static inline `display:none` ancestry;
4. static inline `visibility:hidden` ancestry;
5. legacy inert `<template>` with `enableLegacyTemplate=true`;
6. Angular native `hidden` bindings / dynamic hidden attributes.

Rereview #6 record:

`docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-6.md`

## What changed in `5d43b180...`

The render-authority boundary now handles native-hidden bindings conservatively:

```text
[hidden]="false" / bind-hidden="false" → supported positive safeguard
[hidden]="true" / bind-hidden="true"   → reject
[hidden]="expression"                 → fail closed
[hidden]="False"                      → fail closed
hidden="{{ ... }}"                    → fail closed
[attr.hidden] / bind-attr.hidden       → fail closed
```

Regression file:

`tests/Pkc.CSharp.Tests/AngularHiddenBindingRenderAuthorityRegressionTests.cs`

Do not merely re-run these known cases. Search independently for a different compile-valid/runtime-valid counterexample.

## Review scope

Challenge the bounded proof from frontend render authority through R7.10 composition. High-value seams include:

- whether an interpolation classified as authoritative is actually active/presented under the claimed static/template boundary;
- whether HTML ancestor/tag matching can associate suppression with the wrong node or miss a suppression shape already inside the claimed boundary;
- whether a supported `@if` is actually the controlling active template block for the exact render fact;
- comments, inert fragments, malformed/ambiguous nesting, or lexical structure that could make control-flow association false;
- whether unsupported presentation/control-flow syntax truly fails closed rather than leaking into authoritative output;
- exact render fact ID / visibility fact ID composition and authority downgrade behavior.

Do not require support for every Angular/CSS/runtime feature. A feature being unsupported is **not** a blocker if it fails closed and produces no false authoritative PO-facing rule.

A blocker requires a concrete compile-valid/runtime-valid shape that currently produces false or over-authoritative R7.9/R7.10 output.

## Permanent boundaries to preserve

Do not reopen accepted backend/lineage behavior without a real regression. Preserve:

- exact target-project-semantic `System.Linq.Enumerable.Single/First(predicate)` authority;
- exact selected-reference and projection identities;
- explicit API wire identity and exact frontend member/state binding;
- frontend visibility never upgrades `observed-only` backend authority;
- lower-authority deterministic evidence survives stronger composition failure;
- mutation causality remains separate and closed.

Do not weaken authority or modify benchmark projects merely to obtain positive yield.

## Current exact-SHA evidence

Production `5d43b180e09cc7026919a4dff2563f85e39e1b82`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35702383031 — PASS
pinned Loren                                35702383035 — PASS
Loren-main canary                           35702383053 — PASS
pinned Jellyfin + parity/provenance         35702383040 — PASS

Release build        0 warnings / 0 errors
C# tests             178 / 178 PASS
frontend tests       13 / 13 PASS
```

Pinned Jellyfin:

```text
43,365 facts
195,316 relations
386 workflow candidates
116 product features
504 knowledge Markdown files
43,365 / 43,365 project-semantic
portable parity/no-leak PASS
artifact 10683620529
```

Three-repository fail-closed benchmark:

```text
base production: 5d43b180e09cc7026919a4dff2563f85e39e1b82
wrapper:         b22c846ecebb75c6f52533c280ba4acbbe453657
run:             35702503746 — PASS, 3 / 3
```

Direct artifact inspection remains zero for current R7.9 rendered terminals, selected API projections, UI visibility, joint visibility, and combined visibility rules. This is stability evidence, not R7.14 positive yield.

## Required outcome

### If no new blocker exists

Record an independent PASS and then:

```text
mark R7.10 PASS / COMPLETE
→ mark all V0.4.7-D PASS / COMPLETE
→ unlock only V0.4.7-E
→ keep R7.14 NOT PASS and required for E
→ keep V0.5 locked until E completes
```

Do not start E inside the review itself unless the review outcome has first been recorded and D explicitly closed.

### If a blocker exists

Keep E locked. Record:

1. the exact compile-valid/runtime-valid counterexample;
2. the false emitted fact/knowledge output;
3. the violated proof boundary;
4. a focused regression that reproduces the defect;
5. the minimum generic repair scope.

Then repair regression-first, run focused/related/full verification, review the diff, make one coherent implementation commit/push, rerun exact-SHA standard gates and the pinned real-repository benchmark, and request another independent rereview.

## V0.5 gate

V0.5 remains locked. Even after D passes, V0.4.7-E must still complete and R7.14 must produce at least one natural supported positive on unchanged real software before V0.5 is ready.
