# V0.4.7-D R7.10 Independent Rereview #15 Request — 2026-09-23

## Purpose

Perform a fresh independent rereview, not an implementation continuation.

Review exact production SHA:

```text
47e098dbe910b7f6cfd933a0595370524bec1fb2
fix: correct resolved package root type
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Do not treat docs HEAD as production and do not reset `main`.

## Read order

Read, in order:

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. prior independent R7.10 review/rereview records through #14
7. `docs/reviews/2026-09-23-v0.4.7-d-r7.10-independent-rereview-14.md`
8. `docs/benchmarks/2026-09-23-r7.10-nested-dependency-selector-benchmark.md`
9. this request

Then inspect current `main`, recent commits, regressions and production code.

## Accepted context

Keep accepted A/B/C/R7.9 and the mutation-causality repair closed unless a new real regression is demonstrated.

Previous reviews/repairs closed concrete false-positive classes across inert/raw/template/SVG/browser-container authority, Angular control scope, local component projection, and now external component selectors plus nested dependency resolution. Do not merely replay those regressions.

Native SVG directly-visible render authority remains intentionally unsupported/fail-closed at this checkpoint.

## Why a fresh rereview is required

Independent rereview #14 reviewed:

```text
3e6fa7749eb8ef47be4eedb72d1c159cd502692f
fix: bound Angular direct text containers
```

It found a real compile-valid/runtime-valid blocker: external Angular dependency components can use selectors such as native element, attribute, class or combined selectors that are absent from product-source `@Component` declarations. PKC could therefore mistake a component host for ordinary HTML and over-promote a lexical child interpolation even when the receiving component does not project it.

Rereview #14 is a **FAIL**, not a superseded/pass result:

`docs/reviews/2026-09-23-v0.4.7-d-r7.10-independent-rereview-14.md`

## Repair chain

### External dependency component selector metadata

```text
8f667abc819f048b3dc85fc834677b7ca30f5518
fix: recognize external Angular component selectors
```

This repair:

- discovers external packages actually referenced by product TypeScript imports;
- reads bounded Angular Ivy `ɵɵComponentDeclaration` selector metadata from package `.d.ts` files;
- recognizes external component element/attribute/class/combined selectors;
- handles bounded escaped selector string values;
- does **not** treat `ɵɵDirectiveDeclaration` as a projection boundary;
- uses dependency metadata only to bound render authority;
- does not admit `node_modules` code into product facts or product knowledge.

### Nested dependency-tree resolution

Continuation challenge found `8f667abc...` only resolved packages at repository-root `node_modules`. A normal monorepo layout can instead be:

```text
repo/
  frontend/
    package.json
    src/price.component.ts
    node_modules/@vendor/ui/index.d.ts
```

Angular/Node can resolve the imported package there, but root-only lookup would miss its selector and reopen the false-authority defect.

Regression-first repair:

```text
67c67fc25b6488dbc9f110a1b05c53a4bfee1a6c
fix: resolve nested Angular dependency selectors
```

The resolver now:

- begins at each importing TypeScript file directory;
- walks ancestors toward repository root;
- selects the nearest existing `node_modules/<package>` root;
- preserves root-hoisted dependency behavior;
- reduces scoped/subpath imports to their owning package;
- scans only imported package roots rather than every `node_modules` tree;
- resolves package-root symlinks only when their final target remains inside the selected `node_modules` tree.

Focused regression:

`tests/Pkc.CSharp.Tests/AngularNestedNodeModulesProjectionAuthorityRegressionTests.cs`

The regression constructs a nested `frontend/` Angular app and a local dependency declaration with `ɵɵComponentDeclaration<..., "div[ext-shell]", ...>` and proves no authoritative render/visibility survives for an unprojected child interpolation.

### Final compile correction

The first clean-environment CI compile found `DirectoryInfo.ResolveLinkTarget()` returns `FileSystemInfo`, not `DirectoryInfo`. Final production:

```text
47e098dbe910b7f6cfd933a0595370524bec1fb2
fix: correct resolved package root type
```

The correction changes only that result variable type and does not alter resolution semantics.

## Exact repaired-candidate evidence

Production `47e098...` passed:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35818350915
pinned Loren                                35818350859
Loren-main canary                           35818350997
pinned Jellyfin + parity/provenance         35818350930
three-repository benchmark                  35818835753
```

Core: Release build 0 warnings/errors, C# 248/248, frontend 13/13, tool install, WorkPlay and PokeTrade PASS.

Pinned Jellyfin: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 Markdown files, 43,365/43,365 project-semantic, portable parity PASS. Artifact `10732606989`, digest `sha256:1d2b38aadc18828a75625ea94651f2a1acfc3a3813a3cc9df0c57d9be4b1bde2`.

The unchanged three-repository safety benchmark passes 3/3 and still emits zero supported current R7.9/R7.10 positives. This is stability/safety evidence, not R7.14 completion.

Agentic mutation-causality remains closed: the exact two `runtime-pattern-variable / caller-object-unproven` `UpdatedAt` mutation fact IDs occur zero times outside raw facts in feature candidates, product features and generated Markdown.

Local execution is not claimed in the continuation environment because it could not resolve `github.com` and has no local .NET toolchain. The evidence above is exact-SHA GitHub verification.

## Review objective

Independently search for another compile-valid/runtime-valid counterexample that causes false or over-authoritative R7.9/R7.10 output.

High-value seams now include:

- nearest versus root-hoisted `node_modules` resolution when multiple versions of the same package exist;
- scoped packages and package subpath imports;
- workspace/symlink/package-manager layouts, but only where the shape is compile-valid/runtime-valid and current bounded resolution can create false authority;
- external Angular component versus directive distinction;
- local/external selector collisions;
- element, attribute, class and combined selector matching, including escaped declaration string values;
- product-source isolation: dependency metadata must never become product facts/knowledge;
- HTML/SVG namespace transitions, especially `foreignObject`;
- exact control-block scope across component/projection boundaries;
- exact fact-ID composition and authority downgrade behavior;
- backend observed-only authority must never be upgraded by frontend evidence;
- mutation-causality closure must remain intact.

A blocker requires:

1. compile-valid/runtime-valid input for the relevant supported stack/configuration;
2. current production emits or can deterministically emit false/over-authoritative render, visibility, R7.9 terminal, R7.10 joint fact or PO-facing rule;
3. the violated proof boundary is named precisely.

An unsupported shape that fails closed is not a blocker.

## Decision rule

If no blocker exists:

```text
R7.10       PASS / COMPLETE
V0.4.7-D    PASS / COMPLETE
V0.4.7-E    UNLOCKED / NEXT
R7.14       NOT PASS / REQUIRED FOR E
V0.5        LOCKED until E completes
future W/U  LOCKED until V0.4.7 completes and roadmap is formally re-baselined
```

Update status/handoff/milestones/acceptance accordingly and unlock only E.

If a blocker exists, keep E locked; record the exact counterexample, false output, violated boundary, minimum generic regression-first repair and required regression. Do not modify production in the independent review unless explicitly converted to implementation work.

## After D passes

Finish V0.4.7-E, including R7.14 real-project positive yield: at least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Only after E and the V0.4.x PO-question-readiness exit gate pass may the prepared post-V0.4.7 roadmap re-baseline be applied.

Prepared future packet:

```text
docs/plans/2026-09-22-ai-workspace-continuous-update-plan.md
docs/reviews/2026-09-22-ai-workspace-continuous-update-plan-self-review.md
```

It recommends AI workspace + `run/verify` first, semantic `update/diff` second, and Azure DevOps evidence after those productization gates. This request does not unlock that work.
