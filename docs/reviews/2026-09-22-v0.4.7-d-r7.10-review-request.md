# V0.4.7-D / R7.10 Independent Rereview Request — 2026-09-22

## Review mode

This must be an **independent review**, not an implementation continuation.

Do not modify production code merely to make the accepted fixture pass. Search for compile-valid/runtime-valid counterexamples to the implemented proof boundary. If a blocker is found, report it with exact source shape and why current evidence would be wrong or over-authoritative.

## Exact implementation SHA

Review exactly:

```text
eb64309263489a2b9bd658762b4526a4a32a8508
fix: bound Angular visibility to active template structure
```

A docs-only `[skip ci]` commit may sit on `main` above this SHA. Do not review the docs SHA as production code and do not reset current main.

Compare the R7.10 implementation against the pre-R7.10 docs checkpoint if useful:

```text
base: c055e5a976a0702e9f29ef431397151c7b6597fb
head: eb64309263489a2b9bd658762b4526a4a32a8508
```

## Accepted predecessors — keep closed unless a real regression exists

```text
V0.4.7-C
fbb64b9917da1f63362558355201ff7998384ba0

V0.4.7-D / R7.9
fc4bfa7042f59620b2c7ba1c4f6700cf3b02172a

Mutation-causality blocker repair
67624944da27ff1f1f5a1154018a255aae11d1fe
```

The review should verify R7.10 did not weaken these accepted boundaries.

## R7.10 question

For the **same exact R7.9-proven value path**:

> What backend selection condition and frontend visibility condition jointly determine whether the rendered value is visible?

## Implemented bounded positive

Backend positive is intentionally narrow:

```csharp
var product = _products.Single(product => product.IsPublished);
return new PriceResponse
{
    DisplayPrice = product.Price
};
```

Equivalent `Enumerable.First(predicate)` is supported. Queryable is not.

R7.9 then proves the exact response field through explicit wire identity, typed frontend response member, exact subscribe assignment and authoritative interpolation.

Frontend R7.10 positive is intentionally narrow:

```html
@if (displayPrice > 0) {
  <strong>{{ displayPrice }}</strong>
}
```

Current visibility authority accepts one enclosing active line-anchored Angular `@if`. It does not claim general Angular compiler semantics.

## Exact composition identity to challenge

Review whether any path can create a false joint edge despite these intended keys:

```text
renderedTerminal.frontendRenderFactId
= visibility.renderFactId

renderedTerminal.backendProjectionFactId
→ projection.selectionPredicateFactId
→ predicate.selectedApiProjectionFactId
```

Textual property/type/predicate similarity must not substitute for these exact IDs.

## Authority requirements

The review must confirm:

1. backend predicate authority becomes `observable` only for the exact supported selected API item path;
2. frontend visibility never upgrades an `observed-only` backend predicate;
3. unrelated or ambiguous predicates remain disconnected;
4. multiple supported visibility facts fail closed rather than selecting one arbitrarily;
5. nested/multiple visibility remains fail closed inside the accepted grammar;
6. failure to compose keeps R7.9 and independently proven evidence;
7. inert template syntax cannot become visibility authority.

## Inert-template regressions already repaired

Implementation review after the initial R7.10 commit found that regex-only `@if` discovery could treat comment text as control flow. Two follow-up hardening commits are part of the exact review SHA ancestry:

```text
5cb52f1c34157a7004f26ceb7624ece9f889c2bf
fix: reject inert Angular visibility syntax

eb64309263489a2b9bd658762b4526a4a32a8508
fix: bound Angular visibility to active template structure
```

Current authority requires the `@if` token to begin its template line after whitespace and rejects positions inside HTML comments or tags.

Explicit regressions cover:

- `<!-- @if (...) { -->` on the same line;
- multiline HTML comments where `@if` begins its own line.

The reviewer should still search for another compile-valid/runtime-valid inert or structurally ambiguous shape.

## Focused regressions

```text
tests/Pkc.CSharp.Tests/JointVisibilityRegressionTests.cs
tests/Pkc.CSharp.Tests/JointVisibilityTemplateAuthorityRegressionTests.cs
```

They currently cover:

- positive exact selected backend item + exact rendered-member visibility;
- unrelated same-text predicate isolation;
- ambiguous frontend visibility fail closed + R7.9 retention;
- nested visibility fail closed + R7.9 retention;
- observed-only backend authority non-upgrade;
- same-line HTML-comment false positive;
- multiline HTML-comment false positive.

## Exact-SHA verification already PASS

On exact implementation SHA `eb64309263489a2b9bd658762b4526a4a32a8508`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35646162832 — PASS
pinned Loren                                35646163109 — PASS
Loren-main canary                           35646163085 — PASS
pinned Jellyfin + parity/provenance         35646163035 — PASS
```

Pinned real-repository matrix:

```text
base implementation: eb64309263489a2b9bd658762b4526a4a32a8508
wrapper commit:      0bd0e501cc2bfc1f04d7cee275f43e5f888b3d1c
run:                 35646837448 — PASS, 3 / 3 jobs
```

All three unchanged repositories produced PKC exit `0` and zero R7.9/R7.10 positive chains. Artifact inspection found no false joint visibility. The agentic CRM mutation-causality blocker remains closed.

Benchmark record:

`docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`

## Required review outcome

If no new compile-valid/runtime-valid counterexample exists:

1. mark R7.10 PASS / COMPLETE;
2. mark all of V0.4.7-D PASS / COMPLETE;
3. update status, handoff, milestones and acceptance plan;
4. unlock **only V0.4.7-E**;
5. keep V0.5 locked;
6. retain R7.14 as a required E completion gate — it is still NOT PASS.

If a blocker exists, do not advance E. Record the exact counterexample and required minimum generic fix.
