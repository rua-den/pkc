# V0.4.7-D R7.10 Independent Rereview #8 Request — 2026-09-22

## Purpose

Perform a fresh independent rereview, not an implementation continuation.

Review exact production SHA:

```text
d785807dfa053a5abd1e3b6500a2fc1bd729d38a
fix: ignore comment braces in Angular visibility scope
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Do not treat docs HEAD as production and do not reset `main`.

## Read order

Read, in order:

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. independent rereview records #1 through #7
7. `docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`
8. this request

Then inspect current `main`, recent commits, regressions and production code.

## Accepted context

Keep accepted A/B/C/R7.9 and the mutation-causality repair closed unless a new real regression is demonstrated.

Previous rereviews have repaired these distinct false-positive classes:

- inert `<ng-template>` and legacy `<template>` fragments;
- HTML-comment interpolation and tag/attribute interpolation;
- static HTML `hidden` ancestry;
- static `display:none` and `visibility:hidden` ancestry;
- Angular native hidden bindings and ambiguous dynamic hidden forms;
- HTML-comment braces corrupting bounded `@if` scope matching.

Do not merely replay those regressions.

## Exact repaired-candidate evidence

Production `d785807d...` passed:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35703893704
pinned Loren                                35703893679
Loren-main canary                           35703893712
pinned Jellyfin + parity/provenance         35703893678
three-repository benchmark                  35704017705
```

Core: Release build 0 warnings/errors, C# 180/180, frontend 13/13, tool install, WorkPlay and PokeTrade PASS.

Pinned Jellyfin: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 Markdown files, 43,365/43,365 project-semantic, portable parity PASS.

The three unchanged stress repositories still emit zero supported current R7.9/R7.10 positives. This is stability evidence, not R7.14 completion.

## Review objective

Independently search for another compile-valid/runtime-valid counterexample that causes false or over-authoritative R7.9/R7.10 output.

High-value seams include:

- exact Angular control-flow scope association after comment skipping;
- braces in other compile-valid lexical/template constructs;
- nested or adjacent control-flow ambiguity;
- active versus inert presentation containers;
- exact fact-ID composition and authority downgrade;
- malformed-looking but compiler-valid template structure.

An unsupported shape that fails closed is not a blocker. A blocker requires:

1. compile-valid/runtime-valid input for the relevant stack/configuration;
2. current production emits or can deterministically emit false/over-authoritative render, visibility, R7.9 terminal, R7.10 joint fact, or PO-facing rule;
3. the violated proof boundary is named precisely.

## Decision rule

If no blocker exists:

```text
R7.10       PASS / COMPLETE
V0.4.7-D    PASS / COMPLETE
V0.4.7-E    UNLOCKED / NEXT
R7.14       NOT PASS / REQUIRED FOR E
V0.5        LOCKED until E completes
```

Update status/handoff/milestones/acceptance accordingly and unlock only E.

If a blocker exists, keep E locked; record the exact counterexample, false output, violated boundary, and minimum generic regression-first repair. Do not modify production in the independent review unless explicitly converted to implementation work.

## After D passes

Finish V0.4.7-E, including R7.14 real-project positive yield: at least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Only after E and the V0.4.x PO-question readiness exit gate pass may V0.5 Azure DevOps input evidence be unlocked.
