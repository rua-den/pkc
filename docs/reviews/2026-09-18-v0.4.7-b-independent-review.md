# V0.4.7-B independent review

## Target and verdict

Reviewed current `origin/main` at `53a9463985d13dfc985b238426b46bf8d317b71c`, whose B implementation is `e5b0d47c82b6db99f5184292730919421a6d2a06`.

**The reviewed main checkpoint has one P1 stale-terminal-source blocker.** A bounded repair is committed locally at:

```text
30e87df
fix: invalidate terminal source on nested writes

76d9bb1
test: cover point-in-time derivation
```

Keep B open and C locked until this repair is integrated and its exact-SHA remote gates pass. V0.4.6 and V0.4.7-A remain closed.

## P1 — unsupported simple writes can leave stale terminal authority

The B analyzer tracked supported assignments only when the whole statement was a direct assignment expression. Its preflight rejected compound and unary writes, but skipped simple assignments nested inside another statement or contained in a complex top-level left-hand side.

Two compile-valid executable counterexamples demonstrated the same root cause:

```csharp
service.NetPrice = service.Price - service.Discount; // stored derivation: 90
var observed = (service.NetPrice = 5m);              // nested simple write
return service.NetPrice;                             // runtime: 5
```

and:

```csharp
service.NetPrice = service.Price - service.Discount; // stored derivation: 90
decimal observed;
(service.NetPrice, observed) = (5m, 0m);             // deconstruction write
return service.NetPrice;                             // runtime: 5
```

Before repair, both fixtures compiled and returned `5`, while PKC emitted `value-terminal-source` pointing to the earlier derivation. The derivation fact itself remains correct historical evidence; only the claim that it is the last source before return is false.

## Repair boundary

For each statement, inspect simple assignments which are not a directly supported property assignment. Resolve property write targets from their left-hand side and invalidate only the matching current-value slots. This covers nested assignments and complex/deconstruction targets without deleting earlier derivation evidence or adding a general expression solver.

Permanent regressions require all three conditions:

1. exact fixture compilation and runtime result `5`;
2. the earlier `service.Price - service.Discount → service.NetPrice` derivation remains retained;
3. no authoritative terminal-source fact is emitted after the unsupported write.

## Verification

The two regressions were observed RED against unmodified main, failing because the stale terminal fact was present after the runtime assertions passed.

After Luna's bounded repair, Sol and the root reviewer independently verified:

```text
B regression class:       12 / 12 PASS
all ValueLineage tests:   29 / 29 PASS
Release build:            0 warnings / 0 errors
full C# tests:             117 / 117 PASS
frontend tests:            13 / 13 PASS
git diff --check:          PASS
```

The final Luna-authored coverage also executes the required point-in-time behavior: copy-backed `service.Price=100`, stored `NetPrice=90`, later `service.Price=120`, and terminal `NetPrice=90`. It requires the later override to remain separate causality while Markdown calls the derivation a snapshot and never a dynamic dependency.

The full build initially stopped because the isolated worktree lacked the frontend `project.assets.json`. A dependency restore completed successfully; the fresh Release build and full tests above then passed. This was an environment setup issue, not a code failure.

The original exact-main external gates apply to `e5b0d47...`, not the new repair. They must not be represented as verification of the repaired branch through `76d9bb1`. No remote benchmark or portable parity gate has been run for these repair commits yet.

## Branch disposition

The clean repair branch is `codex/v047-b-origin-audit`, based on current `origin/main`.

An older dirty branch, `codex/v047-b-provenance`, contains a duplicate implementation coupled into the A analyzer. It is not a valid delivery candidate: its focused build fails deterministically with `CS0128` (`byId` declared twice in `EvidenceAwareKnowledgeSynthesizer.cs`). Preserve it only as local history; continue from the clean origin-based repair.
