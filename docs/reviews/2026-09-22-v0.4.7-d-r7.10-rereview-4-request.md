# V0.4.7-D / R7.10 Independent Rereview #4 Request — 2026-09-22

## Review mode

This is an **independent review**, not an implementation continuation.

Do not modify production code merely to make accepted fixtures pass. Search for a new compile-valid/runtime-valid counterexample to the current proof boundary. Green CI and benchmarks prove regression stability, not semantic correctness.

Do not start E during this review.

## Exact production SHA to review

```text
dc69e44206942ffb0012e1994d6d39249d1db4be
fix: reject static display none renders
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Review production behavior at `dc69e442...`; do not reset current `main`.

## Read first

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. `docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-1.md`
7. `docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-2.md`
8. `docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-3.md`
9. `docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`

Then inspect current `main`, recent commits, regressions and production code independently.

## Accepted predecessors — keep closed unless a real regression exists

```text
V0.4.7-C
fbb64b9917da1f63362558355201ff7998384ba0

V0.4.7-D / R7.9 baseline
fc4bfa7042f59620b2c7ba1c4f6700cf3b02172a

Mutation-causality repair
67624944da27ff1f1f5a1154018a255aae11d1fe
```

R7.9 remains an accepted predecessor, but R7.10 rereviews have demonstrated that its rendered-value authority boundary needs conservative exclusions before it can safely feed R7.10. Confirm the latest repair does not weaken exact binding identity or accepted predecessor behavior.

## R7.10 product question

For the **same exact R7.9-proven value path**:

> What backend selection condition and frontend visibility condition jointly determine whether the rendered value is visible?

## Current bounded backend positive

```csharp
var product = _products.Single(product => product.IsPublished);
return new PriceResponse { DisplayPrice = product.Price };
```

Requirements remain intentionally narrow: target-project Roslyn semantics; exact `System.Linq.Enumerable.Single/First`; exact selected reference local; supported scalar auto-properties; direct final response object initializer; no user-defined conversion; exact selection invocation span; exact projection fact identity.

## Current R7.9 frontend rendered-value boundary

R7.9 must prove:

```text
C-proven API response property
→ explicit wire identity
→ typed frontend HTTP result member
→ exact service/result assignment
→ exact component member
→ authoritative active visible Angular text interpolation
→ rendered UI value
```

Previous rereviews already repaired false authority for:

```text
inert <ng-template>
HTML-comment interpolation
HTML tag/attribute interpolation
static HTML hidden ancestry
static inline display:none ancestry
```

Do **not** merely re-check those exact fixtures. Search for another semantic shape crossing the intended visible-render boundary.

## Latest rereview #3 blocker and repair

Rereview #3 failed previous production SHA `97161baa2d0aff9131a7acf9db752393ae913d64` on:

```html
<section style="display: none">
  @if (displayPrice > 0) {
    <strong>{{ displayPrice }}</strong>
  }
</section>
```

Regression-first repair:

```text
dc69e44206942ffb0012e1994d6d39249d1db4be
fix: reject static display none renders
```

The current bounded repair recognizes static inline `display:none` and `display:none !important`, case-insensitively, on active ancestors. Static `display:block` remains supported. Dynamic/interpolated style values are deliberately not interpreted as static proof.

This is not a general CSS engine. Do not treat missing support for arbitrary runtime CSS as a false positive unless current PKC actually promotes a compile-valid/runtime-valid non-visible shape into authoritative rendered-value knowledge.

## High-value challenge areas

Search independently, especially around:

1. other static HTML/template constructs that make text non-presented while current parser still authorizes it;
2. malformed-but-compile-valid or browser-normalized nesting that can defeat the bounded ancestor stack;
3. void/raw-text/foreign-content HTML elements and parser behavior;
4. multiple interpolation occurrences on the same source line and exact render identity;
5. quoted attribute/style syntax and escaped delimiters that could confuse the bounded tag regex;
6. static presentation semantics that current code claims to understand but parses incorrectly;
7. same-name/same-text collisions around exact R7.9 binding identity;
8. backend overload/local/symbol collisions around selected API projection authority;
9. frontend evidence upgrading observed-only backend evidence;
10. lower-authority evidence being dropped when R7.10 composition fails.

Do not demand general Angular compiler, browser CSS cascade, signals, outlets, structural directives or arbitrary runtime DOM semantics unless current implementation falsely claims authority for a concrete case.

## Focused regressions and seams to inspect

```text
tests/Pkc.CSharp.Tests/JointVisibilityRegressionTests.cs
tests/Pkc.CSharp.Tests/JointVisibilityTemplateAuthorityRegressionTests.cs
tests/Pkc.CSharp.Tests/StaticCssRenderAuthorityRegressionTests.cs

src/Pkc.CSharp/CSharpSelectedApiProjectionLineageEnricher.cs
src/Pkc.CSharp/CSharpSelectedApiPredicateAuthorityEnricher.cs
src/Pkc.Frontend/AngularRenderedMemberAuthorityFilter.cs
src/Pkc.Frontend/AngularRenderedMemberVisibilityEnricher.cs
src/Pkc.Frontend/AngularRenderedMemberVisibilityAuthorityFilter.cs
src/Pkc.Knowledge/ApiFrontendBindingCandidateEnricher.cs
src/Pkc.Knowledge/JointVisibilityCandidateEnricher.cs
src/Pkc.Knowledge/JointVisibilityKnowledgeSynthesizer.cs
```

## Exact-SHA verification already PASS

On exact production SHA `dc69e44206942ffb0012e1994d6d39249d1db4be`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35687831689 — PASS
pinned Loren                                35687831632 — PASS
Loren-main canary                           35687831587 — PASS
pinned Jellyfin + parity/provenance         35687831537 — PASS

Release build        0 warnings / 0 errors
C# tests             164 / 164 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

Pinned Jellyfin remains all project-semantic and portable parity/no-leak passes. Artifact `10677695966`, digest `sha256:088353b7a1e30bf2aa68dce408246c2f88de50b843e88a7c57bb8fac595bdb60`.

## Repaired pinned real-repository benchmark

```text
base implementation: dc69e44206942ffb0012e1994d6d39249d1db4be
wrapper commit:      b3b0c5b40c0d34cafe3e7a60c28341ea777dd2cf
run:                 35687951314 — PASS, 3 / 3 jobs
```

All three unchanged repositories currently emit zero supported current R7.9/R7.10 cross-layer positives. Interpret that as fail-closed stress evidence only. R7.14 positive real-project yield remains **NOT PASS**.

The agentic mutation-causality cross-check remains closed: two raw `UpdatedAt` mutations, zero candidate promotion, runtime-pattern/caller-object uncertainty retained.

## Required review outcome

### If no new compile-valid/runtime-valid blocker exists

Record an independent PASS and then:

1. mark R7.10 PASS / COMPLETE;
2. mark all V0.4.7-D PASS / COMPLETE;
3. update status, handoff, milestones and acceptance plan;
4. unlock **only V0.4.7-E**;
5. keep R7.14 required and still NOT PASS;
6. keep V0.5 locked.

### If a blocker exists

Do not advance E. Record:

- exact compile-valid/runtime-valid counterexample;
- current false/over-authoritative output;
- exact violated proof boundary;
- minimum generic repair direction;
- regression required before implementation.

Repair must be regression-first and must not opportunistically broaden Angular/C#/CSS semantics beyond the demonstrated blocker.
