# V0.4.7-D R7.10 Independent Rereview #16 Request — 2026-09-23

## Purpose

Perform a fresh independent rereview. This is not an implementation continuation.

Review exact production SHA:

```text
37a71172c8c425219aef35f5843ac2109810ae9d
fix: preserve Angular infrastructure render evidence
```

A docs-only `[skip ci]` commit may sit above production on `main`. Do not review the docs HEAD as production and do not reset `main`.

## Read order

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. prior independent R7.10 reviews through #14
7. `docs/benchmarks/2026-09-23-real-repo-product-value-scorecard.md`
8. this request

Then inspect current `main`, recent production commits, regressions and production code.

## Accepted context

Keep accepted A/B/C/R7.9 and mutation-causality closed unless a new real regression is demonstrated.

Rereview #14 was a real FAIL on external component projection authority. Subsequent implementation repaired external dependency selector discovery, nested dependency roots, linked/workspace packages, unresolved external component imports and framework/const-array handling.

Do not merely replay a closed regression.

## Production repair chain to challenge

```text
8f667abc819f048b3dc85fc834677b7ca30f5518  recognize external Angular component selectors
67c67fc25b6488dbc9f110a1b05c53a4bfee1a6c  resolve nested Angular dependency selectors
47e098dbe910b7f6cfd933a0595370524bec1fb2  correct resolved package root type
46dcf8941c67a11d96e4416774d02e555a8f6aaf  bound linked Angular package selectors
2ea361ba9d5e6e61041f38ca79e6587e12a1b745  fail closed unresolved Angular component imports
37a71172c8c425219aef35f5843ac2109810ae9d  preserve Angular infrastructure render evidence
```

## Exact candidate evidence

```text
CI + full PKC tests + WorkPlay + PokeTrade   35823346084 — PASS
pinned Loren                                35823346060 — PASS
Loren-main canary                           35823346077 — PASS
pinned Jellyfin + parity/provenance         35823346046 — PASS
real-repository benchmark                   35823872896 — PASS, 3 / 3
```

Core: Release 0 warnings/errors, C# 253/253, frontend 13/13, tool install, WorkPlay and PokeTrade PASS.

Pinned Jellyfin: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 Markdown files, 43,365/43,365 project-semantic, portable parity/no-leak PASS.

Benchmark wrapper `f76c946...` has exact parent `37a711...` and changes only the branch trigger. Canonical output for the three pinned real repos is byte-stable against the preceding safety benchmark.

## Review objective

Independently find a compile-valid/runtime-valid counterexample that current production can over-promote into false R7.9/R7.10 authority.

High-value seams:

- workspace/link/package-manager layouts not already covered;
- unresolved package imports with namespace/default/alias/const-array component imports;
- packages that are resolvable but do not contain Angular component declarations;
- component vs directive distinction;
- local/external selector collision;
- selector matching and static/bound attributes;
- HTML/SVG `foreignObject` namespace transitions;
- exact enclosing control scope;
- exact fact-ID composition;
- observed-only backend authority;
- mutation-causality closure;
- lower-authority evidence preservation after fail-closed filtering.

A blocker requires:

1. compile-valid/runtime-valid input in the supported stack;
2. deterministic false or over-authoritative R7.9/R7.10 output;
3. a precise violated proof boundary.

A supported shape that merely fails closed is not a blocker.

## Product-value benchmark is separate

`docs/benchmarks/2026-09-23-real-repo-product-value-scorecard.md` records product usefulness gaps such as interface-to-implementation traversal, construction/default state, side-effect synthesis and feature-summary fidelity.

Those gaps are important E acceptance evidence, but they do **not** by themselves fail D/R7.10 unless they create false cross-layer authority.

Do not convert incomplete usefulness into a reason to weaken render/visibility authority.

## Decision rule

If no new R7.10 blocker exists:

```text
R7.10       PASS / COMPLETE
V0.4.7-D    PASS / COMPLETE
V0.4.7-E    UNLOCKED / NEXT
R7.14       NOT PASS / REQUIRED FOR E
V0.5        LOCKED
future W/U  LOCKED until V0.4.7 completes
```

Update status/handoff/milestones/acceptance accordingly.

If a blocker exists, keep E locked and record the exact counterexample, false output, violated boundary, minimum generic repair and regression.

The independent reviewer should not modify production unless explicitly converting the work into an implementation continuation.

## After D passes

E must use the known-answer product-value scorecard as an acceptance gate, not just rerun green workflows. R7.14 still requires at least one unchanged real repository to emit a supported positive cross-layer answer.
