# V0.4.7-D R7.10 Independent Rereview #10 Request — 2026-09-22

## Purpose

Perform a fresh independent rereview, not an implementation continuation.

Review exact production SHA:

```text
1bc67e62f1099e4d580072682566c2d305e4db07
fix: ignore Angular style raw text
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Do not treat docs HEAD as production and do not reset `main`.

## Read order

Read, in order:

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. independent R7.10 rereview records #1 through #9
7. `docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`
8. this request

Then inspect current `main`, recent commits, regressions and production code.

## Accepted context

Keep accepted A/B/C/R7.9 and the mutation-causality repair closed unless a new real regression is demonstrated.

Previous rereviews have repaired concrete false-positive classes including:

- inert `<ng-template>` / legacy `<template>` and HTML-comment interpolation;
- attribute/tag interpolation boundaries;
- static `hidden`, `display:none`, `visibility:hidden` ancestry;
- Angular native hidden bindings and ambiguous dynamic hidden forms;
- HTML-comment braces corrupting `@if` scope;
- `ngNonBindable` literal interpolation;
- `>` and control-looking text inside quoted attributes;
- unsupported nested Angular control blocks;
- plain HTML text quotes confusing brace scope;
- legacy/custom `*structuralDirective` ancestors;
- `<style>` raw-text interpolation, CSS braces and fake control syntax.

Do not merely replay those regressions.

## Rereview #9 repair

Rereview #9 found that `<style>` is a valid Angular template element but its text is not Angular interpolation authority. The predecessor could both promote `{{ member }}` inside CSS and count CSS braces/control-looking strings while resolving `@if` scope.

Exact repaired production:

```text
1bc67e62f1099e4d580072682566c2d305e4db07
```

The repair:

- rejects render authority for member interpolation under `<style>`;
- ignores Angular-control regex matches inside `<style>`;
- skips complete style raw-text regions during brace matching;
- fails closed on an unterminated style region;
- retains a positive path for a genuine supported `@if` after a closed style element.

Regression coverage:

`tests/Pkc.CSharp.Tests/AngularStyleElementAuthorityRegressionTests.cs`

## Exact repaired-candidate evidence

Production `1bc67e62...` passed:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35717515912
pinned Loren                                35717515881
Loren-main canary                           35717515909
pinned Jellyfin + parity/provenance         35717515901
three-repository benchmark                  35717582575
```

Core: Release build 0 warnings/errors, C# 198/198, frontend 13/13, tool install, WorkPlay and PokeTrade PASS.

Pinned Jellyfin: 43,365 facts, 195,316 relations, 386 workflows, 116 product features, 504 Markdown files, 43,365/43,365 project-semantic, portable parity PASS. Artifact `10690353622`, digest `sha256:5b82eb6b43aa4dedd223b6a0956367e2fe2b15bd03a887087143bee7c0ee112e`.

The three unchanged stress repositories still emit zero supported current R7.9/R7.10 positives. This is stability/safety evidence, not R7.14 completion.

## Review objective

Independently search for another compile-valid/runtime-valid counterexample that causes false or over-authoritative R7.9/R7.10 output.

High-value seams include:

- other Angular template raw-text or special-text contexts whose runtime binding semantics differ from normal visible text;
- exact control-block scope association across raw-text/template lexical boundaries;
- malformed-looking but Angular-compiler-valid HTML/template structure that defeats bounded ancestry or scope parsing;
- same-line identity collisions only when they create a real false authority or deterministic crash;
- exact fact-ID composition and authority downgrade behavior;
- unsupported nested/control/structural visibility paths that might still be partially promoted;
- backend/frontend identity collisions only where exact current evidence can wrongly join them.

Do not demand a complete CSS/browser/Angular parser merely because such systems are complex. A blocker requires a concrete input that the relevant stack actually accepts and current PKC over-promotes.

An unsupported shape that fails closed is not a blocker. A blocker requires:

1. compile-valid/runtime-valid input for the relevant stack/configuration;
2. current production emits or can deterministically emit false/over-authoritative render, visibility, R7.9 terminal, R7.10 joint fact, or PO-facing rule;
3. the violated proof boundary is named precisely.

## Decision rule

If no blocker exists:

```text
R7.10       PASS / COMPLETE
V0.4.7-D    PASS / COMPLETE
V0.4.7-E    UNLOCKED / NEXT
R7.14       NOT PASS / REQUIRED FOR E
V0.5        LOCKED until E completes
```

Update status/handoff/milestones/acceptance accordingly and unlock only E.

If a blocker exists, keep E locked; record the exact counterexample, false output, violated boundary, minimum generic regression-first repair and required regression. Do not modify production in the independent review unless explicitly converted to implementation work.

## After D passes

Finish V0.4.7-E, including R7.14 real-project positive yield: at least one unchanged real repository must naturally emit a supported positive V0.4.7 cross-layer answer. Only after E and the V0.4.x PO-question-readiness exit gate pass may V0.5 Azure DevOps input evidence be unlocked.
