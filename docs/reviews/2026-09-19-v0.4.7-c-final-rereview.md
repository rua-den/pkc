# V0.4.7-C final independent rereview

Date: 2026-09-19

## Target

Final rereview covered only V0.4.7-C backend entity/domain → DTO/projection → API-response lineage on exact accepted production code:

```text
fbb64b9917da1f63362558355201ff7998384ba0
feat: prove backend API projection lineage
```

V0.4.7-A/B and V0.4.6 were treated as closed baselines. D/E/V0.5 were not used to justify C acceptance.

## Accepted proof boundary

C proves a deliberately bounded target-project-semantic shape:

```text
fresh backend entity/domain local
→ supported scalar auto-property read
→ explicit semantic object-initializer assignment
→ supported scalar response/DTO auto-property
→ direct final API response
```

The focused positive proves a renamed mapping equivalent to:

```text
ProductEntity.Price
→ PriceResponse.DisplayPrice
→ API response
```

The edge is derived from the target project's Roslyn semantic operation/symbol identity, not member-name similarity.

The emitted projection evidence records:

- semantic project identity;
- source assembly/type/member identity;
- source receiver/occurrence;
- source member declaration location;
- target assembly/type/member identity;
- target member declaration location;
- projection assignment location;
- response type/boundary and return location;
- project-semantic analysis mode, confidence and proof marker.

The projection is retained by the endpoint candidate, synthesized into `ValueLineage`, and rendered in PO-facing workflow Markdown with source traceability. A separate terminal fact records the last proven projected field at the API-response boundary.

## Regression and blocker history

The review sequence found and repaired a compile-hygiene issue plus regression-fixture weaknesses before acceptance:

1. a C# pattern-variable scope collision in `UnwrapSafeConversions` caused Release compilation failure;
2. the first assembly-collision fixture accidentally created an ambiguous duplicate semantic type and correctly failed closed;
3. a referenced-project source file was initially included by the main SDK default glob, so the asserted assembly identity was not actually external;
4. the final rereview strengthened the collision proof to the same full type/member name in two different assemblies using a compile-valid `extern alias` fixture.

The accepted production algorithm was not widened merely to make those fixture mistakes pass. The final fixture proves that assembly identity, not namespace/name similarity, distinguishes the source symbol.

## Permanent executable coverage

C now has focused regressions for:

- explicit renamed entity/domain → DTO mapping → API response;
- candidate retention, synthesis and PO-facing Markdown traceability;
- semantic project and assembly identity;
- unrelated same-name entity/DTO members without mapping;
- same-name namespace collisions;
- same full type/member name across different assemblies through `extern alias`;
- custom/non-auto source accessor;
- custom/non-auto target accessor;
- user-defined conversion;
- reference alias ambiguity;
- opaque invocation/effect;
- custom response construction;
- unsupported statement/control-flow/path shapes through the generic preflight;
- missing target-project semantic context.

PokeTrade was not modified merely to manufacture an explicit DTO shape. Focused fixtures cover the required rename/collision shapes while PokeTrade remains a known-answer real-project gate.

## Boundary rereview

No remaining compile-valid/runtime-valid contradiction was found inside the documented supported shape.

The accepted scope is intentionally narrower than general backend projection analysis. C does **not** claim:

```text
general alias equivalence
general effect analysis
general symbolic/path execution
arbitrary helper-method projection dataflow
arbitrary custom property semantics
arbitrary response-wrapper / MVC result unwrapping
arbitrary constructor mapping
name-based DTO matching
```

Unsupported/ambiguous shapes fail closed and do not erase independently proven lower-authority A/B lineage or causality evidence.

## Branch verification before production

Final candidate branch head was exactly the production commit:

```text
fbb64b9917da1f63362558355201ff7998384ba0
```

Branch gates passed before fast-forwarding `main`:

```text
core CI / PokeTrade      35415438138 — PASS
pinned Loren             35415438189 — PASS
pinned Jellyfin          35415438151 — PASS
```

The final branch core gate included the strengthened full-name cross-assembly regression and passed 136 C# tests plus 13 frontend tests.

## Exact-main verification

All required exact-main gates passed on `fbb64b9917da1f63362558355201ff7998384ba0`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35416169067 — PASS
pinned Loren                                35416169091 — PASS
Loren-main canary                           35416169051 — PASS
pinned Jellyfin                             35416169039 — PASS
```

Core CI:

```text
Release build:       0 warnings / 0 errors
C# tests:            136 / 136 PASS
frontend tests:      13 / 13 PASS
tool pack/install:   PASS
WorkPlay:            PASS
PokeTrade:           PASS
```

Pinned Jellyfin:

```text
source commit:           1d7b6d97844c8cc848ed3fb5c4b48bb9cdd5b139
source build:            0 warnings / 0 errors
facts:                   43,365
relations:               195,316
workflow candidates:     386
product features:        116
canonical Markdown:      504
project-semantic facts:  43,365 / 43,365
portable bundle parity:  PASS
ZIP file-set parity:     PASS
ZIP byte parity:         PASS
raw .pkc leak:           none
src/ source-tree leak:   none
artifact id:             10575663250
artifact digest:         sha256:857d33020332f4a68a69177b6809136ebe24b9ea86b5576efc8dbc69d8266347
artifact size:           9,162,486 bytes
```

## Verdict

```text
PASS V0.4.7-C
C PASS / COMPLETE
```

No remaining compile-valid/runtime-valid counterexample is known inside the documented C proof boundary.

## Unlock disposition

Only checkpoint D is unlocked:

```text
V0.4.7-A          PASS / COMPLETE
V0.4.7-B          PASS / COMPLETE
V0.4.7-C          PASS / COMPLETE
V0.4.7-D          CURRENT / UNLOCKED
V0.4.7-E          LOCKED behind D
V0.5 Azure DevOps LOCKED
```

D must begin regression-first for exact API response/result → frontend binding/component state/rendered value identity, with backend and frontend source locations and fail-closed collision/ambiguity coverage. Joint backend/frontend visibility composition starts only after the same item/dataflow path is proven.
