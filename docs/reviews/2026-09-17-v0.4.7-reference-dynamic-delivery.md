# V0.4.7-A implementation delivery

## Scope and starting point

Starting checkpoint: `4d986539bb5cbc37d56010cd3a4731f1e0b9ff26` on branch `codex/v047-reference-dynamic`.

User-approved sequence:

1. Close the reproduced alias-write composition gap, retaining independently valid direct copies and the canonical snapshot positive.
2. Implement a bounded reference/dynamic read positive: read downstream `100`, change upstream to `120`, then read downstream `120` without another scalar copy.
3. Prove the result through semantic scanning, candidate retention, synthesis and workflow Markdown. Dynamic/reference wording must be explicit and separate from snapshot chains.
4. Review and verify before any main-branch checkpoint. Keep V0.4.6 closed; B/C/D/E and V0.5 remain locked pending A acceptance.

## Implementation state

The branch contains the bounded alias repair from the prior coding session and now adds the dynamic/reference implementation candidate.

### Alias composition repair

The repair treats unresolved reference aliasing as a persistent composition boundary rather than trying to solve arbitrary object identity. Direct scalar-copy facts remain available; only unsupported predecessor composition is blocked.

Permanent coverage includes:

- direct alias write through another local;
- alias hidden through cast / `as` shapes;
- user-defined conversion wrapping an object allocation, which must not be classified as a fresh independent reference;
- receiver reassignment and opaque-helper mutation from the previous stale-composition review.

The user-defined-conversion regression executes the fixture and requires runtime `group=100`, `product=50`, `service=50`, both direct transfers retained, and no predecessor/false chain on the later service copy.

### Bounded dynamic/reference proof

The supported shape is deliberately narrow:

```text
fresh upstream local
→ fresh service local constructed with that upstream local
→ inert single-assignment constructor
→ private readonly upstream field
→ read-only expression-bodied scalar getter
→ endpoint reads getter
→ endpoint writes exact upstream scalar auto-property
→ endpoint reads the same getter again
```

The dynamic fact is only emitted in target-project semantic context when exact symbols prove the constructor binding, readonly field, getter dependency, endpoint receiver identities and read/write/read order.

The generated value-lineage explanation says the target is a **dynamic read-time dependency**, not a stored snapshot. Dynamic facts are excluded from snapshot-chain composition.

Fail-closed coverage includes:

- mutable binding field;
- arbitrary/custom getter body;
- extra reference alias local;
- reference receiver reassignment;
- opaque invocation/effect;
- branch ambiguity;
- missing project-semantic context.

The executable positive requires `before=100`, upstream mutation to `120`, then `after=120` without another scalar-copy assignment.

## Verification state

Local network access from the active runtime cannot resolve GitHub, so the repository cannot be cloned into the execution container for a trustworthy local `dotnet test`. The code/test candidate is therefore reviewed statically first and must be validated through branch/PR CI before it can be considered a checkpoint.

Do not merge or fast-forward `main` until:

```text
Release build                         PASS
focused/full C# tests                 PASS
frontend tests                       PASS
WorkPlay / PokeTrade                  PASS
```

After main receives a coherent verified A checkpoint, run the existing pinned Loren, Loren-main and pinned Jellyfin exact-SHA gates and portable parity/no-leak checks before calling A complete.

## Scope guard

No general alias/path solver is introduced. No package/schema bump is implied by roadmap progress. V0.4.7-B/C/D/E and V0.5 remain locked until A's snapshot and dynamic/reference positives plus portable/identity gates are green.
