# V0.4.7-A implementation delivery

## Scope and starting point

Starting checkpoint: `4d986539bb5cbc37d56010cd3a4731f1e0b9ff26` on local branch `codex/v047-reference-dynamic`.

User-approved sequence:

1. Close the reproduced alias-write composition gap, retaining both independently valid direct copies and the canonical snapshot positive.
2. Implement a bounded reference/dynamic read positive: read downstream `100`, change upstream to `120`, then read downstream `120` without another scalar copy.
3. Prove the result through semantic scanning, candidate retention, synthesis and workflow Markdown. Dynamic/reference wording must be explicit and separate from snapshot chains.
4. Review and verify before saving implementation checkpoints. Keep V0.4.6 closed; B/C/D/E and V0.5 remain locked pending A's remaining acceptance gates.

Luna writes implementation and regressions. Sol coordinates, reviews the diff and independently runs verification. The root reviewer checks scope/output and maintains the handoff artifacts. No remote publication is part of this delivery.

## Execution ledger

- Alias repair: regression RED reproduced (six existing cases pass, alias case fails at stale predecessor metadata after runtime assertions). An initial seven-test GREEN did not pass review because its syntax-specific alias detection missed equivalent reference bindings. The replacement uses conservative reference-binding eligibility, retains both direct transfers and qualifies uncertain source stability in Markdown. Sol independently verified seven lineage tests passing; root review requested rejecting user-defined conversion operators before classifying an allocation as fresh. Final checkpoint verification is pending that adjustment.
- Dynamic/reference: pending alias repair review.
- Final local verification and acceptance disposition: pending.

Ruling: keep this implementation on a dedicated branch in the existing checkout, preserving the reviewed `main` checkpoint and its cached dependencies. There is one implementation writer at a time.

Ruling: unsupported alias/effect paths may block composition or dynamic authority; a general alias/path solver is outside this slice. Proven immediate snapshots must remain observable.

## Bounded dynamic shape

The modeled service stores one upstream reference in a private readonly field, via an inert constructor containing only that field assignment. Its read-only expression-bodied scalar getter returns the bound object's scalar auto-property. A straight-line endpoint constructs the service with a specific upstream local and reads that same service before and after changing that upstream property.

Proof follows exact semantic identities from constructor argument to readonly field to getter read and upstream member. Reject unmodeled constructor/base/initializer effects, mutable binding, virtual/indexer access, arbitrary getter code, receiver ambiguity, opaque effects and unresolved semantics. Dynamic facts must not participate in snapshot chain construction. The claim is scoped to that same bound object and modeled read path.
