# V0.4.7-D / R7.10 Independent Rereview #2 Request — Astra — 2026-09-22

## Review mode

This is an **independent review**, not an implementation continuation.

Do not modify production code merely to make the accepted fixtures pass. Search for compile-valid/runtime-valid counterexamples to the current proof boundary. If a blocker exists, report the exact source shape and why current PKC evidence would be false or over-authoritative.

Do not start E during this review.

## Exact production SHA to review

Review exactly:

```text
da5d23771ef8c9d58d0333d1f949e8d742210043
fix: require visible text interpolation
```

A docs-only `[skip ci]` commit may sit above this SHA on `main`. Review production behavior at `da5d2377...`; do not reset current `main`.

Useful base for net repair comparison:

```text
original R7.10 candidate:
eb64309263489a2b9bd658762b4526a4a32a8508

repaired candidate:
da5d23771ef8c9d58d0333d1f949e8d742210043
```

## Read first

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/v0.4.7-acceptance-plan.md`
6. `docs/reviews/2026-09-22-v0.4.7-d-r7.10-independent-rereview-1.md`
7. `docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`

Then inspect current `main`, recent commits and the exact implementation diff. Do not infer acceptance from documentation claims; verify source and regressions independently.

## Accepted predecessors — keep closed unless a real regression exists

```text
V0.4.7-C
fbb64b9917da1f63362558355201ff7998384ba0

V0.4.7-D / R7.9
fc4bfa7042f59620b2c7ba1c4f6700cf3b02172a

Mutation-causality repair
67624944da27ff1f1f5a1154018a255aae11d1fe
```

The review must confirm the R7.10 repairs did not weaken these accepted boundaries.

## R7.10 product question

For the **same exact R7.9-proven value path**:

> What backend selection condition and frontend visibility condition jointly determine whether the rendered value is visible?

## Current bounded backend positive

```csharp
var product = _products.Single(product => product.IsPublished);
// or Enumerable.First(predicate)

return new PriceResponse
{
    DisplayPrice = product.Price
};
```

Requirements are intentionally narrow:

- target-project Roslyn semantics;
- `System.Linq.Enumerable.Single` or `First`, not Queryable;
- exact selected reference local;
- exact scalar auto-properties;
- direct final response object initializer;
- no user-defined conversion;
- exact selection invocation span;
- exact projection fact identity.

The backend business predicate becomes authoritative for selected API output only when the exact predicate/selection/projection path is proven.

## Current R7.9 / frontend rendered-value boundary

R7.9 must prove:

```text
C-proven API response property
→ explicit wire identity
→ typed frontend HTTP result member
→ exact resolved service/result assignment
→ exact component/view-model member
→ authoritative active visible Angular text interpolation
→ rendered UI value
```

The phrase **active visible Angular text interpolation** matters. Independent rereview #1 found that the previous boundary admitted inert/non-visible interpolation.

The repaired code is expected to reject:

```html
<ng-template>
  {{ displayPrice }}
</ng-template>

<!-- {{ displayPrice }} -->

<div data-price="{{ displayPrice }}"></div>
```

The current conservative behavior may reject dynamically instantiated `<ng-template>` content even when it could render at runtime. That is an accepted false negative until instantiation is proven. It must not be silently upgraded.

## Current bounded frontend R7.10 positive

```html
@if (displayPrice > 0) {
  <strong>{{ displayPrice }}</strong>
}
```

Only one supported enclosing active line-anchored Angular `@if` is accepted. Unsupported nested/multiple/ambiguous visibility fails closed.

This is not a general Angular compiler, arbitrary template expression, `*ngIf`, signal, outlet, embedded-view, or collection-filter solver.

## Exact composition identity to challenge

A joint edge must reuse exact fact IDs:

```text
renderedTerminal.frontendRenderFactId
= visibility.renderFactId

renderedTerminal.backendProjectionFactId
→ projection.selectionPredicateFactId
→ predicate.selectedApiProjectionFactId
```

Textual property/type/predicate similarity must not substitute for these IDs.

## Rereview #1 blockers already repaired

Rereview #1 failed the original candidate and added regression-first repairs:

```text
05ad6cb937010702a3fd01d5ef756e31bc4e8d9b
fix: fail closed on inert ng-template renders

099fabfcaeecbaf009b75052d20075745ee02437
fix: reject inert commented renders

da5d23771ef8c9d58d0333d1f949e8d742210043
fix: require visible text interpolation
```

Do not merely re-check those exact fixtures. Search for **another** compile-valid/runtime-valid shape that crosses the intended authority boundary.

High-value challenge areas include:

1. HTML/text lexical boundaries not covered by comment/tag/`ng-template` checks;
2. nested or malformed-but-compile-valid Angular template structure;
3. multiple interpolation occurrences on the same source line;
4. interpolation inside other inert/template-owned constructs;
5. frontend render facts that are active but not user-visible text under the claimed meaning;
6. same-name/same-text collisions that could bypass exact R7.9 fact identity;
7. backend overload/local/symbol collisions around selected API projection proof;
8. predicate authority attached to the wrong selection/projection;
9. frontend evidence upgrading observed-only backend evidence;
10. lower-authority evidence being dropped when joint composition fails.

## Focused regressions to inspect

```text
tests/Pkc.CSharp.Tests/JointVisibilityRegressionTests.cs
tests/Pkc.CSharp.Tests/JointVisibilityTemplateAuthorityRegressionTests.cs
```

Also inspect the production seams they exercise, especially:

```text
src/Pkc.CSharp/CSharpSelectedApiProjectionLineageEnricher.cs
src/Pkc.CSharp/CSharpSelectedApiPredicateAuthorityEnricher.cs
src/Pkc.Frontend/AngularRenderedMemberAuthorityFilter.cs
src/Pkc.Frontend/AngularRenderedMemberVisibilityEnricher.cs
src/Pkc.Frontend/AngularRenderedMemberVisibilityAuthorityFilter.cs
src/Pkc.Knowledge/JointVisibilityCandidateEnricher.cs
src/Pkc.Knowledge/JointVisibilityKnowledgeSynthesizer.cs
```

## Exact-SHA verification already PASS

On exact production SHA `da5d23771ef8c9d58d0333d1f949e8d742210043`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35650084914 — PASS
pinned Loren                                35650084917 — PASS
Loren-main canary                           35650084926 — PASS
pinned Jellyfin + parity/provenance         35650084759 — PASS
```

Core run confirms:

```text
Release build        0 warnings / 0 errors
C# tests             155 / 155 PASS
frontend tests       13 / 13 PASS
tool pack/install    PASS
WorkPlay             PASS
PokeTrade            PASS
```

These gates prove regression stability, not semantic correctness. Do not use green CI as a substitute for rereview.

## Repaired pinned real-repository benchmark

Benchmark wrapper:

```text
base implementation: da5d23771ef8c9d58d0333d1f949e8d742210043
branch:              benchmark/r710-rereview1-05ad6cb9
wrapper commit:      d38b21a68e007c1a85a8ec8e906a08f0db440661
run:                 35650761753 — PASS, 3 / 3 jobs
```

GitHub compare confirms the wrapper changes only one branch-trigger line in `.github/workflows/real-repo-benchmark.yml`; production/test source is byte-identical to `da5d2377...`.

Artifacts:

```text
jin12-xyz/CRM @ 00493af54d4d9e146d1c6eb75f5dc8f3898f09ec
PKC exit 0 | 415 facts | 1580 relations | 25 knowledge files
artifact 10662460505
sha256:f846b5384cc6472bf76ee469bfda888d5721664a440d2f18bf2e358d2f130753

hackersandwizards/agentic-engineering-training-angular @ 22f2aab64617f4de7984370a5bd40e8c9535dbf5
PKC exit 0 | 441 facts | 660 relations | 26 knowledge files
artifact 10661878054
sha256:1a0e1eae1af7cba2c786dfb2c38050387f42dd6f2cc00708af4020ecd9673286

kesetovic/crm-system @ 8e3b74bec4fdcd0144bd65f0c1b49c8e801bd2f7
PKC exit 0 | 488 facts | 2054 relations | 28 knowledge files
artifact 10662505428
sha256:48e3620862ec46aee4a1dbb119326be81fda60d38f8e44881208b1605bc1d399
```

All three currently emit:

```text
R7.9 rendered-value terminal: 0
selected API projection:      0
ui-member-visibility:         0
joint-visibility:             0
combined visibility rule:     0
```

Interpret this as fail-closed stress evidence only. R7.14 positive real-project yield remains **NOT YET PASS**.

Agentic mutation-causality cross-check remains:

```text
raw UpdatedAt mutation facts:        2
candidate UpdatedAt mutation facts:  0
runtime-pattern-variable:            retained
caller-object-unproven:              retained
transitive-mutation warning:         retained
false PO-facing UpdatedAt claims:    0
```

Detailed benchmark record:

`docs/benchmarks/2026-09-22-r7.10-real-repo-benchmark.md`

## Required review outcome

### If no new compile-valid/runtime-valid blocker exists

Record an independent PASS and then:

1. mark R7.10 PASS / COMPLETE;
2. mark all of V0.4.7-D PASS / COMPLETE;
3. update status, handoff, milestones and acceptance plan;
4. unlock **only V0.4.7-E**;
5. keep R7.14 as a required E completion gate — it is still NOT PASS;
6. keep V0.5 locked.

### If a blocker exists

Do not advance E. Record:

- exact compile-valid/runtime-valid counterexample;
- current false/over-authoritative output;
- exact violated proof boundary;
- minimum generic repair direction;
- regression required before implementation.

The repair should be regression-first and should not opportunistically broaden Angular/C# semantics beyond the demonstrated blocker.
