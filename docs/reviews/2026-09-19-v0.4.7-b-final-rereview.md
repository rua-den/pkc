# V0.4.7-B final independent rereview

Date: 2026-09-19

## Target

Final rereview covered only the repaired V0.4.7-B computation/causality and terminal-source boundary on exact production code:

```text
17fd30b3a4b8178208adabc12c40dee060bedb54
fix: fail closed after opaque terminal effects
```

V0.4.7-A and V0.4.6 were treated as closed baselines. C/D/E/V0.5 were not used to justify B acceptance.

## Prior blockers included in rereview

The review sequence found and repaired three stale-terminal-source classes:

1. unsupported nested simple member assignments and deconstruction member writes;
2. deconstruction reference reassignment/declaration aliases;
3. opaque/custom effects and unsupported jump control flow after a proven current value.

Representative stale-authority requirement remained constant across all repairs:

```text
historical derivation/origin may remain valid
but a later unsupported effect must not leave a false authoritative terminal-source claim
```

The final implementation applies a generic conservative boundary rather than fixture-specific exclusions. Once terminal/current-value authority exists, an unsupported/opaque suffix statement clears that authority and blocks stronger terminal claims for the remaining suffix while already emitted deterministic historical evidence is retained. Unsupported `goto`/label/throw control flow fails closed at method preflight.

## Permanent executable coverage

The repaired boundary now has runtime/semantic regressions for:

- nested assignment after derivation;
- top-level deconstruction member write;
- deconstruction reference reassignment;
- deconstruction reference declaration;
- compound and unary writes;
- custom constructor effect;
- custom setter effect;
- user-defined operator effect;
- user-defined conversion effect;
- `goto` path skipping a syntactically present derivation;
- existing invocation/branch/alias/custom-getter/missing-project-semantic negatives;
- point-in-time stored derivation behavior after later input override.

The positive B surface still proves supported multi-input scalar derivation, later compile-time-constant override causality, original-origin retention and direct-return terminal source.

## Boundary rereview

No new compile-valid/runtime-valid contradiction was found inside the documented supported shape.

The accepted scope is intentionally narrower than general C# dataflow:

```text
target-project semantic model
straight-line supported endpoint body
stored supported scalar auto-properties
supported scalar arithmetic derivation
supported direct copies / compile-time-constant overrides
supported direct return boundary
```

The implementation does not claim:

```text
general alias equivalence
general effect analysis
general symbolic execution
arbitrary control-flow/path proof
arbitrary helper/invocation dataflow
arbitrary persistence/API-call terminal proof
```

That distinction matters for R7.7: B closes the **supported direct-return** terminal-source slice. Persistence beyond that is not silently inferred and is not required to close B.

## Exact-main verification

All required exact-main gates passed on `17fd30b3a4b8178208adabc12c40dee060bedb54`:

```text
CI + full PKC tests + WorkPlay + PokeTrade   35379750213 — PASS
pinned Loren                                35379750276 — PASS
Loren-main canary                           35379750231 — PASS
pinned Jellyfin                             35379750251 — PASS
```

Core CI:

```text
Release build:       0 warnings / 0 errors
C# tests:            124 / 124 PASS
frontend tests:      13 / 13 PASS
tool pack/install:   PASS
WorkPlay:            PASS
PokeTrade:           PASS
```

Pinned Jellyfin:

```text
source commit:           1d7b6d97844c8cc848ed3fb5c4b48bb9cdd5b139
source build:            0 warnings / 0 errors
facts:                   43,365
relations:               195,316
workflow candidates:     386
product features:        116
canonical Markdown:      504
project-semantic facts:  43,365 / 43,365
portable bundle parity:  PASS
ZIP file-set parity:     PASS
ZIP byte parity:         PASS
raw .pkc leak:           none
src/ source-tree leak:   none
artifact id:             10561683704
artifact digest:         sha256:7d148704af898f82ecfd23d23e3ceeca6936a36da074a225aa4bafd95c042827
artifact size:           9,162,486 bytes
```

## Verdict

```text
PASS V0.4.7-B
B PASS / COMPLETE
```

No remaining compile-valid/runtime-valid counterexample is known inside the documented B proof boundary.

## Unlock disposition

Only checkpoint C is unlocked:

```text
V0.4.7-A          PASS / COMPLETE
V0.4.7-B          PASS / COMPLETE
V0.4.7-C          CURRENT / UNLOCKED
V0.4.7-D          LOCKED
V0.4.7-E          LOCKED
V0.5 Azure DevOps LOCKED
```

C must begin regression-first for exact backend entity/domain property → DTO/projection property → API response lineage, with exact project/symbol identity, source locations, PO-facing Markdown and same-name/custom-accessor/conversion/alias/effect fail-closed negatives. PokeTrade remains a known-answer gate and must not be changed merely to fit the analyzer; focused fixtures should supply explicit DTO/renamed-property cases when needed.
