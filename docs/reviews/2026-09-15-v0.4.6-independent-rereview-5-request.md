# V0.4.6 Independent Re-review 5 Request

Date: 2026-09-15

## Review target

Review PKC independently from repository state. Do not implement or modify production code during this review.

Review the exact production checkpoint:

```text
7f652c717c17f40f99a08b126b889a27a84c6376
fix: require complete predicate dependency proof
```

A later docs-only handoff commit may be at `main`; semantic review must still target the production SHA above.

## Read first

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/reviews/2026-09-15-v0.4.6-independent-rereview-4.md`
5. inspect production/test diff from `7ef7b9b0733c60bf1602c4490110f5c4cc3c77da` to `7f652c717c17f40f99a08b126b889a27a84c6376`

Stay strictly in V0.4.6.

## Prior disposition

```text
B6.1 PASS — keep closed
B6.2 PASS — keep closed
B6.3 PASS — keep closed
B6.4 BLOCK — re-review 4 dependency-completeness defect
```

Do not reopen B6.1/B6.2/B6.3 unless a new concrete contradiction is found.

## B6.4 defect from re-review 4

The previously reviewed same-type method-group projection proof could collect direct predicate members without proving that they were the complete semantic dependency set.

Counterexample:

```csharp
public IReadOnlyList<Card> GetCards() =>
    _cards
        .Where(card => card.IsPublished && IsAllowed(card))
        .Select(CloneCard)
        .ToArray();

private static bool IsAllowed(Card card) => !card.Blocked;

private static Card CloneCard(Card card) => new()
{
    IsPublished = card.IsPublished,
    Blocked = true
};
```

The old proof could see `card.IsPublished`, ignore `IsAllowed(card)`, prove `IsPublished` copied, and retain a false authoritative full-predicate inclusion rule.

## Implementation under review

Checkpoint `7f652c717c17f40f99a08b126b889a27a84c6376` replaces incomplete member discovery with a fail-closed dependency-completeness proof.

For same-type method-group projection preservation, every semantic reference to the predicate source parameter must now be a supported direct field/property read. Any unsupported whole-item use rejects preservation.

Expected accepted direct shape:

```text
card.IsPublished
(card).IsPublished
```

Expected conservative rejection examples:

```text
IsAllowed(card)
card.SomeMethod()
ReferenceEquals(card, ...)
custom/operator or other whole-item semantics involving card
```

After dependency completeness is proven, all required stored members must still be directly copied input-member → output-member by the projector.

Focused regression added:

```text
Whole_item_helper_dependency_downgrades_same_type_projection_authority
```

Existing positive and negative coverage that must remain valid includes:

```text
Predicate_preserving_same_type_method_group_projection_remains_authoritative
Same_type_method_group_that_changes_predicate_member_is_not_authoritative
Non_identity_projection_does_not_preserve_returned_filter_authority
Returned_filter_through_supported_projection_pipeline_remains_authoritative
Filter_passed_to_helper_that_discards_result_is_not_authoritative
```

## Reviewer questions

Please independently verify at least:

1. Does the new source-parameter reference walk actually prove dependency completeness for the supported predicate forms rather than merely collecting some direct members?
2. Can `helper(card)`, `card.SomeMethod()`, reference/object identity, element/indexer use, or operator/custom semantics still slip through while a direct member is also present?
3. Can parentheses or syntax nesting accidentally let an unsupported whole-item use look like a supported direct field/property read?
4. Does the fix preserve genuine direct-member same-type defensive clones without hardcoding PokeTrade, Mewtwo, Loren, Jellyfin, fixture paths, member names, routes, or classes?
5. Does arbitrary `Select` remain rejected unless identity/item semantics are deterministically proven?
6. Are B6.1/B6.2/B6.3 still closed with no new contradiction introduced by this two-file change?
7. Is the resulting Product Owner rule authority conservative enough that uncertain semantics downgrade/omit rather than fabricate an authoritative inclusion claim?

Adversarial counterexamples are encouraged. If any blocker remains, record the smallest compile-valid reproduction and do not implement the fix in the review thread.

## Exact automation evidence

All final gates for exact production checkpoint `7f652c717c17f40f99a08b126b889a27a84c6376` are green:

```text
CI + PKC tests + WorkPlay + PokeTrade   34933533049 — PASS
pinned Loren                            34933533044 — PASS
Loren-main canary                       34933533104 — PASS
pinned Jellyfin                         34933533050 — PASS
portable parity / provenance / no-leak  PASS
```

Core CI:

```text
PKC build:                         0 warnings / 0 errors
C# tests:                          76 / 76 PASS
frontend tests:                    13 / 13 PASS
tool pack/install:                 PASS
WorkPlay:                          PASS
PokeTrade backend:                 PASS
PokeTrade Angular:                 PASS
PokeTrade live business branches:  PASS
PokeTrade generated knowledge:     PASS
```

Pinned Jellyfin:

```text
repository:              jellyfin/jellyfin @ 1d7b6d97844c8cc848ed3fb5c4b48bb9cdd5b139
source build:            PASS, 0 warnings / 0 errors
facts:                   43,363
relations:               195,314
workflow candidates:     386
product features:        116
canonical Markdown:      504 files
analysis mode:           project-semantic 43,363 / 43,363
bundle canonical parity: PASS
ZIP exact file-set:      PASS
ZIP byte parity:         PASS
raw .pkc leak:           none
src/ source-tree leak:   none
artifact id:             10382099834
artifact digest:         sha256:f46bf8b748bb1e044b61c74f73139b23a12d1b2b0e37fcaf17d520dd89a43113
```

The coding sandbox had no local `dotnet`, so do not treat these GitHub runs as evidence that a local red/green loop occurred. They are exact-head final verification evidence.

## Required verdict

If the semantic blocker is genuinely closed and no new blocker-class contradiction is found:

```text
PASS
mark V0.4.6 COMPLETE
unlock V0.4.7 as next/current milestone
keep V0.5 Azure DevOps locked
```

If any blocker remains:

```text
FAIL / FIX REQUIRED
record concrete reproducible counterexample
update status/handoff/milestones
keep V0.4.7 and V0.5 locked
do not implement the fix in the independent-review thread
```

Preserve reviewer/coder separation.
