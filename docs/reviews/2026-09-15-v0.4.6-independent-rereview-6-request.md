# V0.4.6 Independent Re-review 6 Request — 2026-09-15

Review exact production checkpoint:

```text
18a1f1d1ef551833d859f23ea2e92dd548a6a81d
fix: fail closed on unsafe same-type projector effects
```

Do not semantically review a later docs-only handoff SHA as the production target.

Latest completed independent review:

```text
docs/reviews/2026-09-15-v0.4.6-independent-rereview-5.md
verdict: FAIL / FIX REQUIRED
```

## Reviewer read order

1. `docs/status.md`
2. `docs/handoff.md`
3. `docs/milestones.md`
4. `docs/product-knowledge-contract.md`
5. `docs/reviews/2026-09-15-v0.4.6-independent-rereview-5.md`
6. this request
7. inspect exact production checkpoint `18a1f1d1ef551833d859f23ea2e92dd548a6a81d`

Preserve reviewer/coder separation. This is an independent semantic review, not an implementation thread.

## What re-review 5 found

The prior checkpoint proved that predicate-relevant members were copied, but another initializer write could invoke a custom setter and invalidate the copied predicate state.

The review example used a `Blocked` setter that sets `IsPublished = false` after `IsPublished` had been copied.

Audit note: the exact source initializer ordering shown in re-review 5 (`IsPublished = true, Blocked = false`) would itself leave the source false before `Where`. The blocker remains valid; the new focused regression uses behavior-valid source ordering (`Blocked = false, IsPublished = true`) so the source passes `Where` before the clone invalidates copied state. Do not rewrite the prior review record.

## Production fix to challenge

The same-type method-group projection path now retains `Where` authority only when the entire supported clone initializer fits a closed safe model:

```text
predicate dependencies completely proven
AND every initializer entry is a simple assignment
AND every output target is a direct stored non-static field or auto-property
AND every assignment is exact same-member input → output copy
AND every predicate-required member is directly copied
→ projector may preserve authoritative Where semantics

otherwise
→ downgrade / no authoritative Product Owner inclusion rule
```

This intentionally rejects custom setters, nested/unmodeled initializer effects, helper or constant rewrites, different-member assignments and other output effects that are not proven safe.

Focused regression:

```text
Later_custom_setter_that_invalidates_predicate_state_downgrades_projection_authority
```

Existing positive regression that must remain valid:

```text
Predicate_preserving_same_type_method_group_projection_remains_authoritative
```

Arbitrary non-identity projection remains conservative:

```text
Non_identity_projection_does_not_preserve_returned_filter_authority
```

## Independent review questions

1. Does validating **every** initializer assignment, rather than only predicate-required assignments, close the custom-setter-after-copy authority gap?
2. Can a custom setter/getter, nested initializer, helper RHS, constant/rewrite, different-member copy, extra initializer write, aliasing or another unmodeled output effect still slip through the supported same-type projector path while retaining `Where` authority?
3. Is the exact same-member direct-copy requirement conservative enough for the supported object-initializer projector model?
4. Does the fix preserve the safe PokeTrade-style auto-property/direct-member defensive clone generically, without benchmark-specific production exceptions?
5. Do direct identity projection and arbitrary/non-identity `Select` boundaries remain correct?
6. Are B6.1, B6.2 and B6.3 still closed?
7. Is product-rule authority conservative enough to avoid false Product Owner claims?
8. Confirm that this authority downgrade is not being used as a reason to erase deterministic lower-authority evidence needed for value lineage/provenance and mutation/causality. The permanent direction is documented in `docs/product-knowledge-contract.md`; a future PO must be able to ask questions such as `ProductGroup.A → Product.A → Service.A` or `why did this status change?` without that evidence having been discarded.

For question 8, only raise a V0.4.6 blocker if the current implementation actually destroys evidence needed by the existing pipeline. Do not require V0.4.7 lineage synthesis to be implemented early.

## Exact-head automation evidence

All current push-triggered gates for production SHA `18a1f1d1ef551833d859f23ea2e92dd548a6a81d` passed:

```text
CI + full PKC tests + WorkPlay + PokeTrade   34954590262 — PASS
pinned Loren                                34954590188 — PASS
Loren-main canary                           34954590239 — PASS
pinned Jellyfin                             34954590155 — PASS
portable parity / no-leak                   PASS
```

Core CI evidence:

```text
PKC build:       0 warnings / 0 errors
C# tests:        77 / 77 PASS
frontend tests:  13 / 13 PASS
WorkPlay:        PASS
PokeTrade:       PASS
```

Pinned Jellyfin:

```text
jellyfin/jellyfin @ 1d7b6d97844c8cc848ed3fb5c4b48bb9cdd5b139
source build:            PASS, 0 warnings / 0 errors
facts:                   43,363
relations:               195,314
workflow candidates:     386
product features:        116
canonical Markdown:      504
analysis mode:           project-semantic 43,363 / 43,363
bundle canonical parity: PASS
ZIP file-set parity:     PASS
ZIP byte parity:         PASS
raw .pkc leak:           none
src/ source-tree leak:   none
artifact id:             10390258392
artifact digest:         sha256:6ba875d99cf64141267bb13811485cc025eabc87723beaa9f3a46a985860b497
artifact size:           9,016,989 bytes
```

Automation is supporting evidence only and must not override a semantic contradiction.

## Required disposition

If **FAIL**:

- record the smallest compile-valid and behavior-valid counterexample;
- keep V0.4.6 open;
- keep V0.4.7 and V0.5 locked;
- do not implement the fix in the review thread.

If **PASS**:

- mark V0.4.6 PASS / COMPLETE;
- unlock V0.4.7 as next/current milestone;
- keep V0.5 Azure DevOps locked.
